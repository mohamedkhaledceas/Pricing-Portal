# CEAS Portal — Implementation Tracker

**This is a living document.** It is the permanent, cumulative record of what has been built against the two source requirement documents, what has not, what was tested, and what changed over time. Every future audit session must **update this file in place** — append new Progress History / Change History / Decision Log / Regression rows, correct table statuses, and preserve everything below. Never regenerate this file from scratch and never silently delete a historical entry.

**Source documents (primary source of truth for requirements):**
- `CEAS_Portal_Governance_Workflow.pdf` — operational/process governance: launch timeline, ownership, RACI, access matrix, SOPs, training plan, go/no-go checklist, adoption scorecard, governance calendar. Referenced below as **"Governance doc"**, §N.
- `Employee_Portal_Sections_Requirements.pages` — the 22-section functional/data spec for the employee-facing portal. Referenced below as **"Portal doc"**, §N. (✅ marks in the original doc indicate bullets the doc's author believed were already done as of 2–4 Sep 2026 — each is independently re-verified below, not taken on faith.)

**Codebase audited:** `PricingPortal` repo, branch `feature/add-learn-with-marie-job-title` (1 commit ahead of `main`, HEAD `31688a2`), real app at `server/src/` (Express + better-sqlite3 — **not** the Postgres/Knex target described in some `docs/` files; see `docs/migration-plan.md`'s own 2026-09-08 reconciliation).

**Audit methodology:** Full read of both requirement documents; structural read of every module (`server/src/modules/{auth,employees,management}`), every migration (`server/src/db/migrations/001`–`021`), routes, services, repositories, and role/permission logic; live verification against the running local dev server (`http://localhost:3001`, real working SQLite DB `server/data/app.db`) via authenticated `curl` (admin session, GET-only, no mutations made to shared data); `git log` inspection for feature timelines; `npm audit` and test-infrastructure checks. No UI was exercised in a real browser in this pass (see Testing Status — this is a stated limitation, not a hidden one).

---

## 0. Audit History

| Audit date | Auditor/session | Scope | Tracker version |
|---|---|---|---|
| 2026-09-10 | Single coordinating Claude Code session, using five parallel research agents (each independently reading both source documents and a distinct slice of the codebase: profile/org/documents/policies; leave/time-off/team-availability; KPI/performance/required-actions; missing-modules sweep (training/notifications/announcements/requests/dashboard); governance/access/audit/testing/git-history) | Full bullet-by-bullet audit of both requirement documents against the live codebase | v1 (initial) |
| 2026-09-10 (same session) | Two of the five agents (the missing-modules-sweep agent and the leave/time-off agent) each independently spawned their own further sub-agents to cross-check the other agents' areas, and both wrote directly to this file rather than only to their assigned scratch files — an unplanned but useful convergence: their findings agreed on every material fact checked in common (the working-hours/schedule regression, `manager`=CEO role-mapping, the Team-KPI-Summary RBAC bug, absence of Documents/Policies/Training/Announcements/general-Notifications, the dead `finance` role, zero test infrastructure, and an identical `npm audit` result) | Coordinating session then independently re-verified the highest-stakes claims directly (re-ran `npm audit`, re-read `computeTeamSummary`, re-grepped `process.env` usage) before treating this file as final | v1.1 (cross-verified) |

**Reconciliation note (v1.1):** No factual correction to the tracker's tables was needed from the cross-check. One number was corrected on direct re-verification: the `process.env`-outside-`config/index.js` count (§7 below) was originally reported as 10 files/four ClickUp-related files; a direct repo grep found 11 files (five ClickUp-related), now corrected. The two agents also differed on a presentation choice, not a fact: one scored overall progress as an *unweighted* average across all 22 sections (≈60%); the other independently computed a *bullet-count-weighted* average (≈53%) as a cross-check — both are defensible, see §2 addendum below.

---

## 1. Executive Summary

The CEAS Portal is a working, actively-developed Express/better-sqlite3 application (not a prototype) with real users, a real local dev DB, and a real deployed instance. Its strongest area is **KPI/Performance** (Portal doc §8–11): a genuinely sophisticated, end-to-end, live-verified scoring engine with quarterly cycles, self-evaluation, anonymized peer review, ClickUp-automated metrics, CSV export, and full audit logging — arguably more advanced than the requirement doc's own description. **Leave/Time-Off** (§4–7) is similarly mature as a *request workflow* (two-stage manager → People & Culture approval, ClickUp sync, conflict-pair overlap warnings, real presence tracking for "who's online"), but is missing the specific **Leave Balance** concept the doc asks for in §3 — there is no entitlement/accrual/remaining-days ledger anywhere in the system, only a count of requests by type and outcome. **Employee Profile** (§1) and the self-service profile-edit approval workflow are strong; **Reporting & Organization** (§2) now has the data (a real manager FK), a real org-chart visualization (built 2026-09-14; this section's score/detail rows were only corrected to match on 2026-09-16), and a per-employee "My Reporting Line" breadcrumb (built 2026-09-16) — only "direct manager job title" remains a partial bullet in this section. Three entire requirement-doc sections are **not started at all**: **Employee Documents** (§16), **Policies** (§17, including the governance doc's *mandatory* policy-acknowledgment SOP), and **Training & Onboarding** (§13) — confirmed by zero relevant commits in git history. **Announcements** (§15) and general-purpose **Notifications** (§14) are also absent outside the KPI domain. **Personal & Contact Information** (§18) is missing phone, emergency contact, and profile-completion-percentage entirely.

**Correction (2026-09-20)**, superseding this paragraph's original claim: the KPI engine's *actual* data source today is manual portal entry for essentially the entire framework, not ClickUp. A real, well-built ClickUp automation layer exists (`kpi_auto_metric_mappings`, `kpiClickupMetricsService.js`, `computeAutoScores`, migrations `016`–`018`) — but a metric's `source_type` label (`'auto'`/`'semi'`/`'goals'`/`'odoo'`) is purely descriptive metadata; it does nothing by itself. Automation only actually runs for a metric once an admin has separately configured a `kpi_auto_metric_mappings` row for it, **and** an admin then clicks a button to trigger `computeAutoScores` (no cron — it never runs itself). Live-verified against the real DB 2026-09-20: **only 2 of ~60 Pillar B metrics across all 6 profiles have a configured mapping** (both on the `content` profile), and even that one automated path (a forward-only ClickUp webhook event log, `kpi_clickup_status_events` — a deliberate architecture documented in `docs/adr/0012`'s addendum, since ClickUp's API can't answer "how many times did this re-enter status X" retroactively) has only 2 events ever captured. `odoo`-labeled metrics have zero Odoo integration anywhere in the codebase — "Odoo" is only ever a UI badge (`kpi.js`'s `SOURCE_LABELS`) telling the human data-enterer where to go look the number up before typing it in, identical in mechanism to every other manually-entered metric. See §16 Change History (2026-09-20) for the full verification. The data-accuracy-ownership point below still stands for the 2 metrics that genuinely are automated, but does not generalize to "most Pillar B metrics" as originally written.

On the **Governance doc** side, most of the document is organizational process (meetings, SOPs, training sessions) that has no code surface by design and is correctly out of an implementation audit's scope — but several governance requirements *do* imply system support and are gaps: there is no in-portal audit-log viewer (the data is captured but not reviewable through the UI), no access-list export for the mandatory quarterly audit, no approval-gated account-provisioning workflow (registration is fully self-service, not the doc's HR-request → Ops-approval → Admin-provision chain), and the doc's 7-role model (Employee/Manager/HR/Operations/Commercial/CEO/Admin) does not map cleanly onto the code's 6 auth roles — most notably, the code's `manager` role **is** the CEO's account, "Commercial" and true line-management are not distinct roles at all (see §9 below), and a `finance` role exists in the code with zero permission gates anywhere (dead role).

The entire modular-monolith architecture (`modules/auth`, `modules/employees`, `modules/management`) is **roughly 3 weeks old** as of this audit — the foundation commit is dated 2026-08-18, and the KPI/ClickUp-automation module specifically is under 3 weeks old (`14a1baf`). This is a fast-moving, recently-restructured codebase, which contextualizes both its genuine strengths (KPI/leave engines are recent, well-thought-through work, not legacy cruft) and its gaps (Documents/Policies/Training/Announcements simply haven't been reached yet on a young build timeline, not abandoned).

The codebase has **zero automated tests** of any kind (confirmed: `npm test` fails with "Missing script", no Jest/Mocha/Supertest installed, no CI workflow exists) — every "Implemented" status in this tracker is backed by direct code reading plus manual, read-only live verification against the real dev server, never an automated test run. `npm audit` currently reports **3 moderate-severity vulnerabilities** (transitive `qs` package via `express`/`body-parser`) — this is new information as of this audit, not previously tracked.

## 2. Overall Progress

### Methodology

Progress is scored **per requirement-doc bullet**, not per section, to avoid one large multi-bullet section (e.g. KPI, at ~22 bullets) silently outweighing a small but business-critical one (e.g. Leave Balance, at 7 bullets) or vice versa. Each bullet in Portal doc §1–§22 gets a score: **Implemented = 1.0**, **Partially Implemented = 0.5**, **Not Started = 0.0**, **Unable to Verify = 0.5** (treated as a working assumption, not a claim — flagged wherever it materially affects a section's score), **Not Applicable = excluded** from the denominator. Governance-doc requirements are **not pooled into this percentage** — the Governance doc is overwhelmingly organizational process (SOPs, meetings, training cadence) outside the scope of a code implementation percentage; its code-relevant items are scored separately and discussed narratively in §9.

This produces two numbers: an **unweighted bullet-level average across all 22 Portal-doc sections**, and a **module-weighted view** (grouping sections into the areas a developer would actually work in) so a reader can see where effort is concentrated vs. thin.

### Portal-doc bullet-level score: **≈ 60%** *(headline not recomputed after the 2026-09-13 Leave Balance change — a targeted update, per §19's own guidance, doesn't redo the full 22-section recompute; the §3 row above is current)*

| # | Section | Bullets scored | Score | Notes |
|---|---|---|---|---|
| 1 | Employee Profile | 9 | 89% | One regression (working hours/schedule, dropped 2026-09-06) |
| 2 | Reporting & Organization | 4 | 88% (2026-09-16, was 50% at 2026-09-10 baseline) | Manager FK real; org chart real (built 2026-09-14, row corrected 2026-09-16); per-employee reporting-line breadcrumb also built 2026-09-16 — only "direct manager job title" (0.5) now holds this section back from 100% |
| 3 | Leave Balance | 7 | 100% | Implemented 2026-09-13 (fixed constants, computed on read, no ledger table needed); sick leave over 2 days now conditionally counts against the combined pool via P&C's doctor's-note checkbox (added same day); unpaid/public_holiday remain deliberately uncapped |
| 4 | Time Off Requests | 7 | 100% (2026-09-16, was 93%) | Mature two-stage workflow; "Number of requested days" closed 2026-09-16, see §4 detail table |
| 5 | My Leave Calendar | 3 | 83% | List-based, not a calendar widget |
| 6 | Team Availability | 7 | 86% | Aggregate counts added 2026-09-13; "Number offline" still not a dedicated backend metric |
| 7 | Upcoming Team Leave | 2 | 100% (2026-09-20, was 50%) | Built 2026-09-20: approved-only, name + date range, on both My Team and Overview, scoped to the viewer's own team (not company-wide, not just direct reports) — see §16 Change History |
| 8 | KPI Overview | 4 | 100% | |
| 9 | KPI Details | 5 | 100% | |
| 10 | Performance History | 4 | 100% (2026-09-20, was 88%) | "No trend chart" was a documentation error, not a real gap — a real bar-chart trend (`kpi-trend-row`) has existed since 2026-09-07, before the original baseline audit; fully re-verified 2026-09-20, see §16 |
| 11 | Employee Evaluation | 4 | 75% | "Manager evaluation" is architecturally anonymous peer review |
| 12 | Required Actions | 6 | 25% | KPI-siloed; 3 of 6 items have zero backing (docs/policies don't exist) |
| 13 | Training & Onboarding | 3 | 0%* | Zero relevant commits in git history |
| 14 | Notifications | 5 | 40%* | KPI-domain only |
| 15 | Announcements & Company Updates | 4 | 0% | Confirmed absent |
| 16 | Employee Documents | 5 | 0% | Confirmed absent |
| 17 | Policies | 4 | 13% | Leave *rules* exist; no policy *documents*/acknowledgment |
| 18 | Personal & Contact Information | 7 | 36% | No phone/emergency contact/completion % |
| 19 | My Team | 6 | 92%* | Strong — names/roles/dept/reports/expand-card |
| 20 | Team Performance | 3 | 67%* | **RBAC bug: real line managers can't see their own team's KPI summary — see §7/§11** |
| 21 | Requests Center | 7 | 100% (2026-09-16, was 15%) | Unified "My Requests" (leave + profile changes) built, with search/filter and per-request timeline; approval queues also moved here from "My Team" |
| 22 | Dashboard & System Experience | 16 | 55%* | Zero `@media` queries anywhere — not mobile-responsive |

*Rows marked with an asterisk (§13, §14, §19, §20, §21, §22) were scored primarily from a dedicated background verification pass (grep + route inspection + targeted file reads) run in parallel with this audit; treat their exact percentages as directionally solid but slightly less exhaustively cross-checked line-by-line than §1–§12, which were read in full.

**Unweighted average across all 22 sections: ≈ 66% (2026-09-20, was ≈ 60% at baseline) — §7 moved 50%→100% and §10 moved 88%→100%, see §16 Change History.**

*(v1.1 cross-check: an independent second pass computed a **bullet-count-weighted** average instead — i.e. each individual bullet across all 22 sections counts once, rather than each section counting equally regardless of how many bullets it has — using the same section-level percentages above. That method gives each section's own bullet count as weight (e.g. §22's 16 bullets vs. §7's 2), yielding **≈ 53%**, about 7 points lower. The gap is fully explained by weighting: the weakest sections (§13–§17, 24 bullets combined, ~5–13% each) get proportionally more influence under bullet-count weighting than under equal-per-section weighting, while strong small sections like §11 (4 bullets, 75%) get proportionally less. Neither number is "more correct" — they answer slightly different questions ("how does the typical *section* look" vs. "how does the typical *individual requirement* look") — both are reported here so a reader isn't anchored on a single number without knowing its sensitivity to the weighting choice.)*

### Module-weighted view (grouping into what a developer actually touches)

| Module / Area | Rough completion | Basis |
|---|---|---|
| **Auth & Access** (login, roles, session security, self-service registration) | ~75% | Solid, secure core (memory-only access token, httpOnly scoped refresh cookie, no permissive CORS, rate-limited login); gaps are process-alignment (no approval-gated provisioning) and an in-app audit-log viewer |
| **Employee Profile & Roster** (Portal §1, §2, §18) | ~60% | Strong work-data model + audited self-service approval flow; weak on personal/contact data; org chart + per-employee reporting-line breadcrumb both exist (2026-09-14/16) |
| **Leave / Time Off** (Portal §3–§7) | ~94% (2026-09-20, corrected — was stale at ~65%) | Mature request/approval engine; Leave Balance (§3) has been 100% since 2026-09-13 and Upcoming Team Leave (§7) since 2026-09-20 — this bullet's old "~65%"/"balance concept entirely missing" text predated both and was never propagated back here; only My Leave Calendar (§5, 83%, list not calendar) and Team Availability (§6, 86%, no dedicated "offline" metric) now hold this bucket short of 100% |
| **KPI / Performance** (Portal §8–§12) | ~80% | The most complete area of the whole portal; Required Actions is the one narrow gap |
| **Documents, Policies, Training, Announcements, general Notifications** (Portal §13–§17) | **~5%** | Effectively unbuilt — this is the single largest concentration of missing work |
| **My Team / Team Performance / Requests Center / Dashboard UX** (Portal §19–§22) | ~65% | Core data exists and is reused well; unified requests inbox built 2026-09-16 (§21); still no mobile responsiveness |
| **Governance/RBAC system support** (Governance §9, §12, §15, §21) | ~45% | Audit trail is real but not reviewable in-app; access-list CSV export added 2026-09-20; role model doesn't match the doc's 7 roles |
| **Testing & CI** | **0%** | Zero automated tests, no CI pipeline, of any kind |
| **Out-of-scope-but-real functionality**: Margin Planner (`/planner`), Commercial Leads dashboard (`/commercial-lead`), CEO Dashboard (`/ceo`) | N/A to this audit | Real, substantial, live features not described in either requirement document — see §7 |

---

## 3. Implementation Tracker

Full bullet-by-bullet tables, grouped by Portal-doc section. Evidence is cited as `file:line` where a specific line is meaningful, or a file/route path otherwise. "Live-verified" means an authenticated read-only `curl` call was made against the real running dev server during this audit.

**Last Verified: 2026-09-10** for every row below (this is the tracker's initial audit — no row has been individually re-verified at a later date yet). When a future update revisits a specific row, update that row's evidence/status and note the new date in prose next to the change (e.g. in §16 Change History); this table does not carry a per-row date column by default to keep it readable, but nothing stops a future update from adding one if the tracker starts being revisited piecemeal rather than in full passes.

**Stakeholders per section** (condensed from Governance doc §7's Ownership Matrix — Business Owner / Approver, plus the primary end-user(s) named in the Portal doc itself; not repeated per-row below to keep the tables scannable):

| Portal § | Section | Primary user(s) | Business Owner (Governance §7) | Approver (Governance §7) |
|---|---|---|---|---|
| 1 | Employee Profile | Employee | HR | HR |
| 2 | Reporting & Organization | Employee, HR | HR | Operations Manager |
| 3 | Leave Balance | Employee, HR | HR | HR |
| 4 | Time Off Requests | Employee, Manager | Employee | Manager |
| 5 | My Leave Calendar | Employee | Employee | — |
| 6 | Team Availability | Manager, Employee | Manager | — |
| 7 | Upcoming Team Leave | Manager | Manager | — |
| 8–9 | KPI Overview / Details | Employee, Dept Head | Department Head | Department Head |
| 10 | Performance History | Employee, HR | HR | HR |
| 11 | Employee Evaluation | Employee, Manager | Manager | HR |
| 12 | Required Actions | Employee, HR/Manager | HR / Manager (task creator) | HR |
| 13 | Training & Onboarding | Employee, HR | HR | Operations Manager |
| 14 | Notifications | All roles | Portal Admin | Operations Manager |
| 15 | Announcements & Company Updates | All roles | Operations Manager | CEO |
| 16 | Employee Documents | Employee, HR | HR | HR |
| 17 | Policies | Employee, HR, CEO | HR | CEO |
| 18 | Personal & Contact Info | Employee | Employee | HR (verification) |
| 19 | My Team | Manager | Manager | HR |
| 20 | Team Performance | Manager, Dept Head | Manager | Department Head |
| 21 | Requests Center | Employee, Operations | Operations | Relevant department |
| 22 | Dashboard & System Experience | All roles | Portal Admin | Operations Manager |

(Remember: "Manager" in the Governance doc's Ownership Matrix is ambiguous between "the CEO's account" and "a real line manager" per the role-mapping finding in §17 — read this table with that caveat in mind, not as a literal code-role assignment.)

### Portal §1 — Employee Profile

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Full name | Implemented | End-to-end | `employee.model.js:15-16` (joins `users.first_name/last_name`) | Manually Verified (live `/api/employees/me`) | — |
| Profile photo | Implemented | End-to-end | `middleware/photoUpload.js:1-104` (multer, magic-byte signature check, 2MB cap, UUID filenames); `rosterService.js:294-310` | Manually Verified (code + live route reachability) | Malware scanning is an explicit no-op stub (`photoUpload.js:100-102`, `TODO(security)`) |
| Job title | Implemented | End-to-end | `constants.js` (`JOB_TITLES`, 27 fixed values incl. "Learn with Marie" added `31688a2`); `rosterService.js:79-96`; now a fixed dropdown on all three edit surfaces (signup wizard, admin roster, and Account Settings' Work Details — the last was still free-text until `0abaeb9`, 2026-09-15) | Manually Verified (Account Settings dropdown browser-verified 2026-09-15, see §16) | — |
| Department | Implemented | End-to-end | Real `departments` table + FK (ADR-0011), migrations `010`/`011`; `departmentService.js` | Manually Verified (live `/employees/departments`) | — |
| Employment type (Full-time/Part-time/Freelancer) | Implemented | End-to-end | `employees.employment_type` CHECK constraint, migration `007` | Manually Verified | — |
| Joining date | Implemented | End-to-end | `employees.joining_date`, migration `007` | Manually Verified | — |
| Work location | Implemented | End-to-end | `WORK_LOCATIONS` fixed list, `constants.js`; relabeled Cairo/Alex, hybrid dropped (`e09ff93`, 2026-09-06) | Manually Verified | — |
| **Working hours / schedule** | **Not Started (regressed)** | Removed | Added in migrations `007`/`008`, then **dropped** by migration `021_drop_working_hours_and_work_schedule.js` ("removed as a product decision; there's no replacement") | Not Tested (feature removed) | Doc marks this ✅ (authored ~2 Sep); code dropped it ~6 Sep. **See Regression Tracking §12.** |
| Employee status (Active/On Leave/Remote) | Implemented | End-to-end | `active`/`remote` stored; `on_leave` computed live from approved leave overlapping today — never stored, can't drift (`rosterService.js:124-144`) | Manually Verified | — |

### Portal §2 — Reporting & Organization

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Direct manager name | Implemented | End-to-end | `employees.manager_employee_id` self-FK (migration `002`); `rosterService.js:159-170` resolves `managerName`/`managerEmail` | Manually Verified (live `/api/employees/directory`) | — |
| Direct manager job title | Partially Implemented | Backend only | Manager's full record (incl. `jobTitle`) is resolved server-side in `listDirectory` but only `managerName`/`managerEmail` are surfaced — `managerJobTitle` is one line away | `rosterService.js:159-170` | Not Tested | Trivial to add, not yet done |
| Reporting line (full chain) | **Implemented (2026-09-16, cards 2026-09-16 same day)** | Frontend, computed on read | New "My Reporting Line" card at the top of "My Team" (`team.js`'s `myReportingLineSectionHtml`) — a per-employee breadcrumb ("You → your manager → their manager → ... → CEO"), distinct from the Organization Chart (company-wide tree, Teams tab). Walks `managerEmployeeId` upward through the same already-loaded directory the org chart uses (no new endpoint), stopping at the CEO (`isCompanyManager`) or wherever the real chain actually ends — deliberately does *not* fake a continuation to the CEO the way the org chart's team-head tiering does, since this is meant to reflect the real FK chain, not an idealized structure. Rendered as full `.team-directory-card`s (same avatar/name/title/expand-detail as My Team's and the org chart's own cards, own `reporting-line-card-`/`reporting-line-detail-` id namespace) connected by arrows, not plain text, per user's own follow-up ask | `views/js/team.js` (`myReportingLineSectionHtml`, `reportingLineCardHtml`); `views/css/employees.css` (`.reporting-line-row`/`.reporting-line-arrow`) | **Manually verified twice in a real browser with a real, freshly-registered account** (`zzz-reportingline-test@ceastest.local`, deactivated/reactivated/deactivated between passes via the real API, not deleted). First pass: chain correctly stopped at a team head with no manager assigned. Second pass (after the underlying data had genuinely changed — a background ClickUp reconciliation job reassigned some `manager_employee_id`s between sessions): chain rendered 4 real levels deep (You → Team Head → Ada Designer → Janet Doe [CEO]) and correctly stopped at the CEO despite *her own* stale `manager_employee_id` — the CEO-stop condition this row previously flagged as untested got exercised live by accident, not by design | The literal `chain.length === 1 && isCompanyManager` single-card rendering path (viewer *is* the CEO, nobody above them) is still code-reviewed only, not separately browser-verified — no safe way to test as the real CEO account without resetting a real credential |
| Organizational structure (org chart) | **Implemented** (score row corrected 2026-09-16 — was wrongly still marked Not Started after the feature was built 2026-09-14) | Frontend, client-side | Originally built 2026-09-14 in `team.js`'s "My Team" tab (see that Change History row); moved 2026-09-16 into its own "Organization Chart" card on the new "Teams" tab (`teamsDirectory.js`) and restructured at the same time — see today's Change History row for the new root/team-head tiering. Renders a real nested-tree org chart with expand-on-click cards, sharing the same directory response the tab's department-grouped grid below it also uses (one fetch, two views) | `views/js/teamsDirectory.js` (`orgChartSectionHtml`, `orgChartNodeHtml`, `orgChartCardHtml`) | Manually Verified (live browser, both 2026-09-16 sessions) | Client-side tree, not a graphical chart-library visualization (no boxes-and-lines canvas rendering) — reads as a nested/indented list, which the requirement doc doesn't distinguish from a "chart" |

### Portal §3 — Leave Balance

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Annual leave entitlement | Implemented | Backend + UI | Fixed constants (14 Planned + 7 combined Emergency/Mental Health/Short-Notice = 21/year), no per-employee variation or proration | `leaveBalanceRules.js` | Manually Verified (live browser + curl) | None — by design, not admin-configurable yet; a `leave_policies` table is the stated trigger if that's ever needed |
| Annual leave used | Implemented | Backend + UI | Computed on read from approved `leave_requests` rows, summed in working days (partial-day = 0.5) | `leaveBalanceRules.computeBalances`; `timeOffService.getMyBalances` | Manually Verified | — |
| Annual leave pending | Partially Implemented | Backend only | `getLeaveBreakdown` still tallies *requests* by status, not days; the new balance intentionally only counts `approved` toward "used" (per the user's decision), so pending requests don't reduce the shown balance | `timeOffService.js:198-216` | Manually Verified | Same limitation as before for the *count-of-requests* breakdown; not in scope for the balance display itself |
| Annual leave remaining | Implemented | Backend + UI | `remaining = total - used`, surfaced on the Overview "Leave Balance" card and as a low-balance form warning at ≤30% remaining | `leaveBalanceRules.js`; `overview.js` (`leaveBalanceCardBody`); `timeOff.js` (`renderBalanceWarning`) | Manually Verified (live browser + curl) | Resets by calendar year/month, not by `joining_date` anniversary (explicit user decision) |
| Sick leave balance | Implemented | Backend + UI | `sick` ≤2 consecutive working days stays uncapped always; sick >2 days (already required a doctor's note per `sickLeaveRequiresDoctorNote`) now has a P&C-confirmation-time "Doctor's note provided" checkbox (`leave_requests.doctor_note_provided`, migration 022) — checked stays uncapped, unchecked deducts from the same 7-day combined pool as Emergency/Mental Health/Short-Notice | `leaveBalanceRules.sickCountsAgainstCombined`; `team.js` `doctorNoteCheckboxHtml`/`pcActions` | Manually Verified (live browser + curl, see §16 Change History 2026-09-13) | None — unpaid/public_holiday remain uncapped by design |
| Emergency leave balance | Implemented | Backend + UI | `emergency` now draws from the 7-day combined pool alongside `mental_health` and `short_notice` | `leaveBalanceRules.js` (`COMBINED_TYPES`) | Manually Verified | — |
| Unpaid leave records | Implemented | Backend + full UI (2026-09-16) | Real payroll-relevant fields: `salary_deduction`, `unpaid_days_count`, set by P&C at confirmation. **2026-09-16**: also now aggregated — `leaveBalanceRules.computeBalances` gained `sick`/`unpaid` buckets (uncapped, `used` only, no `total`/`remaining`), surfaced as "X Sick Leave taken this year" / "X Unpaid Leave taken this year" on Overview. On P&C's confirmation queue, this started as a fixed sick+unpaid pair on every card regardless of type, then was corrected the same day (per the user's own follow-up) to show only the *specific* leave type the pending request is actually for — `leaveBalanceRules.usageForLeaveType(balances, leaveType)` picks the one relevant bucket (capped or uncapped), attached as `leaveTypeUsage` on `listPcPending`'s response; null for public_holiday | migration `002:66-73`; live `/leave-requests/mine` (`salaryDeduction`, `unpaidDaysCount` present); `leaveBalanceRules.js` (`sick`/`unpaid` buckets, `usageForLeaveType`); `overview.js` (`uncappedBalanceRow`); `team.js` (`leaveTypeUsageHtml`) | Manually Verified — real end-to-end workflow across 4 leave types (sick/planned/unpaid, plus the pre-existing sick/unpaid pair), confirmed via both the API and a real browser (see 2026-09-16 Change History, both entries) | Previously not surfaced as an aggregate at all, only per-request — closed 2026-09-16; the P&C-queue fixed-pair-regardless-of-type behavior was itself a gap, closed same day |

Superseded note: an earlier version of this section carried a caution that a "peer/background Claude session" might already be working on Leave Balance. No corroborating evidence was ever found in `git log --all`/`git branch -a`, and the feature above is now actually implemented and live-verified — that note no longer applies.

### Portal §4 — Time Off Requests

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Quick Request Time Off button | Implemented | End-to-end | "New Request" sub-tab, `index.html:68`; `POST /employees/leave-requests` | Manually Verified | — |
| Leave request type | Implemented (richer than spec) | End-to-end | 9 types (`planned, short_notice, sick, emergency, mental_health, public_holiday, wfh, excuse, unpaid`) vs. doc's implied 4 | `timeOffService.js:3` | Manually Verified | `'planned'` = functional "annual leave" but not labeled that anywhere |
| Leave request dates | Implemented | End-to-end | `start_date`/`end_date` + `half_day`/`half_day_period` (finer than spec) | migration `002:60-63` | Manually Verified (live API) | — |
| Number of requested days | **Implemented (2026-09-16)** | End-to-end | Not persisted (deliberately — same "computed on read" pattern as leave balances), but now computed and attached to every request object the direct manager or P&C sees (`requestedDays` on `listTeam`/`listPcPending`'s responses), via a new `leaveBalanceRules.requestedDaysFor` (working days, partial_day halved — same rule that already deducts from balance). Same-day follow-up: the day range alone didn't say which weekdays a request actually falls on, so `requestCard`'s meta line now also shows the abbreviated weekday(s) — e.g. "Oct 5, 2026 → Oct 6, 2026, Mon → Tue (Full Day) · 2 days" — via a new `fmtDayOfWeek` | `leaveBalanceRules.js` (`requestedDaysFor`); `timeOffService.js` (`withRequestedDays`, `listTeam`/`listPcPending`); `views/js/team.js` (`requestCard`'s meta line, `fmtDays`, `fmtDayOfWeek`) | Manually Verified (live browser + API, see 2026-09-16 Change History, both entries) | User asked specifically for the manager's and P&C's queues; not added to the employee's own "My Requests" list or Leave Report (out of the stated scope, not a further gap) |
| Status (Pending/Approved/Rejected) | Implemented (richer workflow) | End-to-end | Real enum: `pending → manager_approved → approved`, or `rejected/auto_rejected/cancelled` — two-stage, not the doc's flat 3-state | migration `002:66` | Manually Verified (live) | Doc doesn't describe the 2-stage manager→P&C flow — see Gaps §8 |
| Approval/rejection date | Implemented | End-to-end | `manager_decision_at`, `pc_confirmed_at` | migration `002:69,71`; live API | Manually Verified | — |
| Approval/rejection comment | Implemented | End-to-end | `manager_decision_note`/`pc_decision_note`, required when rejecting | `timeOffService.js:222-224` | Manually Verified | — |

### Portal §5 — My Leave Calendar

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Recent leave requests | Implemented | End-to-end | `GET /employees/leave-requests/mine`, "My History" sub-tab | Manually Verified (live, 5 real rows returned) | — |
| Upcoming personal leave | Partially Implemented | Backend only | No dedicated "upcoming" endpoint; likely a client-side filter, not confirmed | `views/js/timeOff.js` (not read line-by-line) | Unable to Test | Confirm frontend actually filters to future dates |
| Personal leave history | Implemented | End-to-end | Same `/mine` endpoint | as above | Manually Verified | — |

*(Note: doc §5 is literally titled "Calendar" but nothing found in this pass renders an actual calendar-grid widget — it's chronological lists. Flag as a UI-fidelity gap, not re-verify without browser access.)*

### Portal §6 — Team Availability

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Who's off today | Implemented | End-to-end | `GET /employees/leave-requests/off-today?date=` | `timeOffService.js:161-171`; live-verified (200 OK, correct validation on missing `date`) | Manually Verified | — |
| Team availability today (aggregate) | Implemented | Frontend, computed on read | `teamAvailabilitySummaryHtml` in `overview.js`, a pure client-side aggregate over the already-fetched `/employees/directory` response | `overview.js` (2026-09-13) | Manually Verified (live browser, both Overview code paths) | Client-computed, not a dedicated backend endpoint — fine at 21-employee scale |
| Number of team members working | Implemented | Frontend, computed on read | Counts directory entries with `status === 'active'` | `overview.js` `teamAvailabilitySummaryHtml` | Manually Verified | — |
| Number on leave | Implemented | Frontend, computed on read | Counts directory entries with `status === 'on_leave'` (server already decorates this against today's approved leave — see `rosterService.decorateStatusList`) | `overview.js` `teamAvailabilitySummaryHtml` | Manually Verified | — |
| Number working remotely | Implemented | Frontend, computed on read | Counts directory entries with `status === 'remote'` | `overview.js` `teamAvailabilitySummaryHtml` | Manually Verified | — |
| Number offline | Unable to Verify | Backend only (inferred) | "Offline" = logical inverse of online, no dedicated endpoint confirmed | `employeeRepository.js:18-19` | Not Tested | Already surfaced indirectly via "Who's Online"'s own offline count; not folded into the new summary line |
| **Who's online** | **Implemented** | **End-to-end, real-time presence** | `common/realtime` tracks live Socket.IO connections per user id, pushes `presence:change` on every connect/disconnect; `rosterService.listDirectory` cross-references it directly (no DB query involved in the "online" flag at all anymore) | `common/realtime/index.js`; `rosterService.js` (`listDirectory`); `views/js/overview.js` (`applyPresenceChange`); `views/js/realtime.js` | Browser-verified live, both directions (connect + disconnect), 2026-09-15 — see §16 | Fixed 2026-09-15 (previously a 5-minute `last_seen_at` polling heuristic with no live UI refresh — see §16's "Who's Online" entry for the full root-cause and fix) |

### Portal §7 — Upcoming Team Leave

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Upcoming team leave | Implemented | End-to-end | New `GET /employees/leave-requests/team/upcoming` (`timeOffService.listUpcomingTeamLeave`), scoped via `teamMembership.resolveTeamMembership` (the same "My Team" definition as `rosterService.getMyTeam` — direct reports unioned with the rest of the department), `status = 'approved'` and `end_date >= today` only, returning just `{employeeId, name, startDate, endDate}` | `leaveRequestRepository.js` (`findApprovedUpcoming`); `timeOffService.js` (`listUpcomingTeamLeave`); `timeOffController.js`/`routes/index.js` (`upcomingTeamLeave`) | API- and browser-verified live 2026-09-20 — see §16 | — |
| Team leave calendar (UI) | Partially Implemented | UI, list not calendar | Rendered as a tile grid ("Upcoming Team Leave" card) on both My Team and Overview — same visual pattern as "Who's Off Today"/"Who's Online", not a literal calendar/month-grid widget | `views/js/team.js` (`upcomingTeamLeaveSectionHtml`); `views/js/overview.js` (`upcomingTeamLeaveSection`) | Browser-verified live 2026-09-20 — see §16 | A literal calendar-grid visualization was not requested and not built; the tile-list format matches this app's existing "who's out" UI convention |

### Portal §8 — KPI Overview

Fully re-tested 2026-09-20 — role-based authorization (API matrix), DB record verification, and live browser testing, not just code reading. See §16 Change History for the full test methodology and results.

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Overall KPI score | Implemented | End-to-end | `final.total` = Pillar A (60%) + Pillar B (40%) weighted, computed live | `kpiScoringService.js:computeBreakdown` | Live-verified 2026-09-20: hand-computed a real breakdown (3 metrics, 2 sources) against the live API response and the raw DB rows — exact match (`final.total: 9`, `pillarB.total: 9/40`); also browser-confirmed rendering | — |
| Current KPI evaluation period | Implemented | End-to-end | `getCurrentKpiQuarter()`, Cairo-timezone derived, with deadline/days-remaining | `kpiScoringService.js:254-271` | Live-verified 2026-09-20: `GET /kpi/current-quarter` → `{"quarter":"2026-Q3","deadline":{"daysRemaining":11}}`; browser-confirmed "Quarter closes 2026-09-30 · 11 day(s) remaining" | — |
| KPI achievement % | Implemented | End-to-end | `final.achievementPct` (== `final.total`, both out of 100) | `kpiScoringService.js:computeBreakdown` | Live-verified 2026-09-20: API and browser both showed "9.0% achievement" matching the hand-computed total | — |
| KPI status (On Track/Needs Attention/Off Track) | Implemented but Needs Changes | End-to-end | Real 5-tier band (Exceptional/Strong/On Track/Needs Improvement/At Risk) — a functional superset, but literal labels don't match the doc's 3-tier ask | `kpiScoringService.js:31-42` (`STATUS_BANDS`); `kpiShared.js` (`statusBadge`) | Live-verified 2026-09-20: a 9-point score correctly returned/rendered `at_risk`/"At Risk" with its "Performance conversation required" note, both via API and in the browser | Naming/spec mismatch, not a functional gap — open product decision: rename the bands to match the doc's 3 literal labels, or keep the finer-grained 5-tier system as a deliberate improvement |

### Portal §9 — KPI Details

Fully re-tested 2026-09-20, including the full authorization matrix (admin/P&C/self/direct-manager/unrelated-employee/operations) against three different write/view gates, **and a full page-by-page cross-check of every metric's name/weight/target/formula against the actual source spreadsheet** (`KPI_Framework.xlsx`, 8 sheets: Overview, Content Creators, Art Director, AI Designer, Production Team, Account Manager, People & Culture, Scoring Calculator). See §16 Change History.

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| KPI target | Implemented | End-to-end | `kpi_employee_targets` table (per-employee override, for `ratio_employee_target` metrics only — e.g. Production D1/D4); `setEmployeeTarget` | migration `015`; `kpiScoringService.js:333-361` | Live-verified 2026-09-20: admin set a real target (25) on D1 via the API, confirmed the row in the DB, then entered an actual value (20) and confirmed the score computed as `20/25×100=80` both via API and live in the browser | Target-setting is gated on auth role alone (`admin` or `manager`) — **not** on being that employee's actual manager; a real direct manager whose own auth role is plain `employee` is denied (confirmed live: 403). This is intentional per the service's own comment (an operational/production decision, not HR), but is a real, non-obvious authorization asymmetry worth knowing before assuming "the manager can always set the target." |
| Actual KPI result | Implemented | End-to-end | `kpi_scores.actual_value` — manual, self-eval, or ClickUp-auto sourced | migration `002`, `012`; `enterManualScore` | Live-verified 2026-09-20: entered real values as two different authorized actors (direct manager, P&C), confirmed both rows in `kpi_scores` with correct `entered_by`/`source`/`computed_score` | — |
| KPI weight | Implemented | Backend + UI | `weight_pct` per metric, baked into `kpiFrameworkSeed.data.js`, drives `weightedPoints = (score/100) × weightPct` | `kpiScoringService.js:buildPillarB` | Live-verified 2026-09-20: cross-checked the weighted-points math for 3 real entered metrics against the live breakdown response — exact match | — |
| KPI comments/feedback | Implemented | End-to-end | `kpi_scores.comment`, settable on manual entry, triggers an `employee_kpi_notifications` row | migration `012`; `kpiScoringService.js:enterManualScore` | Live-verified 2026-09-20: two comments entered by two different actors both landed correctly in `kpi_scores.comment`, both triggered a real notification row to the employee (confirmed in DB), and both rendered visibly (italicized) in the employee's own browser view | — |
| "View Performance Details" button | Implemented | UI (structurally different) | Literal button with that exact label; expands the KPI tab in place (switches to the History sub-view) rather than navigating to a separate page | `views/js/kpi.js` (button + `renderActiveView`) | Browser-verified 2026-09-20 (via the equivalent "History" sub-nav tab, which drives the identical code path) | Doc envisions a distinct page; app expands in place — functionally equivalent, not a real gap |
| *(new finding, not a doc bullet)* Authorization matrix | — | — | `canViewBreakdown` (admin/P&C bypass, else self-or-direct-manager-by-FK); `enterManualScore` (P&C bypass, else must literally be `employee.manager_employee_id` — **admin gets no bypass here**); `setEmployeeTarget` (admin/manager auth-role only, relationship-independent) | Full 6-actor matrix (admin, P&C, self, direct manager, unrelated employee, operations) tested live against all three gates via real throwaway accounts — every result matched the code exactly, no surprises found beyond the two documented in the "Gaps" column here and below | **Admin cannot enter a manual KPI score for anyone except an employee admin literally manages** (confirmed live: 403) — a deliberate design (P&C and the line manager own data entry, not admin), but easy to assume otherwise. Separately, **the KPI tab's own employee-picker UI never lets admin reach an unrelated employee at all** (it calls `/employees/team`, i.e. admin's own direct reports only, same as any non-P&C role) — so admin's broader `canViewBreakdown` bypass exists at the API layer but has no UI surface; reaching it requires a direct API call. Not a security issue (permissive backend + restrictive UI is the safe direction), but a real usability gap if an admin ever needs to review an arbitrary employee's KPI outside their own reporting line. |
| *(new finding, not a doc bullet)* Framework transcription fidelity vs. `KPI_Framework.xlsx` | Implemented | End-to-end for 6 of the enum's 8 profiles | `kpiFrameworkSeed.data.js` (`DEFINITIONS`) is a line-by-line transcription of the spreadsheet's 6 named role sheets | Every metric on every sheet (Content Creators, Art Director, AI Designer, Production Team, AM, P&C — 10-12 metrics each) checked by hand against the code: name, weight, target, and formula all match. Score bands (`STATUS_BANDS`) are a literal transcription of the Overview sheet's own 5-tier table — this is the actual source for the "5-tier vs. the Portal doc's 3-tier" naming gap noted in §8, not an arbitrary superset. One genuine ambiguity in the source spreadsheet itself (P&C's page says "People replaces Growth" but still lists both sections, summing to 50% literally) is correctly resolved in code (Growth dropped, Pillar B kept at 40%), with the reasoning documented in the code's own comment. | **`heads` and `design` are valid, selectable `kpi_profile` values (roster UI dropdown, `kpi_profile` CHECK constraint) with zero framework definitions anywhere** — they don't appear in the spreadsheet at all and were never added to `kpiFrameworkSeed.data.js`. Assigning either to a real employee would leave that employee's Pillar B permanently "Not yet defined," with no metrics ever available to enter a score against — no error or warning anywhere in the flow. Confirmed via the live DB that no employee currently has either profile, so this is dormant, not yet triggered — but a real landmine for whoever next assigns a "Head of Production"/"Creative Director"-type role via the roster table. |
| *(new finding, not a doc bullet)* ClickUp automation coverage | Partially Implemented | Real infrastructure, minimal configuration | `kpi_auto_metric_mappings` (admin-configured, per metric) + `computeAutoScores` (manual-trigger only, no cron) + `kpiClickupMetricsService.js` (4 real ClickUp-API-backed methods) | Queried the live DB 2026-09-20: **only 2 of ~60 Pillar B metrics have a configured mapping** (both on the `content` profile — `D4` and `Q1`), and the one webhook-driven method in use (`kpi_clickup_status_events`) has only 2 events ever captured. Every other metric, regardless of its `source_type` label, is entered manually via the same `enterManualScore` path documented above. | A `source_type` of `'auto'`/`'semi'`/`'goals'`/`'odoo'` on a metric is descriptive metadata only — it implies nothing about whether that metric is actually automated. `'odoo'`-labeled metrics (AM's D2/D3, P&C's P3) have **zero real Odoo integration anywhere in the codebase** — "Odoo" is only ever a UI badge (`kpi.js`'s `SOURCE_LABELS`) telling the human data-enterer where to look the number up. See the corrected Executive Summary paragraph (§1) and §16 Change History for the full picture — this corrects an earlier, overstated claim that ClickUp is "the real data source... for most Pillar B metrics." |

### Portal §10 — Performance History

Fully re-tested 2026-09-20. **Correction to the 2026-09-10 baseline audit**: "Performance trend" was recorded as "Partially Implemented — no trend line/sparkline," but a real bar-chart trend visualization has existed since this module was first built (`14a1baf`, 2026-09-07) — three days *before* that baseline audit ran. This was an audit miss, not a regression or later change; confirmed via `git log` and direct code/CSS reading, then live-verified with real 2-quarter data. See §16 Change History.

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Previous KPI results | Implemented | End-to-end | `computeHistory()` re-runs `computeBreakdown` per quarter with recorded data | `kpiScoringService.js:404-417` | Live-verified 2026-09-20: built real 2-quarter data (2026-Q2, 2026-Q3) for a test employee, confirmed `GET /kpi/:id/history` and the browser table both showed the exact correct per-quarter totals | — |
| KPI performance history | Implemented | End-to-end | Same data, rendered as a table in `kpiHistory.js` | as above | Browser-verified 2026-09-20: "Previous KPI Results" table rendered both quarters correctly (Pillar A/B, Final, Status columns) | — |
| Performance trend | **Implemented** (corrected from "Partially Implemented") | End-to-end | Real bar chart — `kpiHistory.js`'s `.kpi-trend-row`/`.kpi-trend-bar` (height-scaled per quarter, quarter-labeled), not just a list | `kpiHistory.js:36-44`; `employees.css:267-270`; built `14a1baf` (2026-09-07) | Browser-verified 2026-09-20 with real 2-quarter data: two visibly different-height bars rendered correctly (Q2 shorter/lower score, Q3 taller/higher score), labeled "Q2"/"Q3" | Only visually meaningful with 2+ quarters of history — a brand-new employee with one quarter sees a single bar, which is correct behavior, not a bug |
| Export performance history | Implemented | End-to-end | Real CSV (`quarter,pillarAScore,pillarBScore,finalScore,statusBand`), LF line endings (deliberately unescaped — values are always plain numbers/labels, never free text) | `kpiScoringService.js:exportHistoryCsv`; `kpiController.js:exportHistory`; `kpiHistory.js` (`downloadCsv`) | **Fully browser-verified 2026-09-20** (previously only route/code-verified, explicitly flagged as not click-tested): clicked the real "Export CSV" button in a live session, confirmed the file landed in `~/Downloads/kpi-history-32.csv`, and confirmed its content byte-for-byte matched both the live API response and the raw DB rows. Test file deleted afterward. | — |

### Portal §11 — Employee Evaluation

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Monthly/quarterly evaluation score | Implemented | End-to-end | Same quarterly breakdown system; **quarterly cadence only**, no monthly option exists | `kpiScoringService.js:39-42` | Manually Verified | Doc says "Monthly / quarterly" — only quarterly is real |
| Manager evaluation | Implemented but Needs Changes | End-to-end (different architecture) | Implemented as **anonymous peer review** aggregated into Pillar A (`kpi_pillar_a_reviews`), not a single named manager score — a deliberate design per ADR-0012 | migration `019`; `kpiPeerReviewService.js` | Manually Verified (structure); role semantics differ from the doc | Doc implies manager-attributed scoring; code is "everyone reviews everyone," anonymized |
| Employee self-evaluation | Implemented | End-to-end | `kpi_self_evaluations`, one row/employee/quarter, shown alongside but not folded into the official score | migration `013`; `kpiScoringService.js:370-403` | Manually Verified | — |
| Upcoming evaluation deadlines | Partially Implemented | Backend + UI | Only the *current* quarter's close date is surfaced — no forward multi-deadline list | `kpiScoringService.js:267-271`; `kpi.js:324,338` | Manually Verified | Single countdown, not a calendar of deadlines |

### Portal §12 — Required Actions

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Actions required from employee (general) | Partially Implemented | End-to-end (narrow scope) | `computeRequiredActions()` returns exactly 3 types, all KPI-domain: `self_evaluation`, `missing_kpi_scores`, `missing_pillar_a` | `kpiScoringService.js:435-466`; live-verified `/kpi/required-actions` | Manually Verified | KPI-siloed — see Key Finding below |
| Pending approvals/actions (cross-domain) | Not Started (for non-KPI) | — | No awareness of pending leave approvals or profile-change-requests in this aggregator | grep confirms no cross-domain aggregation | Not Tested | Would require a cross-module aggregation design |
| Pending KPI updates | Implemented | End-to-end | `missing_kpi_scores` action type | `kpiScoringService.js:448-457` | Manually Verified | — |
| Missing employee information | Not Started | — | No profile-completeness check anywhere in the required-actions computation | Confirmed via full read of `computeRequiredActions` | Not Tested | Depends on a profile-completeness concept existing (see §18) |
| Documents requiring signature | Not Started | — | No document-signing concept anywhere; Employee Documents (§16) doesn't exist | grep: no `signature`/`acknowledg` hits (excl. unrelated HMAC webhook "signature") | Not Tested | Blocked on §16 existing |
| Policies requiring acknowledgment | Not Started | — | Same — Policies (§17) doesn't exist | same grep | Not Tested | Blocked on §17 existing |

### Portal §13 — Training & Onboarding

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Required training | Not Started | — | Zero matches for "training" as a feature; `git log --all --grep="training" -i` returns zero commits | grep + git log confirm absence | Not Tested | Entire feature absent |
| Onboarding actions | Not Started | — | Zero matches; `git log --all --grep="onboarding" -i` returns zero commits | grep + git log confirm absence | Not Tested | Entire feature absent |
| Pending onboarding requirements | Not Started | — | Same | same | Not Tested | Entire feature absent |

*Cross-reference: Governance doc §18 "Onboarding Workflow" explicitly calls for a "Mandatory portal onboarding module (Day 1)" — this has zero backing in code. Governance §10's Training Plan is live/recorded-session training (not necessarily an in-portal module), so this is not automatically a contradiction — but the Portal doc's own §13 explicitly asks for in-portal training tracking, which does not exist.*

### Portal §14 — Notifications

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Notifications (general) | Partially Implemented | Backend + UI (KPI-scoped only) | `kpi_employee_notifications` table, `listNotifications`/`markNotificationRead` — no notification type outside the KPI domain | migration `014`; `kpiController.js` | Manually Verified (live `/kpi/notifications`, functional but empty) | No leave/profile/announcement notifications exist |
| Notification indicator | Partially Implemented | UI | KPI notification badge exists (`kpi.js:101` panel) — no portal-wide indicator | `views/js/kpi.js:101` | Not Tested | Scoped to KPI tab only |
| Approval notifications (leave, etc.) | Not Started | — | No notification fires on leave manager-decision/pc-confirm | grep confirms no notification write in `timeOffService.js` | Not Tested | — |
| KPI feedback notifications | Implemented | End-to-end | Covered by the KPI notification system | migration `014`, `kpiNotificationRepository.js` | Manually Verified | — |
| Policy update notifications | Not Started | — | Policies (§17) doesn't exist, so nothing to notify about | — | Not Tested | Blocked on §17 |

### Portal §15 — Announcements & Company Updates

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Company announcements | Not Started | — | Zero matches for "announcement" anywhere in code; `git log --all --grep="announcement" -i` returns zero commits | grep + git log confirm absence | Not Tested | Entire feature absent |
| Policy updates | Not Started | — | Same (depends on §17 too) | — | Not Tested | — |
| Important internal updates | Not Started | — | Same | — | Not Tested | — |
| Upcoming company events/meetings | Not Started | — | Same | — | Not Tested | — |

### Portal §16 — Employee Documents

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Employee contract | Not Started | — | Zero matches for a document-storage feature (excluding unrelated DOM `document.*` calls and one unrelated CEO-dashboard mock string) | repo-wide grep | Not Tested | Entire feature absent |
| Job description | Not Started | — | Same | — | Not Tested | — |
| NDA | Not Started | — | Zero matches for "NDA" | — | Not Tested | — |
| Employee handbook | Not Started | — | Zero matches for "handbook" | — | Not Tested | — |
| Download/view documents | Not Started | — | The only file-upload code in the repo is photo-upload (`photoUpload.js`), narrowly scoped to images; no generic document model/table/routes exist | — | Not Tested | `multer` is already a dependency and reusable, but no document schema exists |

### Portal §17 — Policies

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Company policies | Not Started | — | Zero matches for a policy-storage feature | repo-wide grep | Not Tested | Entire feature absent |
| Performance policy | Not Started | — | Same | — | Not Tested | — |
| Leave policy (as a document) | Not Started (rules ≠ document) | — | Leave *rules* (notice windows, auto-reject logic) exist and are shown under a "Leave Rules" sub-tab — but that's a computed rules engine, not a published, versioned policy document with acknowledgment | `timeOffRules.js`; `views/js/timeOff.js` "Leave Rules" sub-tab | Manually Verified (rules exist) / Not Started (as a document) | The distinction matters for the governance SOP's acknowledgment requirement |
| Policy acknowledgment | Not Started | — | No acknowledgment-tracking mechanism of any kind | grep: zero "acknowledg" hits | Not Tested | **This is a *mandatory* SOP item per Governance §12/§13 with a hard 5-business-day compliance window — zero backing implementation.** |

### Portal §18 — Personal & Contact Information

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Employee personal information | Partially Implemented | Backend only (work info only) | Only work-related fields exist; no DOB/national ID/address etc. | `employee.model.js:5-31` (full field list) | Manually Verified (schema read) | No true "personal info" fields beyond identity/work data |
| Contact information | Not Started | — | Only `users.email` (login/work email) exists | grep: no `phone`/`emergency_contact`/`personal_email`/`address` hits | Not Tested | No phone field anywhere |
| Emergency contact | Not Started | — | No fields exist in schema at all | same grep | Not Tested | HR/safety-relevant gap |
| Work email | Implemented | End-to-end | `users.email`, returned on every employee/directory response | `employee.model.js:14` | Manually Verified (live login response) | — |
| Work phone if applicable | Not Started | — | No phone field of any kind | same grep | Not Tested | — |
| Profile completion percentage | Not Started | — | No completion computation anywhere in the employees module (only unrelated KPI peer-review "completion rate" hits) | `grep -rn "completion"` — 9 matches, all KPI-unrelated | Not Tested | — |
| Ability to update allowed personal information | Implemented (for work fields; N/A for absent personal fields) | End-to-end | Real two-tier self-service model: first edit applies immediately and locks the profile; every edit after that becomes an audited change-request an admin/manager/P&C must approve | `rosterService.js:325-384`; migration `008`; `employeeProfileChangeRequestRepository.js`; routes `/profile-change-requests/:id/approve|reject` | Manually Verified (code + live route reachability) | Approver role set is `admin/manager/people_culture`, not literally "HR" — a naming/role-mapping note, not a functional gap |

### Portal §19 — My Team

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| My Team section for managers | **Fixed (2026-09-12)** | End-to-end | `maintab-team` nav tab; `myTeamSectionHtml()` now renders the result of a new backend endpoint (`GET /employees/team/mine` → `rosterService.getMyTeam`) instead of computing membership client-side. Membership rule (shared with Team Performance, see §20): no current direct reports → team is the rest of your department; at least one direct report → team is those reports unioned with the rest of your department (deduplicated), computed dynamically from `manager_employee_id`, not the `is_team_head` flag or auth role. Previously (bug, found and fixed this session): the client-side filter matched people who shared *your own* manager (peers) OR your department, not people who report to *you* — so a manager's direct report in a different department never appeared in the manager's "My Team", and unrelated peers did. | `index.html:62`; `views/js/team.js`; `rosterService.js` (`getMyTeam`); `services/teamMembership.js` (new, shared rule); `employeeRepository.js` (`findByDepartment`); `rosterController.js`/`routes/index.js` (`/employees/team/mine`) | **Manually Verified end-to-end in a real browser** — created 3 throwaway test employees (`zzz-debug-a/b/c@ceastest.local`; A=operations dept & manages B via `manager_employee_id`, B=content dept, C=operations dept/no reporting link) via the real registration + admin APIs, logged into A's account in a real browser session at `localhost`, and confirmed My Team shows exactly B (the cross-department direct report — previously invisible, the reported bug) + C (department match) + one pre-existing department colleague; also confirmed B (no reports) sees only its department roster plus A as "Manager". No console errors, no failed network requests. Deactivated all 3 test accounts afterward (not deleted). | Visible to anyone with a manager or department, not gated to formal "manager" role only — broader than doc implies, not narrower |
| Team member names | Implemented | End-to-end | `myTeamCardHtml()` | `team.js:26-41` | Manually Verified | — |
| Team member roles (job title) | Implemented | End-to-end | Card shows `jobTitle` | `team.js:33` | Manually Verified | — |
| Team member departments | Implemented | End-to-end | Card shows department label | `team.js:37` | Manually Verified | — |
| Employees reporting directly to the manager | Implemented | End-to-end | `getDirectReports(managerEmployeeId)` | `rosterService.js:189-191` | Manually Verified | — |
| Quick link to team member profile | Partially Implemented | UI | Card expands inline (accordion) to show department/email/manager — not a navigable "profile page" link | `team.js:44-52` | Manually Verified | Functional but not literally a "quick link to profile" |

### Portal §20 — Team Performance

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Team KPI summary where permission allows | **Fixed and browser-verified (2026-09-10); membership + permission rule refined and re-verified (2026-09-12)** | End-to-end | `kpiScoringService.computeTeamSummary` returns different shapes by caller. Non-company-wide branch, as of 2026-09-12: **permission** to view the page is now "do you currently have at least one direct report" (dynamic, `findByManagerId`), not the `is_team_head` flag — this closes a real bug found live today (see §20 table's "Team performance visibility" row): the flag and the frontend's own tab-visibility check (`kpiPerms.hasReports`) could disagree, showing the tab and then 403ing. **Membership**, once permitted, uses the same shared rule as "My Team" (`services/teamMembership.js`): direct reports unioned with the rest of the employee's department, deduplicated. `manager` (CEO)/`admin` still get `{ myTeam, byDepartment }` instead of one flat company-wide list — `myTeam` is their own direct reports (same FK), `byDepartment` is every department's real, current membership, independent of `myTeam` (confirmed intentional with the user, not deduplicated); this company-wide branch was deliberately left unchanged, since it's a separate, broader "see every department" feature. Frontend (`kpi.js`, `kpiTeam.js`) unaffected (only the non-company-wide array's composition and its 403 condition changed, not the response shape). | `kpiScoringService.js` `computeTeamSummary`/`summaryRow`; `container.js` (added `departmentRepository`, then `teamMembership`, dependencies); `kpi.js` (tab-visibility gate); `kpiTeam.js` (full rewrite) — commit `16d8341` (2026-09-10) on branch `feature/team-kpi-summary-department-view`; `services/teamMembership.js` (2026-09-12, working-tree change) | 2026-09-10 shape/RBAC change: **Manually Verified end-to-end in a real browser** (see notes in prior entry). 2026-09-12 refinement: **Manually Verified end-to-end in a real browser** — the same throwaway test manager (`zzz-debug-a`, a real direct report, `is_team_head=false`) first reproduced the 403-despite-visible-tab bug live, then confirmed the fix: Team Performance now renders and matches My Team's membership exactly (3 rows: the cross-department direct report, the department colleague, and the pre-existing department member). | None remaining. |
| Team KPI summary — "Team Head" badge (manager/admin view only) | **Implemented and browser-verified (2026-09-10)** | End-to-end | Follow-up UI request, not in either source doc: within the manager/admin `{myTeam, byDepartment}` view specifically (not the team head's own flat view — confirmed with the user this is scoped to admin/manager accounts only), any row where `isTeamHead === true` gets a "Team Head" badge next to their name, reusing the exact same `badge badge-approved` style `roster.js` already uses for the identical flag, rather than inventing a new one. | `kpiScoringService.js` `summaryRow` (added `isTeamHead: entry.isTeamHead` to the returned row); `kpiTeam.js` (`summaryTableHtml`'s new `showTeamHeadBadge` parameter, `true` for `myTeam`/`byDepartment` call sites, `false` for the team-head's own flat-array call site) | Manually Verified in a real browser — reactivated the same 3 throwaway test accounts, confirmed a real existing team head ("Mohamed Khalid") showed the badge in the live "AI & Innovation" department section, and the dedicated test team head ("ZZZ_TEST TeamHead") showed it in the "Test" department section while "ZZZ_TEST Report1"/"Report2" correctly showed no badge | None remaining. |
| Team availability (in this section) | Implemented | Backend only | Covered by the Leave module's "off today"/"who's online" features (see §6) | see §6 | Manually Verified | Not a dedicated "team performance" widget, reuses §6's data |
| Team performance visibility based on permissions | **Fixed (2026-09-12)** | End-to-end | Stale row — the RBAC gap this described was closed 2026-09-10 (see row above). Left stale until today, when a *related but distinct* bug was found and fixed while re-verifying: the "Team Performance" sub-tab's visibility (frontend, `kpi.js`'s `kpiPerms.hasReports` — has this employee been listed as anyone's `manager_employee_id`) and its actual backend 403 gate (`computeTeamSummary`, previously `actorEmployee.isTeamHead`) used two *different* definitions of "manages people" — an employee with a real direct report but `is_team_head=false` saw the tab, clicked it, and got "You do not have permission..." Fixed by making the backend gate use the same dynamic reports-based definition as the tab and as `teamMembership.js`, so the two can no longer disagree. | `kpiScoringService.js` `computeTeamSummary` (permission check) | **Manually Verified in a real browser** — reproduced the 403-despite-visible-tab bug live with a throwaway test manager (`zzz-debug-a@ceastest.local`, a real direct report, `is_team_head=false`), confirmed the fix resolves it (Team Performance now renders the same 3-person team as My Team) | None remaining. |

### Portal §21 — Requests Center

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Recent requests (unified, cross-type) | **Implemented (2026-09-16)** | End-to-end | New "Requests" tab, "My Requests" section: every employee's own leave requests + own profile-change requests, merged client-side and sorted most-recent-first. Genuinely new — nothing before this combined the two types anywhere | `requestsCenter.js` (`renderRequestsCenter`, `myRequestsCache`); new `GET /employees/profile-change-requests/mine` (`rosterService.getMyChangeRequestHistory`, backed by a new `employeeProfileChangeRequestRepository.findByEmployeeId`) | Manually Verified (live browser + curl, see 2026-09-16 Change History) | Doesn't include `conflict_pairs` (not a request a user submits — a config/detection table, deliberately out of scope, confirmed with the user) |
| Request date | **Implemented** — now genuinely unified, not just "exists per-silo" | End-to-end | Every row in "My Requests" shows its date (leave: range + weekday; profile change: submitted date) | `requestsCenter.js` (`myRequestRowHtml`) | Manually Verified | — |
| Request category | **Implemented** — same correction | End-to-end | Leave type label or "Profile Change", shown per row and filterable | `requestsCenter.js` (`categoryLabelFor`) | Manually Verified | — |
| Request status | **Implemented** — same correction | End-to-end | Status badge per row (shared leave/profile-change status vocabulary), filterable | `requestsCenter.js` (`statusLabelFor`) | Manually Verified | — |
| Request details | **Implemented** — same correction | End-to-end | Leave: reason. Profile change: the field-level diff (reused from the existing P&C review card's own diff renderer) | `requestsCenter.js` (`changeRequestDiffHtml`) | Manually Verified | — |
| Search / filter requests | **Implemented (2026-09-16)** | Frontend, client-side | Free-text search + category + status + date-range filters over "My Requests", all client-side (this app's per-employee request volume is tiny — no server-side search/pagination needed) | `requestsCenter.js` (`matchesFilters`, `window.filterMyRequests`) | Manually Verified (live browser — text search narrowed correctly; category/status use the identical filter path) | Scoped to "My Requests" only, per the user's own decision — not applied to the relocated approval-queue sections below it |
| Activity / history log | **Implemented (2026-09-16)** — per-request timeline, not the `audit_log` table | Frontend, computed on read | User's own choice among two options offered: a timeline *per request* (submitted → manager decision → P&C decision / reviewed), not one combined chronological feed. Built entirely from timestamps/notes already on the request/change-request row — no new table, nothing extra fetched | `requestsCenter.js` (`timelineHtml`, `window.myRequestToggleTimeline`) | Manually Verified (live browser — expand/collapse, correct actor names via the directory, correct notes) | The `audit_log` table (internal, admin-facing) remains separate and still not user-facing — unchanged, not what this bullet needed |

### Portal §22 — Dashboard & System Experience

| Requirement | Progress | Depth | Evidence | Testing | Gaps |
|---|---|---|---|---|---|
| Dashboard greeting + current date | Implemented | UI | Overview tab renders a greeting/summary header | `views/js/overview.js` | Manually Verified (code read) | — |
| Quick actions section | Partially Implemented | UI | Some quick-nav buttons exist (switch to roster/team/KPI tabs) but not a dedicated "Quick Actions" widget matching the doc's exact list | `overview.js:167,212,259,295,306,364` | Not Tested | — |
| → Request Time Off | Implemented | UI | Tab switch to Time Off | as above | Manually Verified | — |
| → View KPIs / Performance | Implemented | UI | Tab switch to KPI | as above | Manually Verified | — |
| → View Team | Implemented | UI | Tab switch to Team/Roster | as above | Manually Verified | — |
| → Update Profile | Partially Implemented | UI | Profile editing lives in Account Settings (accessed via account menu), not a one-click Overview action | inferred from `rosterService.js` `updateMine` being the backing call | Unable to Test (not browser-exercised) | — |
| → View Documents | Not Started | — | Feature doesn't exist (§16) | — | Not Tested | — |
| → View Policies | Not Started | — | Feature doesn't exist (§17) | — | Not Tested | — |
| Useful empty-state CTAs | Partially Implemented | UI | Some empty states exist (e.g. "No other teammates found yet", `team.js:84-86`) but not confirmed comprehensive across every panel | spot-checked in `team.js` | Not Tested | — |
| Hide irrelevant sections when no data/role | Implemented | UI | Nav tabs (`maintab-team`, `maintab-roster`, `maintab-leave-report`, `tab-users`) are `style="display:none"` by default and shown via JS role checks | `index.html:62-64`; `main.js` role-gating logic | Manually Verified (HTML source read) | — |
| Role-based visibility for employees/managers/HR/admins | Implemented | End-to-end (server + client) | Both client-side nav hiding **and** server-side `requireRole`/service-level role checks exist for every sensitive route reviewed in this audit | multiple, see Governance §9 table below | Manually Verified | — |
| Clear alerts for items requiring immediate action | Partially Implemented | UI | KPI required-actions and notification badges exist; no unified "alerts" surface spanning leave/profile/other domains | `kpi.js` notification panel | Not Tested | Same KPI-siloed limitation as §12/§14 |
| **Mobile-responsive dashboard** | **Not Started** | — | **Zero `@media` queries found in any of the 4 module CSS files** (`login.css`, `employees.css`, `ceoDashboard.css`, `commercialLeads.css`) | `grep -l "@media" src/modules/**/*.css` → no output | Not Tested | Hard, verifiable gap — directly fails Governance §21's "Mobile responsiveness checked" launch-checklist item |
| Filters by date/status where relevant | Partially Implemented | UI | Some filtering exists in specific views (e.g. off-today by date); no universal filter pattern | scattered, not centrally verified | Not Tested | — |
| Notification indicator for new requests/approvals/KPI/policy | Partially Implemented | UI | KPI notifications only, as in §14 | see §14 | Not Tested | — |
| Export/download option for leave and performance history | Partially Implemented | Backend (KPI only) | Only `kpi/:employeeId/history/export` (CSV) exists — no leave-history export | `kpiController.js:60-66` | Manually Verified | Doc explicitly asks for leave export too — absent |

---

## 4. Implemented

Fully end-to-end, live-verified, and matching (or exceeding) the requirement doc's intent:

- Employee Profile core fields (name, photo w/ magic-byte validation, job title, department, employment type, joining date, work location, computed status) — Portal §1
- Real manager reporting-line FK (`manager_employee_id`) and directory manager-name resolution — Portal §2
- Two-stage (manager → People & Culture) leave approval workflow with ClickUp sync, conflict-pair overlap warnings, and audited decisions — Portal §4
- Real-time-ish presence ("who's online," 5-min staleness window) — Portal §6
- The entire KPI scoring engine: overall score, quarter tracking, targets/weights/comments, CSV history export, self-evaluation, anonymized peer review (Pillar A) — Portal §8–§11
- Self-service profile-edit lock + audited change-request approval workflow — Portal §18
- My Team directory (names/roles/departments/direct reports) — Portal §19
- Team KPI summary, correctly scoped to the reporting-line FK for team heads (fixed 2026-09-10 — this line was inaccurate in the original v1 audit, which listed this as working while §7 correctly flagged it as broken; both are now consistent) — Portal §20
- Server-side + client-side role gating on every sensitive route reviewed (Margin Planner, CEO Dashboard, Commercial Leads, roster management, leave decisions) — Governance §9
- Least-privilege signup default (`employee` role, no self-elevation path) — Governance §15
- Access-token-in-memory / httpOnly-scoped-refresh-cookie / no-permissive-CORS security model — Governance §9, CLAUDE.md
- **Module-boundary discipline (CLAUDE.md's non-negotiable module-isolation rule) is fully compliant** — a repo-wide grep for cross-module `require()` calls found zero violations; every inter-module reference goes through that module's own `index.js` public interface, exactly as specified
- Audit-log coverage of every mutation in every module that actually exists (30+ call sites) — see the Governance §12 coverage table in §9 below
- **Leave Balance** (§3) — fixed 21-day/year entitlement (14 Planned + 7 combined Emergency/Mental Health/Short-Notice), 1 WFH day/month, 4 Excuse hours/month; Overview card + non-blocking low-balance and notice-period form warnings; sick leave over 2 days conditionally joins the combined pool via P&C's doctor's-note checkbox; live-verified 2026-09-13 (see §16 Change History). Unpaid/Public Holiday remain deliberately uncapped.

## 5. Partially Implemented

- ~~Reporting line/org structure (§2) — one-level FK exists; no chart, no multi-level chain endpoint~~ — **Closed 2026-09-16.** A real org chart (client-side tree, built 2026-09-14, moved/restructured 2026-09-16) and a per-employee "My Reporting Line" breadcrumb (built 2026-09-16, "My Team" tab) both now exist — see Change History for both.
- ~~Team Availability aggregates (§6) — count-based summaries (# working/on leave/remote/offline) are not~~ — **Done 2026-09-13**, see §16 Change History (offline count still not a dedicated metric, minor)
- Employee Evaluation (§11) — self-eval and quarterly scoring are real; "manager evaluation" is architecturally anonymous peer review, not a named manager score
- Required Actions (§12) — real but entirely KPI-scoped; 3 of 6 doc bullets have zero backing
- Notifications (§14) — real but entirely KPI-scoped
- Personal & Contact Information (§18) — work-side data is solid; no phone/emergency-contact/completion-% fields exist
- ~~Requests Center (§21) — the underlying data (leave, profile-change, audit log) all exists; there is no unified cross-domain view~~ — **Closed 2026-09-16.** Unified "My Requests" built (leave + profile changes, search/filter, per-request timeline); approval queues also moved here from "My Team" — see Change History.
- Dashboard quick actions / empty states / filters / notification indicator (§22) — partial coverage, not systematic across every panel

## 6. Missing / Not Started

- **Organizational chart visualization** (§2)
- **Training & Onboarding module** (§13) — zero commits in git history
- **Announcements & Company Updates** (§15) — zero commits in git history
- **Employee Documents** (§16, contract/job description/NDA/handbook storage) — zero commits in git history
- **Policies module + acknowledgment tracking** (§17) — a **mandatory** governance SOP item with a hard 5-business-day compliance window and zero implementation
- **General-purpose (non-KPI) notifications** (§14) — leave approvals, policy updates, announcements all fire no notification of any kind
- **Mobile-responsive CSS** — zero `@media` queries anywhere in the codebase (Governance §21 launch-blocker item)
- **In-app audit-log viewer** — the data is captured (`audit_log` table + mirrored JSON logs) but there is no `/api/audit` route or admin UI to review it (Governance §9, §15)
- **Access-list export** for the mandatory quarterly access audit (Governance §9)
- **Automated tests of any kind** — zero test files, no CI pipeline

## 7. Needs Correction

- ~~Team KPI Summary was scoped by auth role instead of the real reporting line (§20)~~ — **Fixed and browser-verified 2026-09-10.** `kpiScoringService.computeTeamSummary` previously granted access to `MANAGER`/`ADMIN` auth roles only and returned the entire company's KPI standing as one undifferentiated list, silently denying the feature to every real team head who wasn't the CEO or an admin. Fix (confirmed with the user in two rounds — first the RBAC scoping, then a refinement to split company-wide into `myTeam` + per-department groups instead of one flat list): team heads get their own direct reports (flat); `manager`/`admin` get `{ myTeam, byDepartment }`. A second bug was found and fixed in the same pass: the frontend never showed the "Team Performance" tab to team heads at all (gated on auth role only), so the backend fix alone wouldn't have been visible to any real team head. See §20 table above for full verification detail.
- **`working_hours`/`work_schedule` regression** (§1) — was implemented, then deliberately dropped by migration `021` on 2026-09-06; the requirement doc (authored ~2 Sep) still marks this ✅. Needs a product decision: update the requirement doc, or reconsider the drop. See Regression Tracking, §12.
- **Response envelope does not match CLAUDE.md / `docs/api-guidelines.md`** — CLAUDE.md states responses must follow `{ data }` / `{ error: { message, code, details } }`. The real, deliberate implementation uses a flat `{ error: "<string>" }` for errors (`common/errorHandler.js:4-7`, explicitly commented as intentional to match the legacy Margin Planner frontend's contract) and unwrapped success payloads (`{ token, user }`, not `{ data: { token, user } }` — `authController.js:28,37`). This is a **documented, deliberate** divergence in the code, but `docs/api-guidelines.md` was never updated to describe it (unlike `docs/migration-plan.md`, which *was* reconciled on 2026-09-08). Needs either a doc update or a real migration to the documented envelope.
- **`process.env` read outside `config/index.js`** — CLAUDE.md: "Never read `process.env` outside `config/index.js`" is listed as non-negotiable. Directly re-verified by the coordinating session (`grep -rln "process\.env" src --include="*.js"`, excluding `config/index.js` itself): **11 other files** read it directly — `src/backup.js`, `src/auth.js` (likely dead legacy code per `migration-plan.md`'s own note), `src/seed-owner.js`, `src/index.js`, `src/common/logger.js`, `src/common/integrations/clickupClient.js`, and five ClickUp-related job/controller files: `modules/management/commercial-leads/jobs/reconcile.js`, `modules/management/commercial-leads/controllers/webhookController.js`, `modules/employees/jobs/clickupUserSyncSchedule.js`, `modules/employees/controllers/kpiClickupWebhookController.js`, `modules/employees/jobs/kpiClickupListSync.js`. **This is a partially self-documented, deliberate deferral** — `config/index.js:3-8` has a comment naming several (but not all) of these files as "deliberately out of scope... belongs with the Commercial Lead relocation pass" — but the comment's file list is now stale relative to the actual set, and `src/index.js` itself (reading `JWT_SECRET`/`PORT`/`HOST` directly even though `config/index.js` already exports all three) isn't mentioned in that comment at all.
- **KPI status labels don't literally match the doc** (§8) — 5-tier band vs. the doc's named 3-tier (On Track/Needs Attention/Off Track). Functional superset, but a literal-spec mismatch worth a product decision either way.
- **Admin lacks Governance §9's promised "Full" access on P&C leave queues** — `listPcPending`/`listAutoRejected` are hard-gated to `people_culture` only, no admin bypass (`timeOffService.js:174,184`); live-verified: admin token → `403` on both routes. Deliberate business-logic scoping, but contradicts the letter of the Access Matrix's "Admin: Full" column on every row.

## 8. Testing Status

**No automated tests exist anywhere in this repository.** `npm test` fails with `Missing script: "test"`. `package.json` devDependencies contain only `smee-client` (a ClickUp tunneling tool, not a test framework). No Jest/Mocha/Supertest/ESLint. No `.github/workflows` directory — no CI pipeline of any kind. This directly contradicts `docs/testing.md`, which still describes an (unbuilt) Jest + Supertest + real-Postgres-DB + CI-gate strategy — unlike `docs/migration-plan.md`, this file has **not** been reconciled to reality as of this audit.

Every status in §3 above marked **Manually Verified** means: the relevant service/repository/migration code was read in full, **and** at least one authenticated, read-only `curl` call was made against the real running local dev server (`localhost:3001`, real working SQLite DB) confirming the endpoint responds with the expected shape. No mutating (`POST`/`PATCH`/`DELETE`) calls were made against the shared dev database during this audit — write-path correctness (e.g., the leave two-stage approval actually transitioning state correctly end-to-end) is verified by code reading only, not by exercising it live.

**Not exercised in this pass:** any real browser session (no `claude-in-chrome` UI walkthrough was performed — every UI claim above is a static-code read of the relevant `views/js/*.js` file, not an observed rendering). Mobile responsiveness, calendar-widget rendering, empty-state completeness, and click-through flows should be spot-checked in a real browser in a future audit before being upgraded to "Manually Verified."

**Server health:** the local dev server (running since 2026-09-09, PID observed at audit time) responded `200` on `/` and handled every test request without crashing; a spot-check of `server/data/logs/app-2026-09-10.log` showed only expected 4xx errors from this audit's own deliberate probing (missing query params, role-gated 403s) — no unexpected 5xx or stack-trace crashes.

**`npm audit`: 3 moderate-severity vulnerabilities** (transitive `qs` package, via `body-parser`/`express`) — fixable via `npm audit fix`. This is new information; a prior session's memory recorded this as "clean, 0 vulnerabilities" as of 2026-08-19 — see Regression Tracking §12.

## 9. Gaps and Contradictions

**Between the two requirement documents:**
- The Governance doc's Access & Permission Matrix (§9) names 7 distinct roles: **Employee, Manager, HR, Operations, Commercial, CEO, Admin**. The Portal doc doesn't define roles at all (it's a data/feature spec), but its "My Team"/manager-scoped sections implicitly assume a real line-manager role distinct from "CEO." Neither document reconciles this with the fact that a true line-manager relationship (who approves whose leave) is a *data relationship* (an FK), while "Manager" as an *access role* in the Governance doc's matrix is really the CEO's account in the shipped system — two very different concepts sharing one word.
- The Governance doc's §12 SOP table describes leave approvals as a single-owner, single-approver process ("Owner: Manager, Approver: Manager"); the actual (and, per code comments, deliberately designed) system requires a *second* People & Culture confirmation step after the manager's decision. Not a contradiction of intent, but the SOP documentation doesn't describe the real process.

**Between the requirement documents and the current website:**
- Three requirement-doc sections (Documents §16, Policies §17, Training & Onboarding §13) and one governance-mandatory SOP item (policy acknowledgment) have **zero code**, despite the Governance doc explicitly marking related processes "Yes — mandatory" for in-portal handling (§12 SOP table).
- Governance §9's access-lifecycle workflow ("HR requests access → Ops Manager approves role template → Portal Admin creates account → tests login → HR notifies employee") does not exist — the real system is fully self-service registration (`authService.js:36-72`), with role elevation as a separate, unlinked, no-approval-trail manual step afterward.
- ~~Governance §9's mandated quarterly access-list export doesn't exist — `listUsers` returns a plain in-memory JSON array with no export capability (`accountAdminController.js:5-8`).~~ — **Done 2026-09-20**, see §16 Change History. The in-app audit-log *viewer* gap (reviewing `audit_log` itself, not the account list) remains open.

**Between the code's own role model and the documents' role models:**
- Code has 6 roles: `employee, manager, operations, finance, admin, people_culture`. `manager` = the CEO's account (confirmed by multiple code comments across `rosterService.js`, `timeOffService.js`, `ceo-dashboard/routes/index.js`, `employees/views/js/main.js`). There is **no distinct "Commercial" role** — the Commercial Leads module is gated to `USER_MANAGER_ROLES` (admin/manager/operations), the same set that gates several unrelated management functions, not a role scoped to Commercial staff specifically. There is **no distinct "CEO" role separate from `manager`**. `people_culture` is the functional equivalent of the doc's "HR." **`finance` exists in the role enum and is assignable via the Users admin UI, but has zero permission gates anywhere in the codebase** — a fully dead/inert role as of this audit.

**Between the frontend and backend:**
- None found that constitute a security gap — every sensitive frontend nav-hiding decision reviewed in this audit (Margin Planner, CEO Dashboard, Commercial Leads, roster management) has a matching server-side `requireRole`/service-level check, not just a hidden button.

**Between roles/permissions and intended stakeholder access:**
- Admin does not get the Governance doc's promised blanket "Full" access on the P&C leave-approval queues (§9 finding above) — a real, verified (403, live-tested) divergence from the documented Access Matrix.
- **Admin has the same narrow KPI-editing access as any other non-owning role, despite the Access Matrix's "Full" expectation.** Live-tested 2026-09-20 (Portal §9): `enterManualScore` grants no admin bypass at all — an admin who isn't literally an employee's `manager_employee_id` gets a 403 trying to enter a score, the same as an unrelated employee would. Separately, the KPI tab's own employee-picker UI calls the same `/employees/team` (direct-reports-only) endpoint for admin as it does for any manager, so admin has no UI path to even reach an employee outside their own reporting line — even though `canViewBreakdown` *does* grant admin a read-only bypass at the API layer. Net effect: admin can view (via direct API call) but not edit any KPI data outside what they personally manage, and the normal UI never surfaces that read bypass at all. Deliberate, not a bug — but another instance of the Access Matrix's "Admin: Full" column not matching the shipped system, on top of the P&C-queue one above.

**Between database design and requirements:**
- No schema exists at all for: leave entitlements/balances, documents, policies, announcements, training/onboarding records, general notifications, or personal/emergency-contact fields. These aren't partial — they are absent at the schema level, not just the UI level.
- **`kpi_profile`'s own CHECK constraint allows `heads` and `design` — both selectable in the live roster UI — with zero corresponding rows in `kpiFrameworkSeed.data.js`.** `KPI_Framework.xlsx` itself doesn't define either role. Live-verified 2026-09-20 that no employee currently holds either value, so this is a dormant landmine, not an active bug: the schema/enum was widened at some point (likely anticipating department-head-level KPI profiles) without the framework ever being built out to match.

**Between the KPI_Framework.xlsx source spreadsheet and the operational reality it assumes:**
- The spreadsheet's own "Overview" and per-role sheets name **five concrete data-hygiene blockers** ("must fix before any automation is reliable") and explicitly flag **"no named owner for quarterly KPI data pull" as an unresolved blocker — the system will not run without this person named.** None of these are code gaps — they're organizational prerequisites the spreadsheet's own authors called out — but they directly explain why live automation coverage is 2 metrics out of ~60 (see §9's new "ClickUp automation coverage" row): due dates still missing on most tasks, the "Feedback Reason" custom field not yet created, task statuses not standardized, and (most consequentially) nobody assigned to actually run the quarterly pull. Worth surfacing to whoever owns the KPI rollout, since building more automation before these are resolved wouldn't fix anything.

**Audit-trail coverage vs. Governance §12's mandatory list** (every process below is marked "Yes"/"Yes — mandatory" for in-portal handling; ✅ = `audit.record()` is actually called on this action, ❌ = not possible because the feature doesn't exist):

| Governance §12 process | Audited in code? |
|---|---|
| Employee information updates | ✅ (`rosterService.js`, roster + self-service change-request flows) |
| Leave requests / Leave approvals | ✅ (`timeOffService.js`, 4 call sites: submit, manager decision, P&C confirm, cancel) |
| KPI updates | ✅ (`kpiScoringService.js`, manual entry / target-setting / auto-mapping changes) |
| Performance evaluation | ✅ (via KPI manual entry + peer review submission) |
| Required actions | N/A — no distinct "action" entity beyond the above to audit |
| Policy acknowledgment | ❌ — module doesn't exist |
| Employee document updates | ❌ — module doesn't exist |
| Team information (reporting changes) | ✅ (`rosterService.js` update) |
| Announcements | ❌ — module doesn't exist |
| Internal requests | Partial — leave and profile-change requests are audited; there is no generic "requests" entity to audit as a whole |
| Onboarding tasks | ❌ — module doesn't exist |

Where the underlying feature exists, audit coverage is genuinely comprehensive (30+ `audit.record()` call sites found across auth, roster, departments, KPI, conflict-pairs, and time-off) — the gaps above are 100% a function of the missing modules, not of the audit-logging discipline itself. **However, the captured audit trail has no in-app viewer anywhere** — reviewing it requires direct DB access or grepping structured JSON log files by user UUID (a pattern explicitly described in a code comment in `usersAdmin.js:58`). This blocks the Governance doc's weekly/quarterly review processes (§9, §23) from actually happening inside the portal.

**Role elevation has no second-approver step.** Governance §15 states "elevated access requires Operations Manager approval, logged in the Permission Matrix change log." In the real code, `accountAdminService.changeRole` performs the role change immediately, in one step, by whichever single authorized actor initiates it — there is no separate approval/second-actor gate, only after-the-fact audit logging and admin-shielding guards (can't touch/assign `admin` except as an existing admin). This satisfies "dated and attributed" but not "[requires separate] approval."

**Documentation vs. documentation (repo `docs/` vs. reality):**
- `docs/migration-plan.md` was explicitly reconciled to reality on 2026-09-08 (a genuinely good practice — it now states plainly what was and wasn't built). `docs/testing.md` and `docs/api-guidelines.md` have **not** been similarly reconciled and still describe unbuilt/inaccurate targets (Postgres+Jest+CI; the `{data}/{error:{...}}` envelope). This inconsistency-in-reconciliation is itself worth flagging: some docs get updated as reality diverges, others don't, with no visible policy for which.

**No system-side support for the Downtime SOP (Governance §14) — v1.1 addition.** The Downtime SOP assumes a human reports an outage ("Issue reported → Portal Admin logs timestamp and severity"), with no automated-detection assist anywhere in the app: `grep -rn "'/health'\|\"/health\"\|/status\b\|maintenance" server/src` found no health-check route, no `/status` endpoint, and no maintenance-mode banner/flag of any kind. This is a small, cheap addition (a simple `/health` route already has everything it needs — `db` connectivity and process uptime) that would let external monitoring (or even a simple browser tab) detect P1/P2 outages without waiting on a person to notice and report one, which is exactly the failure mode Governance §14 exists to shorten.

## 10. Dependencies

- ~~**Leave Balance (§3)** needs a new `leave_entitlements`/`leave_balances` table + accrual logic before "remaining days" can be shown anywhere~~ — **Done 2026-09-13** without a new table (computed on read from fixed constants + existing `leave_requests` rows, see §16 Change History). Still partially blocks §22's "export leave history" (an aggregate day-based export, not per-request).
- **Documents (§16) and Policies (§17)** are foundational to: Required Actions' "documents requiring signature"/"policies requiring acknowledgment" (§12), Announcements' "policy updates" (§15), Notifications' "policy update notifications" (§14), and the Governance doc's mandatory policy-acknowledgment SOP (§12/§13). Building a shared document/content module first would unblock four downstream doc sections at once.
- **A general-purpose (non-KPI) notification system** is a prerequisite for real "approval notifications," "policy update notifications" (§14), and a real "notification indicator" across the whole dashboard (§22) — currently every notification concept is hand-rolled inside the KPI module (`kpi_employee_notifications`) with no shared abstraction other modules could reuse.
- **Profile completion percentage (§18)** depends on first deciding which fields count as "the profile" (work fields only, or the not-yet-built personal/contact fields too) — a product decision, not just an engineering task.
- **An in-app audit-log viewer (Governance §9/§15)** depends on nothing new — the `audit_log` table and `common/audit.js` writer already exist; this is a pure "expose existing data" task, the cheapest gap to close in the whole tracker.
- ~~**Access-list export (Governance §9)** similarly depends on nothing new — `listUsers` already returns the full data; only a CSV/export endpoint is missing.~~ — **Done 2026-09-20**, see §16 Change History.
- **Mobile responsiveness (§22 / Governance §21)** has no code dependency, only design/CSS work — but is a hard launch-checklist blocker per the Governance doc's own Go/No-Go criteria.

## 11. Recommended Implementation Order

Ordered by dependency, business importance, security/permissions, data integrity, and testing need — not by document order:

1. ~~Fix the Team KPI Summary RBAC scoping bug (§20)~~ — **Done 2026-09-10**, see §16 Change History.
2. ~~Leave Balance / entitlement ledger (§3)~~ — **Done 2026-09-13**, see §16 Change History.
3. **Automated test coverage for the leave and KPI approval paths** (currently the two highest-value, most-mutation-heavy modules in the app, with zero test coverage and real payroll/HR consequences if a status-transition bug ships silently).
4. ~~In-app audit-log viewer + access-list export~~ — access-list export **done 2026-09-20**; the audit-log viewer is still the cheapest remaining win (data already exists), closing the last Governance §9/§15 mandatory-SOP gap in this pair.
5. **Fix the `npm audit` moderate vulnerabilities** (`npm audit fix` — likely a quick, low-risk dependency bump) and reconcile `finance`'s dead-role status (either wire it to something real or remove it from `ASSIGNABLE_ROLES`).
6. **Documents + Policies module** (shared content infrastructure) — unblocks four downstream doc sections at once (§12, §14, §15, §17) and closes the Governance doc's mandatory policy-acknowledgment SOP gap.
7. **Mobile-responsive CSS pass** — a hard Governance §21 launch-checklist blocker, low engineering complexity relative to its business urgency given the Governance doc's compressed 9-Sep launch timeline.
8. **General-purpose notification system**, generalizing the existing KPI-notification pattern to leave/profile/policy events.
9. **Announcements & Training/Onboarding modules** — lowest immediate business urgency of the missing features (no hard compliance deadline attached, unlike policy acknowledgment), but explicitly required by the Governance doc's mandatory-use policy (§13) once the higher-priority items above are done.
10. ~~Unify Requests Center (§21) and org-chart visualization (§2)~~ — **Both done.** Org chart: 2026-09-14 (moved/restructured 2026-09-16). Requests Center: 2026-09-16.

## 12. Regression Tracking

| Date detected | Feature | Regression | Previous state | Current state | Cause | Resolution |
|---|---|---|---|---|---|---|
| 2026-09-10 (this audit) | Employee Profile — Working hours / schedule (Portal §1) | Feature was built, then deliberately removed | Columns `employees.working_hours`/`work_schedule` existed (migrations `007`, `008`); Portal doc marks this bullet ✅ (authored ~2 Sep 2026) | Columns dropped entirely, no replacement (migration `021_drop_working_hours_and_work_schedule.js`, commit `e09ff93`, 2026-09-06, merged via PR #38) | Explicit, documented product decision (migration comment: "removed as a product decision; there's no replacement") — not a bug | **Open** — needs a decision: update the requirement doc to remove the ✅, or reconsider the drop |
| 2026-09-10 (this audit) | `npm audit` vulnerability count | Regression in dependency security posture | Prior session memory (2026-08-19) recorded "clean, 0 vulnerabilities" | 3 moderate-severity vulnerabilities (`qs` via `body-parser`/`express`), confirmed by running `npm audit` directly in this session | Upstream dependency advisory published between 2026-08-19 and 2026-09-10; not a code change in this repo | **Open** — `npm audit fix` likely resolves it; not yet applied (out of scope for an audit-only session) |

## 13. Decision Log

| Date | Decision | Type | Reason | Affected Features | Status |
|---|---|---|---|---|---|
| 2026-09-06 | Drop `working_hours`/`work_schedule` fields, drop hybrid work locations, relabel Cairo/Alex | **Existing decision** (found in code) | Migration `021` comment: "removed as a product decision; there's no replacement" — no further rationale captured in-repo | Portal §1 (Employee Profile) | Implemented in code; **not yet reconciled with the Portal requirement doc**, which still shows this bullet as ✅ |
| 2026-09-08 | Reconcile `docs/migration-plan.md` to describe the real SQLite/no-Knex/no-Postgres architecture instead of the original aspirational plan | **Existing decision** (found in `docs/migration-plan.md`'s own header) | Explicitly stated in the doc: keep the original plan as historical record, add a reality-check section on top | Documentation accuracy generally | Done for this one file; `docs/testing.md` and `docs/api-guidelines.md` were **not** similarly reconciled — flagged as an open inconsistency |
| 2026-09-10 (this audit) | Leave requests require a two-stage approval (manager decision, then People & Culture confirmation) before terminal "approved" status | **Existing decision** (found in code, cited against an internal, non-audited `Time_off.pdf` spec) | Code comments cite this as the actual designed process; not described by either requirement document audited here | Portal §4 (Time Off Requests) | Implemented; **recommended** that the Governance doc's §12 SOP table be updated to describe this second stage explicitly |
| 2026-09-10 (this audit) | "Manager evaluation" (Portal §11) is implemented as anonymous, aggregated peer review rather than a named manager score | **Existing decision** (documented in ADR-0012) | ADR-0012 (`docs/adr/0012-kpi-evaluation-lifecycle.md`) | Portal §11 | Implemented; a genuine, documented architectural choice — **recommended** that the Portal requirement doc's "Manager evaluation" language be reconciled with this reality, or the decision revisited, as a product conversation |
| — | Build a shared Documents/Policies module before the KPI-domain notification system is generalized | **Recommended decision** (this audit) | Four downstream doc sections (§12, §14, §15, §17) and one mandatory governance SOP all depend on this existing first | Portal §12, §14, §15, §17; Governance §12/§13 | Open |
| — | Should the `finance` role be wired to real permissions, or removed from `ASSIGNABLE_ROLES`? | **Open decision** | Currently assignable via the Users admin UI with zero effect — a confusing, inert option for whoever administers roles | Governance §9 Access Matrix, `common/permissions.js` | Open |
| 2026-09-10 | Re-scope Team KPI Summary, round 1: keep `manager`/`admin` company-wide (unchanged), add `is_team_head` employees scoped to `findByManagerId(actorEmployee.id)` (their own direct reports, one level) | **Existing decision** (made this session, with the user, before implementing) | User confirmed exact semantics in conversation: team heads see people they manage; the CEO's `manager` role is "the same thing... plus the company-wide teams/departments," i.e. company-wide already is a superset of "their team," so no separate merge logic was needed | Portal §20 (Team Performance) | **Superseded by round 2 below** — implementation was correct for round 1's spec, but the spec itself changed |
| 2026-09-10 | Re-scope Team KPI Summary, round 2 (final): company-wide is not one flat list — split into `myTeam` (the manager's own direct reports) + `byDepartment` (every department's full real membership, independent of `myTeam`, duplicates allowed). Team heads unaffected (still flat, round 1's design). | **Existing decision** (user explicitly corrected round 1's assumption: "i need the company wide kpi summary to be different reports, not all reports of the company") | Company-wide as one undifferentiated list didn't give department-level visibility the business actually wants; user confirmed via a concrete worked example (using their own employee-numbering scenario) with three structural options presented — picked "departments always complete, duplicates allowed" | Portal §20 (Team Performance) | **Done** — implemented, and separately confirmed a frontend bug (Team Performance tab never shown to team heads) that would have made the backend fix invisible; both fixed and verified live in a real browser with a throwaway test account (see §20 table, §16 Change History) |
| — | Should role elevation (`changeRole`) require a second approver, per Governance §15's literal language? | **Open decision** | Currently one authorized actor can both initiate and finalize a role change in a single step; only after-the-fact audit logging exists | Governance §15 Access & Permissions SOP | Open |
| 2026-09-12 | Define "who is on my team" as one shared, dynamic rule everywhere the concept appears (My Team tab, Team Performance, any future screen): no current direct reports → department only; at least one direct report → direct reports unioned with department (deduplicated). "Manages other employees" is computed purely from the current `manager_employee_id` reporting line, never from the `is_team_head` flag or auth role. | **Existing decision** (user explicitly requested a scalable, single-source-of-truth fix after clarifying two questions: which screens it applies to — "every existing screen and every future screen, make this fix scalable" — and how "manages others" is decided — by the reporting-line FK, not a flag) | User reported the My Team tab showed people from other departments and a manager couldn't see a direct report in a different department — traced to a bug where the tab matched *peers* (people sharing the viewer's own manager) instead of the viewer's *direct reports* | Portal §19 (My Team), §20 (Team Performance) | **Done** — see §20/§19 tables and §16 Change History |
| — | Should `docs/testing.md` and `docs/api-guidelines.md` be reconciled to reality (the way `docs/migration-plan.md` was), or should the code be changed to match them? | **Open decision** | Both currently describe unbuilt/inaccurate targets; CLAUDE.md states docs are source of truth when they conflict with implementation, but that assumes the docs are current | Testing strategy, API response envelope | Open |
| 2026-09-13 | Leave Balance business rules: `short_notice` folds into the 7-day combined pool alongside `emergency`/`mental_health` (making it "Emergency/Mental Health/Short-Notice"); `sick` and `unpaid` stay uncapped/untracked "for now"; low-balance warning fires at ≤30% of a bucket's total remaining; balances reset by calendar year/month (no `joining_date` proration); only `status='approved'` requests count as "used" (pending/manager_approved don't reduce the shown balance yet) | **Existing decision** (user explicitly chose each of these 4 rules when asked, before implementation) | The 4 stated quotas (14/7/1/4) didn't map cleanly onto all 9 leave types or specify a reset cadence, "almost run out" threshold, or in-flight-request treatment — genuine product decisions, not inferable from the code | Portal §3 (Leave Balance) | **Done** — implemented and live-verified, see §16 Change History. Revisit sick-leave capping and approved-vs-pending counting if the business later wants stricter enforcement |

## 14. Questions / Decisions Needed

Genuine open questions that cannot be resolved from the two requirement documents or the code alone — each needs a human/stakeholder decision, not an engineering guess. (These are drawn from the Decision Log's "Open"/"Recommended" rows above, surfaced here as a standalone worklist per the tracker's reporting spec.)

1. **The Portal doc still marks "Working hours / schedule" (§1) ✅, but migration `021` deliberately dropped those fields on 2026-09-06.** Should the requirement doc be updated to remove that checkmark, or should the drop be reconsidered? Cannot be resolved from code alone — this is a product call.
2. **The Portal doc's "Manager evaluation" (§11) is implemented as anonymous, aggregated peer review (ADR-0012), not a named manager score.** Is this architectural substitution acceptable as the permanent design, or does the business actually need a manager-attributed score alongside it? The code and ADR explain *what* was built, not whether it still satisfies the original ask.
3. **Should the `finance` role be wired to real permissions, or removed from `ASSIGNABLE_ROLES`?** It is currently assignable via the Users admin UI with zero effect anywhere in the codebase — neither document explains what `finance` was meant to gate.
4. ~~Should the Team KPI Summary endpoint (§20) be re-scoped to the real reporting-line FK?~~ — **Resolved 2026-09-10, in two rounds.** User confirmed: yes for team heads (their own direct reports). For `manager`/`admin`, the initial fix kept the old flat company-wide list, but the user then refined this further: company-wide should be `myTeam` (their own direct reports) plus one section per real department, not one undifferentiated list. Implemented, and verified live in a real browser. See §13 Decision Log and §20 table.
5. **Should `docs/testing.md` and `docs/api-guidelines.md` be reconciled to reality** (the way `docs/migration-plan.md` was on 2026-09-08), or should the code be changed to match them (real Jest/Supertest/CI; the `{data}/{error:{...}}` envelope)? CLAUDE.md states docs win when docs and code disagree, but that assumes the docs are current — these two aren't, and no one has yet decided which direction to reconcile.
6. **Should role elevation (`changeRole`) require a second approver**, per Governance §15's literal "elevated access requires Operations Manager approval" language? Today one authorized actor completes the change alone, with only after-the-fact audit logging.
7. **Is the Governance doc's 7-role model (Employee/Manager/HR/Operations/Commercial/CEO/Admin) meant to be implemented literally** (i.e., should the code eventually grow a distinct line-manager role and a distinct Commercial role, separate from `manager`=CEO), or was the doc written aspirationally/organizationally without intending a 1:1 role-enum mapping? This affects how much of the "Needs Correction" RBAC work in §7 above is actually in scope for engineering vs. already acceptable as-is.
8. **Is a literal in-portal "Training & Onboarding module" (Portal §13) actually wanted**, given the Governance doc's own Training Plan (§10) describes live/recorded-session training that may not need a portal-tracked module at all? The two documents don't contradict each other outright, but they also don't confirm the Portal doc's §13 is still the intended design — worth a direct check with whoever owns both documents before scoping this work.

---

## 15. Progress History

| Date | Overall Progress (Portal-doc bullet score) | Major Completed Areas | Major Remaining Areas |
|---|---|---|---|
| 2026-09-10 | **≈ 60%** (unweighted, all 22 Portal-doc sections; see §2 methodology) | KPI/Performance engine (§8–11, ~80–100% per section); Time Off Requests workflow (§4, 93%); Employee Profile core (§1, 89%); My Team (§19, ~92%) | Documents/Policies/Training/Announcements/general Notifications (§13–17, ~5% combined); Leave Balance concept (§3, 43%); mobile responsiveness (0%); automated testing (0%) |

*(This is the first snapshot. Future updates must append new rows here, never overwrite this one, even if the methodology changes — document any methodology change explicitly alongside the new row.)*

## 16. Change History

| Date | Change | Features Affected | Previous Status | New Status | Commit / Reference | Notes |
|---|---|---|---|---|---|---|
| 2026-09-10 | **Tracker created** — full initial audit of both requirement documents against the live codebase | All | N/A (no prior tracker existed) | See §3 table | This document, initial commit | Baseline for all future updates |
| 2026-09-06 | Working hours/schedule fields dropped; hybrid locations dropped; Cairo/Alex relabeled | Portal §1 Employee Profile | Implemented | Not Started (regressed) | `e09ff93`, PR #38 | See Regression Tracking §12 |
| 2026-09-07 | KPI dashboard, ClickUp automation, hosted peer review built out | Portal §8–§11 | Partial (base scoring only, from `63bf7dd` 2026-08-19) | Implemented (most bullets) | `14a1baf` | Largest single-commit feature addition found in this audit's git history review |
| 2026-09-06 | Departments converted from a hardcoded array to a real role-managed DB table + FK; conflict-pair edit/delete added | Portal §1 (Department), internal conflict-pair feature | Partial | Implemented | `eb4e2a1`, ADR-0011 | — |
| 2026-09-10 | "Learn with Marie" added to the fixed job-title list | Portal §1 (Job title) | Implemented | Implemented (list extended) | `31688a2` (current HEAD) | Minor, noted for completeness |
| 2026-09-10 | **Cross-verification pass** — two of the five parallel research agents used in this same audit session independently cross-checked each other's areas and both wrote to this file; the coordinating session then re-verified the highest-stakes claims directly against the code (`npm audit`, `computeTeamSummary`, `process.env` grep) | All | v1 findings | One correction (process.env file count, 10→11); all other findings converged, no other corrections needed (see §0 reconciliation note) | This document | Additive findings folded in: Downtime SOP / no health-check-endpoint gap (§9), module-boundary-compliance bullet (§4) |
| 2026-09-10 | **Fixed the Team KPI Summary RBAC bug, round 1** — `computeTeamSummary` no longer 403s every non-manager/admin actor; `is_team_head` employees now see their own direct reports, `manager`/`admin` keep the company-wide list | Portal §20 Team Performance | Implemented but Needs Changes (real RBAC gap, §7) | Implemented (round 1) | `kpiScoringService.js` (working-tree change) | Verified read-only against real dev data via the DI-wired service. Superseded same-day by round 2 below once the user refined the requirement. |
| 2026-09-10 | **Refined the fix, round 2 — company-wide split into `myTeam` + `byDepartment`; frontend tab-visibility bug found and fixed; full browser verification performed** | Portal §20 Team Performance | Implemented (round 1) | **Implemented and browser-verified** | `kpiScoringService.js` (`computeTeamSummary`, new `summaryRow` helper, added `departmentRepository` dependency), `container.js` (dependency wiring), `kpi.js` (`renderSubNav`'s tab-visibility gate, line ~418), `kpiTeam.js` (rewritten to render both response shapes) — commit `16d8341` | User corrected round 1's assumption that company-wide should stay one flat list; after three rounds of clarifying questions (using concrete worked examples with the user's own employee numbers) landed on `{myTeam, byDepartment}` with duplicates allowed across groups. Separately discovered the frontend never showed the "Team Performance" tab to team heads at all (`kpi.js` gated it on `isManager\|\|isAdmin` only) — without this, round 1's backend fix would have been permanently invisible to any real team head. Both fixed together. **Full live verification**: created 3 throwaway test accounts via the real registration + admin APIs (`zzz-test-teamhead/report1/report2@ceastest.local`), wired a real `is_team_head`/`manager_employee_id` structure, restarted the dev server (it wasn't in watch mode and hadn't picked up the earlier edits), logged into the team-head test account in a real browser session and confirmed the tab now appears and shows exactly the 2 direct reports; temporarily promoted the same account to `manager`, re-logged-in, and confirmed it shows "My Team" + one section per real department with correct real membership. Reverted the role change and deactivated all 3 test accounts afterward (not hard-deleted, per this repo's revoke-never-delete convention) — confirmed as a side effect that deactivation correctly blocks the live session too. Flagged to the user before logging into a test account in their real Chrome profile, per standing guidance that claude-in-chrome uses their actual browser session. |
| 2026-09-10 | **Added a "Team Head" badge to the manager/admin Team Performance view** — follow-up UI request from the user, not in either source doc | Portal §20 Team Performance | Implemented and browser-verified (round 2) | Implemented and browser-verified (with badge) | `kpiScoringService.js` (`summaryRow`), `kpiTeam.js` (`summaryTableHtml`) — commit `16d8341` | Confirmed with the user first (scope = manager/admin view only, every `is_team_head` row gets it, "Team Head" label is sufficient, reused the existing `roster.js` badge style rather than a new one). Verified live: reactivated the same 3 throwaway test accounts, confirmed the badge on both a real pre-existing team head and the dedicated test team head, confirmed no badge on non-team-head rows. **Incident during this verification pass**: a stale httpOnly refresh-token cookie from an earlier admin login in this same session silently resumed when navigating to `/login` in the browser, so the first attempt briefly landed on the real admin account's dashboard instead of the test account — caught immediately (before any data was changed), fixed by explicitly logging out first each time before logging into a test account. Re-deactivated and role-reverted all 3 test accounts afterward, same as round 2. |
| 2026-09-10 | **Committed and pushed** all of the above (Team KPI Summary fix, department view, Team Head badge, and this tracker's creation) to a new branch | Portal §20 Team Performance; this document | Working-tree changes | Committed | `16d8341` on `feature/team-kpi-summary-department-view` (pushed to `origin`, PR not yet opened) | Branched off `feature/add-learn-with-marie-job-title` (HEAD at the time, itself already fully pushed/up to date with `origin`) rather than reusing that branch, matching this repo's own convention of one scoped branch per feature. `docs/migration-plan.md`, modified before this session started and unrelated to today's work, was deliberately left unstaged/uncommitted. |
| 2026-09-12 | **Fixed the "My Team" tab's membership bug and unified the rule with Team Performance** — user reported seeing people from other departments in "My Team", and a manager not seeing a direct report who was in a different department. Root cause: `myTeamSectionHtml()`'s client-side filter matched people sharing the *viewer's own manager* (peers) OR the viewer's department — never people who actually report to the viewer. Extracted the correct rule into a new shared, pure module (`services/teamMembership.js`): no current direct reports → department only; has direct reports → direct reports ∪ department (deduplicated), decided purely from `manager_employee_id`, never `is_team_head`/role. Added `GET /employees/team/mine` (`rosterService.getMyTeam`) so the My Team tab now gets its membership from the server instead of computing it client-side; `kpiScoringService.computeTeamSummary`'s non-company-wide branch (Team Performance, for team heads) now uses the same shared module instead of its own flat-direct-reports-only query, so team heads also see their department there. | Portal §19 (My Team), §20 (Team Performance) | §19: Implemented (broader-than-doc gap only). §20 non-company-wide branch: flat direct-reports-only | §19: Fixed. §20 non-company-wide branch: reports ∪ department (matches §19) | New file `server/src/modules/employees/services/teamMembership.js`; `employeeRepository.js` (`findByDepartment`); `rosterService.js` (`getMyTeam`); `rosterController.js`/`routes/index.js` (`GET /employees/team/mine`); `container.js` (wiring); `kpiScoringService.js` (`computeTeamSummary`); `views/js/team.js` (`myTeamSectionHtml`, `renderTeam`) — working-tree change on `feature/team-kpi-summary-department-view`, not yet committed | Two clarifying questions asked before implementing (scope = every current and future team screen; "manages others" = dynamic reporting-line FK, not a flag) — see §13 Decision Log. First verified read-only against real dev data (DI-wired repository/service called directly). User then reported the feature still wasn't loading data; **full live browser verification performed** in response — created 3 throwaway test employees via the real registration + admin APIs (see §16's next row for the second bug this surfaced), logged into a real browser session, and confirmed both My Team and Team Performance now show the correct cross-department-report + department-colleague set with no console errors and no failed requests. |
| 2026-09-12 | **Found and fixed a second, related bug while browser-verifying the above: Team Performance's tab-visibility check and its actual backend permission check disagreed** — the frontend showed the "Team Performance" sub-tab based on `kpiPerms.hasReports` (a real direct report exists), but `computeTeamSummary` still 403'd unless `actorEmployee.isTeamHead` was also true; an employee with a real report but `is_team_head=false` saw the tab and then got a permission error clicking into it. Fixed by making the backend permission check use the same dynamic reports-based definition as the tab and as `teamMembership.js`, so tab visibility and actual access can no longer disagree. | Portal §20 (Team Performance) | Reproduced live: tab visible, 403 on click | Fixed: tab and content now agree | `kpiScoringService.js` (`computeTeamSummary`'s permission check) — working-tree change on `feature/team-kpi-summary-department-view`, not yet committed | Found only because of the live browser pass above — the earlier read-only service-level check couldn't have caught this, since it calls `computeTeamSummary` directly and doesn't exercise the frontend's separate tab-visibility logic. **Manually Verified end-to-end in a real browser**: reproduced the 403 with the same throwaway test manager (`zzz-debug-a@ceastest.local`), fixed, restarted the local server, confirmed Team Performance now renders and matches My Team's membership exactly. Deactivated all 3 throwaway test accounts (`zzz-debug-a/b/c@ceastest.local`) afterward, not deleted, per this repo's revoke-never-delete convention. **Incident during this pass, same class as 2026-09-10's**: navigating to `/login` on the local dev server silently resumed a stale session for the real admin account (`mokhaled2012@gmail.com`) via a leftover refresh-token cookie, and separately the login form had that same account's credentials pre-filled by Chrome's saved-password autofill — caught immediately before submitting anything, explicitly logged out, and typed the test account's credentials by hand instead. |
| 2026-09-13 | **Built the Leave Balance concept (Portal §3)** — fixed, non-configurable, non-prorated company policy: 14 days/year Planned + 7 days/year combined Emergency/Mental Health/Short-Notice (= 21 total), 1 WFH day/calendar month, 4 Excuse hours/calendar month (1 hour per approved Excuse request). Computed on read from existing `leave_requests` rows (only `status='approved'` counts, per the user's decision) exactly like `getLeaveBreakdown` already does — deliberately **no new migration/table**; if quotas ever need to become admin-configurable or vary by employment type, that's the trigger to add a `leave_policies` table, not before. Displayed as a new "Leave Balance" card on Overview, unconditional on role (renders for any authenticated user with an employee profile). Added two **non-blocking** warnings to the New Request form, reusing the existing `.alert.alert-warn` convention: (1) low-balance, when a selected type's remaining ≤ 30% of its total; (2) notice-period risk, a live preview of the exact same `checkNoticeWindow` rule `submit()` already enforces — surfaced earlier, not a new auto-reject. Sick/Unpaid/Public Holiday remain uncapped and untracked (per the user's explicit decision, "for now"). | Portal §3 (Leave Balance) | 43% — "No entitlement/accrual ledger exists" (Not Started on annual entitlement/remaining) | Implemented — see updated §3 table below | New file `server/src/modules/employees/services/leaveBalanceRules.js`; `timeOffService.js` (`getMyBalances`, `checkNotice`); `timeOffController.js` (`getBalances`, `checkNotice`); `routes/index.js` (`GET /employees/leave-requests/balances/mine`, `GET /employees/leave-requests/notice-check`); `container.js` (wiring); `views/js/overview.js` (`leaveBalanceCardBody`, new card); `views/js/timeOff.js` (`renderBalanceWarning`, `checkNoticeLive`); `views/js/leaveTypes.js` (`balanceBucketForType`) — committed `bc245b7` on `feature/leave-balance-tracking`, merged to `main` same day | Clarified 4 business rules with the user before implementing (see §13 Decision Log): short_notice folds into the 7-day combined pool, sick stays uncapped for now; warning threshold is ≤30% remaining; reset is calendar year/month (no anniversary proration); only `approved` status counts as used. **Live-verified**: seeded one approved 10-working-day Planned request directly in the dev DB for a real test employee (`tester1@example.com`, cleaned up afterward), confirmed via curl that `GET .../balances/mine` returned `4/14` and `GET .../notice-check` correctly flagged a 1-day-notice Planned request; then browser-verified end-to-end — Overview showed the `4/14` card, the New Request form showed both the low-balance warning and (with a next-day start date) the notice-period warning simultaneously, and "Sick Leave" correctly showed neither. The stale "concurrent background session" note previously in this section (see prior version of this row's neighbor) found no corroborating evidence in `git log --all`/`git branch -a` and is now superseded by this completed implementation. **Incident, same class as prior rows**: the browser session's stale refresh-token cookie auto-resumed an unrelated account on first navigation; caught immediately, explicitly logged out via `/api/auth/logout`, and logged into the test account by hand. Restarted the local dev server in `--watch` mode (it was running plain `node src/index.js` and hadn't picked up the new files) — left running in watch mode afterward for convenience. |
| 2026-09-13 | **Added a P&C "Doctor's note provided" checkbox for sick leave, closing the sick-leave-balance gap the previous row left open** — sick leave over 2 consecutive working days already required a note per policy (`timeOffRules.sickLeaveRequiresDoctorNote`), but whether one was actually provided was never captured; now P&C sees a checkbox on exactly those requests at confirmation time. Checked → stays uncapped (same as short sick leave, which never shows the checkbox at all and is always free). Unchecked (the default) → deducted from the same 7-day combined Emergency/Mental Health/Short-Notice pool, using the same working-day-count/partial-day-halving logic the other combined types already use. | Portal §3 (Leave Balance) | Sick leave balance: Partially Implemented (uncapped/untracked in all cases) | Sick leave balance: Implemented (conditionally capped, see updated §3 table) | New migration `022_add_doctor_note_provided_to_leave_requests.js` (`leave_requests.doctor_note_provided`, default 0); `leaveRequest.model.js` (`doctorNoteProvided`); `leaveRequestRepository.js` (`updatePcDecision`); `leaveBalanceRules.js` (`sickCountsAgainstCombined`, `computeBalances` now takes `sickLeaveRequiresDoctorNote`); `timeOffService.js` (`pcConfirm` accepts `doctorNoteProvided`; `listPcPending` now attaches `requiresDoctorNote` per row; `getMyBalances` passes the new dependency); `timeOffController.js` (`pcConfirm`, `getBalances` pass-through); `views/js/team.js` (`doctorNoteCheckboxHtml`, `pcActions`, `pcConfirm`) — committed `18a254b` on `feature/sick-leave-doctor-note-confirmation`, merged to `main` same day | Two clarifying questions asked before implementing (see §13 Decision Log): which balance pool an un-noted sick leave draws from (same existing 7-day combined pool, not a separate one), and whether the checkbox applies to every sick request or only ones already requiring a note (only >2-day ones — short sick leave stays always-free with no checkbox at all). **Live-verified end-to-end**: submitted two 3-working-day sick requests via the real API as a test employee, manager-approved both, confirmed one with the checkbox checked (stayed uncapped) and one unchecked (deducted 3 days from the combined pool, confirmed via `GET .../balances/mine` dropping from 7/7 to 4/7) — then repeated the unchecked case through the actual browser UI (real click on a real checkbox and Confirm button, not just the API) and confirmed the same DB result. All test requests cleaned up afterward. |
| 2026-09-13 | **Fixed the Stale badge: excluded 'Closed' deals, lowered threshold to 7 days** — user-reported fix to the item below. A 'Closed' deal is finished, not neglected, so it was wrongly flagging every one of the 18 Closed Active Clients deals regardless of age; also tightened the threshold from 14 to 7 days per the user's request. | Every 'Closed' deal flagged Stale if untouched 14+ days | 'Closed' deals never flagged, threshold now 7 days | `views/js/deals.js` (`isStale` now also checks `deal.status.toLowerCase() === 'closed'`) — working-tree change, not yet committed | Live-verified in a real browser: 0 of the 18 'Closed' Active Clients deals show the badge afterward (was 18 before); the 3 still flagged are all 'in progress'; pipeline's stale count rose from 1 to 4 with the tighter 7-day threshold, as expected. |
| 2026-09-13 | **Added a "Stale" badge to both Commercial Leads tables (pipeline and Active Clients)** — first of a 10-item approved batch spanning the Employees and Commercial Leads pages (see the batch's plan file for full scope; items are being implemented, live-verified, and reviewed one at a time per the user's explicit request). Flags any deal whose ClickUp task hasn't been updated in 14+ days, helping AMs spot neglected accounts. Purely client-side — `clickupUpdatedAt` was already present and populated on every deal for every list. | N/A — out of requirement-doc scope (Commercial Leads, per line 91) | No staleness indicator on either table | 14-day-threshold "⚠ Stale" badge on both tables | `views/js/deals.js` (`isStale`, `staleBadgeHtml`, used in both `renderPipelineTable` and `renderActiveClientsTable`); `views/css/commercialLeads.css` (`.stale-badge`) — working-tree change, not yet committed | Live-verified in a real browser (`tester1@example.com`, manager role): 1/25 visible pipeline deals and all 21 Active Clients deals correctly flagged (matches the real data — Active Clients hasn't had a ClickUp status change since July/August, per the stage-duration research done for a later item in this same batch). |
| 2026-09-13 | **Added a team availability summary line to Overview** — second of the 10-item batch. "N working · N remote · N on leave" rendered just above "Who's Online" for both the normal (employee-profile) and manager/admin/P&C no-profile Overview branches. Purely client-side — `GET /employees/directory` already returns each entry's `status` as `'active'\|'remote'\|'on_leave'` (server-decorated against today's approved leave), no new fetch needed. | Portal §6 (Team Availability) | 50% — "who's online" real, aggregate counts weren't | Aggregate counts added | `views/js/overview.js` (`teamAvailabilitySummaryHtml`, called from both `renderOverview` and `renderManagerOverview`) — working-tree change, not yet committed | Live-verified in a real browser with both a normal manager account (has an employee profile) and a manager-role account with no employee profile (exercises `renderManagerOverview`'s separate code path) — both correctly showed "15 working · 0 remote · 0 on leave" matching the real 15-active-employee dev roster. |
| 2026-09-13 | **Added CSV export to both Commercial Leads tables (fifth of the 10-item batch)** — follows the one existing CSV precedent in the codebase (`kpiScoringService.exportHistoryCsv`), but fixes its missing escaping: deal names/custom-field values are free text and routinely contain commas/quotes/newlines, so a proper `csvEscape()` (quote-wrap + double embedded quotes) was added rather than copied without it. Columns are Name/Status/the union of every custom field seen across that list's deals/Created/Last Updated. Exports the full list, not just the currently filtered/paginated view (a reasonable simple default; filtered export would be a follow-up if wanted). User confirmed CSV is sufficient (opens natively in Excel) rather than a real `.xlsx`, which would require adding a new dependency this codebase doesn't have. | No export of any kind on either table | "Export CSV" button on both tables, full-list, properly escaped | `services/dealsService.js` (`exportDealsCsv`, `csvEscape`, `fieldValueText`); `controllers/dealsController.js` (`exportDeals`); `routes/index.js` (`GET /commercial-lead/deals/export?list=`); `views/index.html` (button on both toolbars); `views/js/deals.js` (`downloadDealsCsv`, same raw-fetch+Blob pattern as the Employees module's `kpiHistory.js`) — working-tree change, not yet committed | Live-verified end-to-end: curl-tested both lists (activeClients: 21 rows, correct; pipeline: 205 rows, confirmed proper quote/comma/embedded-newline escaping on real free-text fields) and the invalid-`list` rejection; then in a real browser, clicked the actual "Export CSV" button, confirmed the network request returned 200, and confirmed the file genuinely landed in `~/Downloads` with correct content — not just that a Blob was created. Test downloads cleaned up afterward. |
| 2026-09-13 | **Item 4 (manual Refresh button + "Refreshed HH:MM:SS" timestamp on Overview/My Team) — built, live-verified, then reverted same day per user review.** User liked the existing refresh-on-tab-switch behavior (pre-existing, untouched by this item) but asked to remove the added button and timestamp entirely. Cleanly reverted: `views/index.html`'s two page headers back to their original markup, `views/js/dom.js`'s `stampRefreshed` helper removed (now unused), `overview.js`/`team.js`'s calls to it removed, `main.js`'s button wiring removed. Confirmed via grep that zero references to the removed ids/functions remain anywhere in the module. | N/A — reverted same day | N/A — reverted same day | Net change after revert: none (all touched files back to their pre-item-4 state) | Live-verified the revert in a real browser: both page headers show only their original title/subtitle with no button or timestamp, and tab-switch still re-fetches fresh data exactly as it always did before this item existed. |
| 2026-09-13 | **Added click-to-expand detail drill-down to Active Clients — AM (third of the 10-item batch)** — pipeline rows already expanded to show every custom field/subtask on click; Active Clients rows did not, for no real reason. Reused `renderDealDetails(deal)` verbatim (already list-agnostic) and copied the exact `.deal-row`/toggle-details `<tr>` pattern `renderPipelineTable` already used. | N/A — out of requirement-doc scope (Commercial Leads, per line 91) | No drill-down on this table | Click any row to expand/collapse its full custom fields + subtasks | `views/js/deals.js` (`renderActiveClientsTable`, no new functions — pure reuse) — working-tree change, not yet committed | Live-verified in a real browser (manager role): clicked "BIC - Egypt Retainer Onboarding," confirmed it expanded showing Client Name/Month custom fields and ~20 subtasks; clicked again and confirmed it collapsed (`0` open `.deal-details` elements afterward). Filter/search from the earlier Active Clients change continued to work correctly alongside it. |
| 2026-09-13 | **Auto-compute 'remote' status from a same-day approved WFH request, same pattern as the existing 'on_leave' override** — follow-up to the Roster Status-column removal, surfaced by the user asking how Overview's new "remote" count could ever be non-zero now that nothing can set it manually. Root cause found in the process: `decorateStatus`'s existing "is this employee on leave today" check (`findApprovedOverlapping`) never distinguished `leave_type`, so an approved WFH request was already being lumped into `'on_leave'` — semantically wrong (WFH is working, not away). Split today's approved overlapping requests into two sets (`wfh` vs everything else); `on_leave` still wins if both somehow apply the same day. Fully computed-on-read, same as `on_leave` already was — reverts to the stored default automatically the day after the WFH request's `end_date`, no scheduled job or manual step needed. | Portal §6 (Team Availability) | 'remote' was a manually-set, effectively-dead value (nothing in the dataset ever used it, and it had just lost its only editing UI) | 'remote' now auto-derives from approved same-day WFH requests | `rosterService.js` (`onLeaveEmployeeIdsToday` renamed/expanded to `todaysApprovedOverrides`, returns `{onLeaveIds, wfhIds}`; `decorateStatus` checks both, `on_leave` takes priority) — no repository or schema change, `findApprovedOverlapping` already returned `leave_type` on every row | Live-verified via direct API calls and the real browser: inserted an approved WFH request for today → directory correctly returned `status: "remote"` and Overview showed "14 working · 1 remote · 0 on leave"; added a second approved (non-WFH) request the same day → correctly flipped to `on_leave` (priority confirmed); removed both → correctly reverted to the stored `active` default, confirming the "next day it returns to default" behavior without needing to wait a real day. All test rows cleaned up afterward. |
| 2026-09-13 | **Removed the editable Status (Active/Remote) column from the Roster table UI** — ad-hoc request outside the 10-item batch, made while explaining how the new "team availability" summary computes its counts. No role can view or edit `employees.status` from Roster anymore; the backend `create`/`update` validation and the underlying column/CHECK constraint are untouched (the column still exists and still drives the Overview summary's "remote" count — it just can no longer be changed from any UI, since the Roster table was its only editable surface). | N/A — UI-only change, no requirement-doc section | Editable dropdown per row (admin/manager/operations/P&C only) | Column removed entirely | `views/js/roster.js` (removed the Status `<td>`/`<th>`, `STATUS_OPTIONS`/`STATUS_LABELS`/`STATUS_BADGE_CLASS` constants now unused and deleted; corrected two stale `colspan` values from 15 to the real 12-column count in the same pass) | Live-verified in a real browser (manager role): confirmed 12 headers now exactly match 12 cells per row, `.edit-status` no longer exists anywhere on the page, and the table renders cleanly with no leftover empty column. |
| 2026-09-14 | **Started a page-by-page mobile-responsiveness pass — Commercial Leads done first.** User is going through all 4 views one at a time (per standing incremental-batch preference); this closes part of the §22/Governance §21 mobile gap, which was more advanced than the tracker's stale "zero `@media` queries" claim below suggested — all 4 CSS files already had some (`login.css` 3, `employees.css` 2, `ceoDashboard.css` 10, `commercialLeads.css` 4 before this change). Root-caused two real overflow bugs at narrow widths via a live browser session at ~500px width (Chrome enforces a ~500px minimum window, so true 375–414px phone widths can't be tested via plain window resize — this is the narrowest reachable live): (1) `.grid-2 > .card` had no `min-width:0`, so a chart's flex children (`.bar-col{min-width:52px}` × 8–10 bars) forced the whole page to overflow horizontally instead of scrolling within their own card — the classic CSS grid/flexbox min-width:auto blowout; (2) the pipeline table (5 columns) had no horizontal-scroll container, so long values (e.g. "Emirates") got clipped at the viewport edge. | Portal §22 / Governance §21 (mobile responsiveness) | Not Started (per stale tracker claim; actually partially started, see above) | Commercial Leads page: fixed (bar charts + both tables now scroll within their own card, no page-level horizontal overflow) | `views/css/commercialLeads.css` (`.grid-2 > .card{min-width:0}`, new `.table-scroll` class, `#pipelineTable{min-width:560px}`, narrow-width search-input tweak); `views/index.html` (wrapped both `<table class="data-table">` elements in `.table-scroll`) — working-tree change, not yet committed | Live-verified in a real browser (`mokhaled2012@gmail.com`, manager role) at ~500px width: confirmed via `document.documentElement.scrollWidth <= window.innerWidth` (no page-level overflow) both before (true, bug reproduced) and after (false, fixed) the change; visually confirmed charts and both tables now scroll independently inside their cards. CEO Dashboard, Employees, and Login/auth pages not yet audited — next in the batch. |
| 2026-09-14 | **CEO Dashboard mobile overflow — initial audit wrongly cleared it; user caught two real bugs live in prod, both fixed.** First pass (same day, superseded by this row) tested live at Chrome's ~500px window-resize floor and found no overflow, including in the `buildTable()`-rendered "Largest overdue balances" (Collections) and "Projects needing intervention" (Delivery) tables — but 500px had enough slack to hide the bug. The user reported both were actually broken in prod at real phone widths. Re-tested using an in-page iframe pinned to a true 375px width (bypasses Chrome's window-resize floor entirely) and confirmed `document.documentElement.scrollWidth` (477) > `clientWidth` (371) — real overflow, "Live projects"/other trailing columns pushed fully off-screen with no way to reach them. Root cause was two-fold: (1) `buildTable()` (shared by all 4 data tables on this page) emitted a bare `<table>` with no scroll container, same class of bug as Commercial Leads; (2) `.grid`'s children (used by every `.g2`/`.g3`/`.g4`/`.g-hero` two-or-more-column section) had no `min-width:0`, so a wide child (e.g. the People section's "Open roles" table) could expand past its own grid track and blow out the whole page — this is what actually caused the Collections/Delivery overflow, since `buildTable()`'s table alone, once wrapped, still needed a shrinkable grid parent to make the wrapper's `overflow-x:auto` take effect. Fixed both at their shared source rather than per-callsite. | Portal §22 / Governance §21 (mobile responsiveness) | Broken in prod (real overflow at 375px, confirmed after user report) | Fixed — verified zero overflow across all 3 entity tabs, every ~400px of scroll, at a true 375px width | `views/js/charts.js` (`buildTable()` now wraps output in `<div class="table-scroll">`); `views/css/ceoDashboard.css` (new `.table-scroll{overflow-x:auto}`; `.grid > *{min-width:0}`) — working-tree change, not yet committed | **Process note for future audits**: Chrome enforces a ~500px minimum window size, so plain `resize_window`-based live testing cannot reach real phone widths (375–414px) and can miss genuine bugs, as it did here. Fixed by pinning an in-page `<iframe style="width:375px">` to the same origin (cookies/auth carry over) and reading `iframe.contentDocument.documentElement.scrollWidth` directly — this should be the standard method for any further mobile-overflow verification on this project, not the ~500px window resize. Re-verified Commercial Leads (previous row) with this same true-375px method as a sanity check: still clean, that fix held. **Follow-up same day**: user also reported that on their real iPhone, the "Ceas Comm" tab visibly renders shifted left and smaller than "Learn with Marie"/"Consolidated", which render centered — a second symptom of the same root cause, confirmed directly against **production** (`pricing-portal.onrender.com`, not yet deployed with this fix). Same true-width-iframe method, this time same-origin against production itself (a cross-origin iframe can't be read via `contentDocument` — had to navigate the tab to the prod origin first): fresh-loaded, default "Ceas Comm" measured `scrollWidth` 477 vs `clientWidth` 386 (real overflow, matching the two table bugs above); switching to "Learn with Marie" or "Consolidated" (no fresh navigation) dropped to 386/386 (zero overflow) every time. Ceas Comm is the only entity with the full dashboard (all 8 sections, all `buildTable()` tables, every `.grid` section populated) — Learn with Marie and Consolidated render mostly empty-state placeholders, so they don't hit the bug at all. Mobile Safari's known behavior when a page's content is wider than the `width=device-width` viewport is to silently shrink the whole page to fit rather than adding a scrollbar the way desktop Chrome does — this fully explains both the "shifted left" and "smaller/not filling the screen" symptoms as one and the same overflow bug already fixed above, not a separate issue. **Not yet independently confirmed on a real device post-fix** (Chrome, even via the iframe trick, doesn't reproduce iOS Safari's shrink-to-fit rendering — only the underlying overflow condition, which is the actual trigger). **Update, same day**: while investigating the next bug below, found this branch's fix (`table-scroll` + `.grid > *{min-width:0}`) is already live on production (`curl .../ceo/js/charts.js` / `.../ceo/css/ceoDashboard.css` on prod both show the fixed code) — deployed by the user directly, outside this session. Re-verification against prod now shows 0 overflow across 5 fresh reloads and a full-page scroll sweep on Ceas Comm; the "shifted left" symptom should be resolved, pending the user's own confirmation on their phone. |
| 2026-09-14 | **Found and fixed a third, distinct CEO Dashboard mobile bug while re-checking Revenue after the above: `chartBarsH`'s SVG labels render outside the card, uncontained, unlike the two table/grid bugs above.** User reported "some of the text is outside the card" in "02 Revenue" specifically. The two earlier fixes didn't touch this — `document.documentElement.scrollWidth` stayed at 386 (no overflow) even with this bug present, because SVG `<text>` elements don't inflate document scrollWidth the way block-level overflow does, especially under this page's `.plot svg{overflow:visible}` (required so chart hover tooltips aren't clipped). Found by scanning each chart's rendered SVG `<text>` elements directly for `getBoundingClientRect()` positions outside their `.card`'s bounds — 7 offenders found on prod's Revenue section alone (e.g. "Cairo Grand Developments" at `left:-30` against a card `left:20`, i.e. rendering off the left edge of the viewport entirely). Root cause: `chartBarsH`'s label column width is `Math.min(190, Math.max(90, containerWidth * 0.30))` — comfortable on desktop, but as little as ~90px on a phone-width card, nowhere near enough for real names like "Content & Social Retainers" or "Zamalek Hospitality Group" at the label's `text-anchor:end` position; SVG text never wraps or clips on its own. Affects every chart built with `chartBarsH`: Revenue's "by service line" and "Client concentration", Pipeline's "by stage", People's "Utilisation by team" (the last two just hadn't hit a long-enough name yet to visibly break). | Portal §22 / Governance §21 (mobile responsiveness) | Broken — SVG text rendering past the card/viewport edge, unclipped, invisible to scrollWidth-based checks | Fixed — labels now truncate to fit with an ellipsis; full name still available via the bar's existing hover tooltip | `views/js/charts.js` (new `fitSvgText()` helper using `getComputedTextLength()`, called on each label in `chartBarsH`) — working-tree change, not yet committed | Live-verified: 0 SVG-text offenders across all 3 entity tabs after the fix (was 7 on prod's Revenue section alone before); visually confirmed "Performance / Media Buying" → "Performanc…" etc., readable and contained. **Second process note**: `scrollWidth`-based overflow checks (the method established for the first two bugs on this page) do not catch this class of bug — SVG content under `overflow:visible` needs its own check, scanning rendered `<text>`/other SVG element bounds against their containing `.card`, independent of document-level scrollWidth. |
| 2026-09-14 | **Employees Portal mobile-responsiveness audit (fourth and final page of the pass) — found already largely solid, one real bug fixed.** Full sweep using the true-375px-iframe method (established earlier this same day) across all 6 top-level nav tabs (Overview, Time Off, KPIs, My Team, Roster, Leave Report), all 4 Time Off sub-tabs, and all 5 KPI sub-views (Overview/History/Team Reviews/Team Performance/ClickUp Mappings) — 0 offenders on every one, confirmed against real populated data (Roster: 22 rows/12 columns; Leave Report: 11 rows; KPI Team: 21 rows), not empty states. Roster's 12-column table — the single highest-overflow-risk element on this page — was already correctly wrapped in the existing `.table-scroll{overflow-x:auto}` class (present in this page's CSS before this audit), so this page had already avoided the table-overflow bug class found on Commercial Leads and CEO Dashboard. The one real bug: the KPI notification bell's dropdown panel (`.kpi-notif-panel`, fixed `width:320px; right:0`) rendered 207px off-screen to the left on a 386px-wide viewport — its holder sits in a `justify-content:space-between` flex row that wraps on narrow screens, landing the holder near the LEFT edge (not the right, where `right:0` anchoring assumes it'll be), so the fixed-width panel extended almost entirely past the left edge of the screen. Also checked the shared account-menu dropdown (`accountMenu.css`, used on all 4 pages) for the same anchor-direction bug — safe, since its avatar button is reliably pushed to the header's far right by `.spacer{flex:1}` regardless of screen width. | Portal §22 / Governance §21 (mobile responsiveness) | Unverified (no prior audit) | Audited — 1 real bug found and fixed, rest of the page already correctly built for mobile | `server/src/modules/employees/views/css/employees.css` (`.kpi-notif-panel` narrow-width override: `right:auto; left:0; width:min(320px, calc(100vw - 48px))`) — working-tree change, not yet committed | Live-verified: confirmed the bug (`left:-207.8` at 386px viewport) before the fix, confirmed contained (`left:37, right:357`) after, re-tested at 340px to confirm the responsive `min()` width clamp holds at even narrower widths, then re-ran the full 15-tab/subtab sweep post-fix — still 0 offenders everywhere, including with the notif panel open. **This completes the mobile-responsiveness pass across all 4 pages** (Commercial Leads, CEO Dashboard, Employees Portal, and the Login/auth page still needs its own pass if not already covered — see the pre-existing `login.css` media queries noted at the start of this pass). |
| 2026-09-14 | **Hid the Users admin table's UUID column on narrow screens** — user-requested follow-up after the mobile-responsiveness pass; this table (Name/Email/Role/Status/action/UUID) is already correctly wrapped in `.table-scroll` so it wasn't broken, but the UUID+Copy-button column alone accounted for roughly a third of its scroll width, making Role/Status/Deactivate needlessly hard to reach on a phone. UUID is a support-tracing feature (grep audit logs by it), not needed for everyday mobile use. Confirmed no regression to the existing admin-only restriction — that's enforced server-side (`accountAdminService.listUsers`'s `includeUuid = actorRole === roles.ADMIN`, verified by reading the code) and was already correct; this change only affects whether the already-admin-gated column shows on narrow screens, nothing about who can see it. | N/A — UX refinement, not in requirement-doc scope | UUID column always shown to admins regardless of screen width, consuming ~1/3 of the table's scroll width on mobile | UUID column hidden below 640px; unaffected on desktop | `views/js/usersAdmin.js` (`uuid-col` class added to the `<th>`/`<td>`); `views/css/employees.css` (`@media (max-width:640px){.uuid-col{display:none}}`) | Live-verified in a real browser at 390px width (logged in as an admin account): confirmed the column is absent and the table's scroll width drops from 634px to 531px; re-confirmed no page-level overflow. |
| 2026-09-14 | **Roster's Departments card: removed the grey department-code text, made department names clickable to show their members** — user-requested UX improvement, unrelated to the mobile pass. The code text (e.g. "content" next to "Content") was internal plumbing with no value to the person reading it. Clicking a department name now expands an inline member list (first + last name, job title if set) sourced from `rosterCache` (already loaded for the roster table above, no new API call) filtered by `employee.department === department.code`; clicking again collapses it. Deliberately did not add a member-count badge in its place — the user asked to declutter, and a "12" beside the name would just be a different piece of grey text doing the same job the code text did. | N/A — UX improvement, not in requirement-doc scope | Department rows showed a grey internal code, were not interactive | Code text removed; click a department to expand/collapse its member list | `views/js/roster.js` (`expandedDepartmentId` state, `toggleDepartmentExpand`, `renderDepartmentsSection` rewritten); `views/css/employees.css` (`.dept-name-btn`/`.dept-name-chev`/`.dept-members`/`.dept-member-row`, chevron rotates open/closed matching the existing CEO Dashboard `.chev` convention) | Live-verified in a real browser: clicked "AI & Innovation," confirmed it expanded showing its 5 real members (Ada Designer, Amy Manager, Ari Director, Kim Content, Mohamed Khalid) with no grey code text visible anywhere in the list; clicked again and confirmed it collapsed. No console errors. Also checked at a true 375px width via the iframe method — no overflow introduced. **Same-day refinement**: user asked for the whole card to be clickable (not just the name), and for members to render as expandable info cards like My Team's, not plain text rows. Reworked: the entire `.request-card` is now the click target (`role="button"`, `onclick` on the card itself); Edit/Save/Cancel/the rename input all call `event.stopPropagation()` so they still work independently without also toggling the department open/closed. Members now render via a new `deptMemberCardHtml()` — deliberately a local copy of My Team's `myTeamCardHtml` pattern (avatar + name + job title, click to expand Email/Manager), not an import from `team.js`, matching that file's own stated precedent for why: both tabs' DOM can coexist (tab-panels are hidden, not removed) and would otherwise collide on shared `#team-directory-card-N` ids. Reuses the same globally-loaded `.team-directory-card`/`.team-directory-grid` CSS from `accountMenu.css`, so it looks and behaves identically to My Team's cards. Live-verified: clicking anywhere on the "AI & Innovation" card (not just its name) expands it into a grid of 5 real member cards with avatars; clicking a member card expands its own Email/Manager detail without collapsing the parent department card; clicking Edit opens the rename form without triggering expand/collapse. No console errors; re-checked at true 375px width, still 0 overflow with both the department and a member card expanded simultaneously. |
| 2026-09-14 | **Added a Company Org Chart to the My Team tab** — user asked for a reporting-line tree view and, when given a choice, picked "My Team" (open to every employee) over "Roster" (admin-only) since the goal was company-wide transparency, not an admin editing tool. Built entirely client-side from `directoryById`, the same directory fetch My Team's own section already loads (`GET /api/employees/directory` — no permission gate beyond authentication, per `rosterService.listDirectory`'s existing comment) — no new backend endpoint needed, since `managerEmployeeId`/name/department/jobTitle/photo is exactly what a tree needs and was already in the payload. Employees are grouped by `managerEmployeeId` into a tree; anyone with no manager (or a manager who's inactive/missing) becomes a root rather than being silently dropped, so the chart always accounts for every active employee even with imperfect data. Each node reuses My Team's own `.team-directory-card` click-to-expand pattern (own id namespace, `org-card-`/`org-detail-`, matching that file's established precedent for why two instances of this pattern don't collide) — clicking a person shows Department/Email without leaving the tree. | N/A — new feature, not in requirement-doc scope | No org-chart/reporting-line view anywhere in the app | Full-company tree, open to every employee, nested under My Team | `views/js/team.js` (`orgChartMemberCardHtml`, `orgChartToggleCard`, `orgChartNodeHtml`, `orgChartSectionHtml`, wired into `renderTeam`'s `sections` array right after My Team); `views/css/employees.css` (`.org-chart-tree`/`.org-chart-children` — indent + left border per level, no new library) | Live-verified in a real browser: real 5-level-deep hierarchy rendered correctly (Mohamed Khalid → test test → Sam Lee → Janet Doe → further); clicking a node expanded its Department/Email without collapsing its parent or siblings; multiple employees with no manager assigned correctly appeared as separate chart roots rather than vanishing (the inactive/missing-manager fallback was code-reviewed but not separately exercised against real data). No console errors. Checked at a true 375px width: 0 page overflow, and even at the deepest (5th) nesting level the card was still a readable 202px wide. |
| 2026-09-14 | **Fixed ClickUp leave-request sync silently dropping the Manager/Employee "people" fields on some tasks** — user-reported bug ("excuse submissions aren't assigning the direct manager in ClickUp"). Root-cause investigation (production DB queries + live ClickUp API probing, not just code reading) found the reported symptom was actually three unrelated, independently-confirmed causes wearing the same face: (1) a real code gap — `clickupLeaveSync.js`'s `setUsersFields` fired each "people"-type field-set call once with no verification; live-replaying the exact same call against a real, already-broken production task (`123xrj7hrhx`) made it stick immediately, proving ClickUp's own documented flakiness (already called out in this file's `buildCustomFields` comment for the *bundled* create-time case) also silently drops the *individual* follow-up call this code was relying on as the fix; (2) several employees (Alaa Yakout, Omar Mohyeddin, Mahmoud Farag, a duplicate Meral Mahmoud roster row) have no `clickup_user_id` because the sync job matches purely by exact login-email-to-ClickUp-email equality with zero fallback or manual-override UI, and their login email differs from their ClickUp account's email (role email, personal Gmail, one-character typo, or a stale duplicate signup) — confirmed employee-by-employee against production `users`/`employees` tables and the live ClickUp `/team` member list; (3) two employees (Hana Ismail, Engy Sameh) simply have no manager assigned in the roster at all, so a blank Manager field for them is correct, not a bug. Only (1) was fixed in this pass, since it's the one genuine code defect and is self-contained; (2) needs a roster-UI field to manually set `clickup_user_id` (backend already accepts it via `PATCH /api/employees/:id`, just has no UI control) and (3) is a data/roster-completeness decision for People & Culture — both intentionally deferred as separate follow-ups, not silently dropped. | Portal §4–7 (Leave/Time-Off, ClickUp sync) | Implemented but with a live, user-reported data-integrity bug | Retry-on-verify added for the flaky-write case; the two data-gap causes documented and deferred, not yet fixed | `server/src/modules/employees/services/clickupLeaveSync.js` (`setUsersFields` now reads the task back after each POST and retries once after a 500ms delay if the value didn't persist; the previously-silent "no value to set" skip now logs at info level with the field name, so a blank field caused by a roster/linking gap is distinguishable in logs from this sync actually failing) | Root cause confirmed against real production data end-to-end: SQL queries against the production `app.db` (employee/manager FK chains, `clickup_user_id` values) cross-checked against live ClickUp API calls (`GET /list/.../field`, `GET /list/.../task`, `GET /team`) proved the DB and field-ID mapping were correct and pinned the failure to the write itself; then a live replay of the exact field-set call against a real broken production task confirmed it was a write-reliability bug, not a data or logic bug. The fix itself was smoke-tested with a mocked ClickUp client (no test infra exists in this repo) covering three paths: skip-with-log (no value to set), retry-then-succeed (first read-back empty, second confirms), and retry-then-still-failed (logs the final error) — all three behaved as intended. Not yet verified against a live ClickUp write in production (the earlier live replay used a real HTTP call but predates this code change; the code path itself has only been exercised via mocks). |
| 2026-09-14 | **Added a manual "ClickUp ID" column to the Roster admin table** — direct follow-up to the ClickUp-sync fix above; deferred item (2) from that investigation. The automatic `clickup_user_id` linking job matches purely by exact login-email-to-ClickUp-email equality with no fallback, so several real employees (Alaa Yakout, Omar Mohyeddin, Mahmoud Farag, a duplicate Meral Mahmoud row) can never link automatically and previously had no way to be fixed by hand at all. No backend change was needed — `PATCH /api/employees/:id` already accepted and audited a `clickupUserId` field end-to-end (`rosterController` → `rosterService.update` → `employeeRepository.update`), it simply had no UI control exposing it. Added one text-input column to the existing roster table, wired to the same autosave `rosterFieldChanged` pattern every other roster field already uses (no new JS function, no new endpoint). | Portal §4–7 (ClickUp sync), Portal §2 (Reporting & Organization) | No way to manually correct a broken `clickup_user_id` link — only the fully-automatic, no-fallback email-matching job could ever set it | Admin/P&C/manager/operations can now view and directly edit any employee's ClickUp user id from the roster table | `server/src/modules/employees/views/js/roster.js` (`renderRosterTable`'s row template: new `<td>` with a text `<input>` calling `rosterFieldChanged(id, 'clickupUserId', this)`; table header and both empty-state `colspan`s bumped 12→13) | Live-verified in a real browser against local dev data (created a throwaway local-only admin test account for this, since the existing session's account lacked nothing relevant — cleaned up afterward): confirmed the new column renders with "Unlinked" placeholders for employees with no link and real ids (e.g. `112517131`, `88503603`) for linked ones, matching production's actual values; edited a test row's id to `999888777`, confirmed the "Saved" toast, then reloaded the page and separately queried the local `app.db` directly to confirm the value actually persisted server-side (not just an unsaved DOM value) before reverting the test edit. Deferred items (2)'s actual data fixes (relinking Alaa/Omar/Mahmoud, deciding on the duplicate Meral row) and (3) (Hana Ismail/Engy Sameh's missing manager assignment) still need a human decision — this only adds the tool to do it. |
| 2026-09-14 | **Upgraded the roster's ClickUp ID field from a raw text input to a searchable member picker** — same-day follow-up once the user tried the field above and pointed out a real usability gap: ClickUp's own UI never displays a member's numeric id anywhere a non-technical person would find it, so the plain text field from the previous entry was only usable by someone willing to query the ClickUp API directly. Added a new read-only, roster-permission-gated endpoint that proxies ClickUp's own member list (id/name/email only — never the API key, never anything else ClickUp's member object carries) so the roster table can render a real `<select>` of actual people instead of asking for a bare number. Chose a plain `<select>` (browser-native type-ahead-to-jump is sufficient for ~30 members) over building a custom autocomplete widget, matching this file's own existing dropdown conventions (`departmentOptionsHtml`, `jobTitleOptionsHtml`) rather than introducing a new UI pattern for one field. | Portal §4–7 (ClickUp sync), Portal §2 (Reporting & Organization) | ClickUp ID field required already knowing someone's raw numeric id, discoverable only via direct API access | Dropdown lists every real ClickUp workspace member by "Name (email)", searchable via native select type-ahead, current value preserved even if it's since left the workspace (same fallback pattern as the department/job-title dropdowns) | `server/src/modules/employees/services/clickupUserSync.js` (new `fetchTeamMembers`/`listMembers`, `run()` refactored to share the same fetch instead of duplicating it); `server/src/modules/employees/services/rosterService.js` (`listClickupMembers`, gated by the existing `requireCanManageRoster`); `server/src/modules/employees/controllers/rosterController.js` (`clickupMembers`); `server/src/modules/employees/routes/index.js` (`GET /employees/clickup-members`, wrapped in `catchAsync` since it's the module's first async roster-management endpoint); `views/js/roster.js` (`loadClickupMembers`, `clickupMemberOptionsHtml`, the ClickUp ID `<td>` swapped from `<input>` to `<select>`) | Live-verified end-to-end in a real browser (restarted the local dev server to pick up the new route, confirmed session survived via the refresh-token cookie): `GET /api/employees/clickup-members` returned 200 with zero console errors; read the actual rendered `<select>`'s DOM and confirmed it lists the real, live ClickUp workspace (Alaa Yakout, Mahmoud Farag, Marie Haddad, etc. — the same real people this investigation found earlier), alphabetically sorted, with the employee's current link correctly pre-selected; selected "Mahmoud Farag" for a test row, confirmed it persisted by querying `app.db` directly, then reverted the test edit and deleted the throwaway QA accounts used for this and the prior entry. |
| 2026-09-14 | **Fixed portal-submitted leave requests always landing in ClickUp's ungrouped "empty" bucket instead of their month; added a submit-button spinner** — two more user-reported issues from the same day's ClickUp work. (1) The list view groups tasks by a "Month" drop-down custom field; `clickupLeaveConstants.js` already had the field's id defined (`CF.MONTH`) but `buildCustomFields` never actually pushed a value for it, so every request created by the server-side sync (as opposed to the older manually-created tasks, which had a human pick the month by hand) landed with that field blank — ClickUp's grouped view has no separate "unset" bucket, it just calls it "empty". (2) `timeOff.js`'s Submit Request button only ever did `btn.disabled = true` with no visible feedback; now that a submit also waits on the ClickUp retry-and-verify logic from earlier this same day, a submission can take a few seconds and the button looked frozen/unresponsive for that whole window. | Portal §4–7 (ClickUp sync) | Every portal-submitted request appeared in ClickUp's "empty" group regardless of its actual date; submit button gave no feedback beyond going gray | New requests post directly into their correct month's group in ClickUp; Submit Request shows an inline spinner + "Submitting…" while in flight | `server/src/modules/employees/services/clickupLeaveConstants.js` (`MONTH_OPTIONS`, the 12 real option ids fetched live from `GET /list/.../field`); `server/src/modules/employees/services/clickupLeaveSync.js` (`buildCustomFields` now pushes `CF.MONTH` from `start.getMonth()`); `views/js/timeOff.js` (`submitRequest` swaps the button's innerHTML to a spinner + "Submitting…" for the duration of the request, restored in every exit path — the early return, the success path, and `finally`); `views/css/employees.css` (`.btn-spinner`, an inline `currentColor`-based ring reusing the existing `spin` keyframe rather than a second animation) | Live-verified against the real ClickUp API end-to-end, not just read the code: created a disposable test task through the actual `clickupLeaveSync.createTask` function (not a mock) and confirmed its Month field read back as orderindex `8` (September) for a 2026-09-20 request, deleted it, then re-verified through the full real HTTP flow — submitted an actual request via a real browser against the restarted local dev server, confirmed the resulting live ClickUp task's Month field also read back as `8`, then deleted that task and its `leave_requests` row too. Separately screenshotted the Submit Request button mid-flight in the same browser session and confirmed the spinner + "Submitting…" text render correctly before the success banner replaces the form. |
| 2026-09-13 | **Added a status filter + name search to the Commercial Leads "Active Clients — AM" table** — this list previously had zero client-side controls (unlike the "2026 Projects" pipeline table directly above it in the same page, which already had status/country/source filters + search). Added a matching `.table-toolbar` (Status `<select>` + search `<input>` + Clear filters button), sourcing status options dynamically from the synced ClickUp list's own statuses (no hardcoded enum — this list's canonical status set lives only in the `commercial_lead_status_colors` table, synced from ClickUp) exactly like the existing pipeline filter already does. Out of scope for this audit's Portal/Governance scoring (line 91 — Commercial Leads is explicitly a real, out-of-requirement-doc feature), noted here only per the standing "touch the tracker after any code change" practice. | N/A — out of requirement-doc scope | No filter/search controls on this table | Status filter + search + clear button, mirroring the pipeline table's existing pattern | `server/src/modules/management/commercial-leads/views/index.html` (`.table-toolbar` added to the Active Clients card); `views/js/deals.js` (`populateActiveClientsFilterOptions`, `applyActiveClientsFilters`, `updateClearFiltersButtonActiveClients`, wired into `renderActiveClientsTable`/`bindDealsUi`); `views/js/state.js` (`activeClientsFilters`) — working-tree change, not yet committed | Live-verified in a real browser session (`tester1@example.com`, manager role): selecting "in progress" correctly narrowed 21 rows to the exact 3 with that status; searching "juno" correctly isolated the one matching name; Clear filters correctly reset both controls and the full 21-row list. No computed insights were added — this list stays exactly "live display only" as labeled, per the user's own framing of the request. |
| 2026-09-15 | **Added a same-day-after-9am WFH rule: P&C-facing warning + deduction, no auto-reject** — WFH previously had no notice-window rule of its own (`checkNoticeWindow` explicitly exempts it, only the monthly quota was enforced). User asked for a WFH request submitted for *today*, at/after 9:00 AM, to be flagged to People & Culture with a deduction applied — but, per the user's explicit answers to clarifying questions, *not* auto-rejected like short_notice/mental_health's same-day rule, and the deduction stays a **manual** P&C judgement call via the existing dropdown (not auto-set), and the warning shows **only** on the P&C queue card, not the shared manager card. New pure function `timeOffRules.isWfhLateSameDaySubmission({leaveType, submittedAt, startDate})` (same-calendar-day check + `getHours() >= 9`); computed on read in `listPcPending` (`wfhSubmittedLate`) exactly like the existing `requiresDoctorNote` flag — no new column, no write-path. Also wired into the existing live pre-submit notice-check endpoint (`checkNotice` now also returns `lateWfhSubmission`) so the New Request form shows the same yellow `alert-warn` box other types already get, and updated the WFH policy copy on the type-dropdown hint and the Rules tab. | Portal §3 (Leave Balance) / §4 (Time Off Requests) — WFH had no notice-window rule at all | WFH: quota-only, no lateness signal anywhere | WFH: quota unchanged, plus a same-day-after-9am flag surfaced to P&C (warning only) and live on the request form | `server/src/modules/employees/services/timeOffRules.js` (`isWfhLateSameDaySubmission`, `WFH_SAME_DAY_CUTOFF_HOUR`); `timeOffService.js` (`listPcPending`'s `wfhSubmittedLate`, `checkNotice`'s `lateWfhSubmission`); `views/js/team.js` (`lateWfhWarningHtml`, wired only into the P&C-pending section's `requestCard` call, not the manager sections); `views/js/timeOff.js` (`checkNoticeLive`, Rules tab WFH alert copy); `views/js/leaveTypes.js` (WFH `notice` hint text) — committed `33a4ff3` on `feature/wfh-9am-rule-and-jobtitle-dropdown`, pushed to `origin`, PR not yet opened | Four clarifying questions asked before implementing (same-day-only scope, warn-don't-auto-reject, manual-not-automatic deduction, P&C-only visibility — see §13 Decision Log). Root-cause note from this same session: the local dev server had been running `node src/index.js` (no watch) since the previous day and was still serving pre-change code when the user first tested — the yellow warning genuinely wasn't showing, not a logic bug; fixed by restarting it under `node --watch` (`npm run dev`), left running in watch mode afterward. Logic verified directly (`isWfhLateSameDaySubmission` returns `true` for today's date at the real current time); the authenticated live endpoint itself was not curl-tested past the expected 401 (no session token available to the CLI), so full end-to-end browser confirmation of the P&C-queue warning specifically is still the user's to do, though the underlying computation and the restart root-cause are confirmed. |
| 2026-09-15 | **Made Job title in Account Settings a fixed dropdown, matching the signup wizard and admin roster** | Portal §1 (Employee Profile — Job title) | Implemented (dropdown on signup + admin roster; Account Settings' own Work Details section was still a free-text `<input>`, despite `roster.js`'s own comment already claiming parity) | Implemented (all three surfaces — signup, admin roster, Account Settings — now use the same `JOB_TITLES` dropdown) | `server/public/shared/accountSettings.js` (`renderWorkDetailsSection`'s Job title field, `<input>` → `<select>` sourced from `window.OrgConstants.JOB_TITLES`, with the same "(unmatched)" legacy-value fallback option `roster.js`'s `jobTitleOptionsHtml` already uses) — committed `0abaeb9` on `feature/wfh-9am-rule-and-jobtitle-dropdown`, pushed to `origin`, PR not yet opened | No backend change needed — `rosterService.js`'s `JOB_TITLES` validation on `PATCH /api/employees/me` already enforced the fixed list; this closed a frontend-only gap. Browser-verified live (logged in as `mokhaled2012@gmail.com`): opened Account Settings, confirmed Job title now renders as a `— Select —` combobox, and read the rendered `<select>`'s DOM directly to confirm all 27 real `JOB_TITLES` options are present in the correct order, matching the signup wizard's list exactly. |
| 2026-09-15 | **Process note: PR #56 merged on GitHub, but this file's own tracker-update commit (`b6a0b02`, the row directly above and the one above that) was pushed to the feature branch *after* GitHub had already merged it, so it never made it into `main`** — discovered when the user reported "merged to main" and a `git merge-base --is-ancestor` check confirmed `main` had the code but not the tracker rows. The user declined a direct push of just that commit to `main` (reasonably — a bare force-in felt like the wrong fix for a merge-ordering slip). It surfaced again this same day: a fresh local session had `git checkout main`'d without realizing this gap, and began new work (the email/Forgot-Password feature below) directly on `main` with a stale tracker copy — silently re-losing the same two rows a second time until caught here, mid-edit, before any new commit could bake the loss in permanently. | This document's own integrity | Two Change History rows existed only on a merged-but-orphaned feature branch, absent from `main` | Recovered via `git show b6a0b02 -- docs/governance/implementation-tracker.md \| git apply -` against the current working copy, before adding anything new | N/A — process/documentation only | **Lesson for future sessions**: after any `git checkout <branch>` mid-session, diff the tracker against what's expected before trusting it as current, especially right after a user-reported merge — don't assume "merged" means "everything intended for that PR actually landed." The new email/Forgot-Password work below is being committed to its own fresh feature branch (not `main`) specifically so this can't recur the same way a third time. |
| 2026-09-15 | **Built the app's first outbound-email capability: a standalone Resend integration, ahead of any caller.** First of three features the user is planning (email, notifications, Forgot Password); user explicitly chose to stand this up on its own first rather than build it directly into Forgot Password, so it's a reusable, independently-testable module before anything depends on it. Mirrors the existing `common/integrations/clickupClient.js` shape exactly (plain `fetch`, no SDK dependency, the one place its API key is read) rather than installing the `resend` npm package. Config added properly through `config/index.js` (`resendApiKey`, `emailFrom`) — deliberately not copying the pre-existing, documented `process.env` shortcut `clickupClient.js` itself takes, since that shortcut is flagged in `config/index.js`'s own comment as known debt, not a pattern to extend. | N/A — new capability, no prior email sending anywhere in the app | No email-sending capability existed at all | `sendEmail({to, subject, html, text})` available to any module; verified working via a real API-key failure path (see Testing note) | New `server/src/common/integrations/resendClient.js`; `server/src/config/index.js` (`resendApiKey`, `emailFrom`); new `server/src/test-email.js` + `npm run test-email <address>` CLI script (same precedent as `create-user`/`seed`, not a permanent HTTP route) — working-tree change on a new branch, not yet committed | Extensive back-and-forth with the user (who is non-technical on the DNS/email-infra side) on Resend account setup: account email vs. sending domain are different things; recommended a dedicated subdomain (`updates.theceas.com`) over the root `theceas.com` specifically to avoid risking the company's real Google-Workspace-hosted `@theceas.com` mail deliverability via an SPF-record mistake; walked through Return-Path (left at Resend's `send` default) and click/open tracking (left off — inappropriate for transactional mail, and Resend's own docs flag open-tracking as unreliable). **Domain verification is still pending** as of this row — `RESEND_API_KEY` is set in `server/.env` (gitignored), `EMAIL_FROM` is not yet, so `sendEmail` currently fails fast with a clear "Email is not configured" `AppError` rather than silently no-op'ing; confirmed this exact failure path fires correctly via a real `forgotPassword` call below, once Forgot Password was built on top of it. No live email has actually been delivered yet — that's the one remaining step, gated on the user's domain DNS propagating and Resend showing Verified. |
| 2026-09-15 | **Built Forgot Password end-to-end (backend + login-page UI), the second of the three planned features and the email module's first real caller.** New `password_reset_tokens` table (migration `023`), modeled directly on the existing `refresh_tokens` table/`refreshTokenRepository.js` — random 32-byte token, only its sha256 hash ever persisted, 30-minute TTL (short, unlike refresh tokens' 30 days, since a leaked reset-email link is time-sensitive), single-use (`used_at`), and a new request invalidates any still-pending earlier one for that user. `authService.forgotPassword`/`resetPassword` added alongside the existing `login`/`changePassword`, reusing exactly the same patterns: `AuthError` for user-facing failures, `transaction()` + `refreshTokenRepository.revokeAllForUser(..., 'password_reset')` on completion (forces re-login everywhere else, identical to `changePassword`'s own behavior), and `audit.record()` for both the request and the completion. **User-enumeration is the one property enforced hardest here**: `forgotPassword` always resolves the same way and the controller always returns the same generic message, regardless of whether the email matched a real, active account — a deactivated account is deliberately treated identically to a nonexistent one (it already can't log in with its real password either, so letting a reset resurrect it would be inconsistent), and a real send failure (e.g. today's unverified-domain case) is only logged, never thrown, so it can't be distinguished from "no such account" by the response. A dedicated, tighter rate limit (5/15min, IP-keyed) was added on `/auth/forgot-password` specifically, separate from the shared 120/min `authLimiter` every other auth route uses, since this route has a real external cost/spam surface the others don't. Frontend: two new cards on the existing single-page `/login` (`forgotPasswordForm`, `resetPasswordForm`), following that page's existing show/hide-cards pattern; the reset card is triggered by a `?resetToken=` URL param, checked *before* the page's existing already-logged-in auto-redirect (a reset must work even from a browser with a live session), and the token is stripped from the visible URL via `history.replaceState` immediately so it doesn't linger in history. | N/A — neither requirement doc names a self-service reset path (Governance doc's account lifecycle is HR-request → Ops-approval → Admin-provision only, no reset flow) | No password-reset path existed; a locked-out user's only option was asking an admin | Full self-service reset flow, live and fully verified except the actual email delivery (blocked on Resend domain verification, see the email-module row above) | New `server/src/db/migrations/023_create_password_reset_tokens.js`, `server/src/modules/auth/repositories/passwordResetTokenRepository.js`; `authService.js` (`forgotPassword`, `resetPassword`); `authController.js` (same names, `forgotPassword` wrapped in `catchAsync` — the auth module's first async controller function); `routes/index.js` (`POST /auth/forgot-password` with the new dedicated limiter, `POST /auth/reset-password`); `container.js` (wiring `passwordResetTokenRepository`, `resendClient.sendEmail`, `logger` into `authService`); `views/index.html`/`views/js/main.js`/`views/css/login.css` (the two new cards, `showOnly()` replacing the old three-way hidden-toggle helpers, `resetPasswordHint` reusing signup's existing `passwordStrength()`) — working-tree change on a new branch, not yet committed | **First caught a real, unrelated infrastructure problem while testing**: the very first `forgot-password` request hit `EADDRINUSE` then a `SQLITE_CONSTRAINT_PRIMARYKEY` migration race — turned out to be **six** leftover `node --watch` dev-server processes accumulated across old sessions going back to Aug 25, all watching the same files and fighting over port 3001 and the same SQLite file on every edit-triggered restart; killed all six, confirmed migration `023` had still applied cleanly despite the race, restarted a single clean instance. **Then fully live-verified in a real browser**: logged out and confirmed the plain `/login` page (new "Forgot password?" button visible); submitted the real admin account's email through the actual UI, confirmed the generic success message, and confirmed server-side via direct DB query that a real token row was issued (`expires_at` correctly ~30 minutes out) and the expected `Email is not configured — set EMAIL_FROM` failure was logged (not thrown) — proving the whole chain works right up to the actual send, which only fails because domain verification is still pending. For the reset-page UI itself, used a **disposable throwaway account** (`zzz-test-forgot-password@ceastest.local`, created via the existing `create-user.js` script) rather than risk the real admin account's live credentials — minted a valid raw token directly via the real repository code (not a mock), visited `/login?resetToken=...`, set a new password through the actual form, confirmed the redirect back to `/login`, then independently verified via direct `bcrypt.compareSync` against the DB that the password hash actually changed, the token's `used_at` was set, and the `user.password_reset_completed` audit row was written — then deactivated the throwaway account afterward (never hard-deleted, per this repo's revoke-never-delete convention). **Not yet tested**: the actual email arriving in an inbox (blocked on domain verification) and the reused/expired-token error path in the UI (the repository-level `used_at`/expiry check was exercised structurally but not re-submitted against an already-used token through the browser). |
| 2026-09-15 | **User-requested security review of the Forgot Password feature specifically ("what if I enter a colleague's account and hit send?"), found and fixed one High and one Medium vulnerability, both proven exploitable and re-verified fixed.** (1) **High — password-reset-link poisoning via Host header injection**: `authController.forgotPassword` built the emailed reset link from `` `${req.protocol}://${req.get('host')}` ``; with `trust proxy` enabled (required for Render), that header is attacker-controllable on the very request that triggers the email. Proved it directly — a spoofed `Host: evil-attacker.com` header produced a real reset email (to the real target's real inbox) containing `https://evil-attacker.com/login?resetToken=<valid token>`; if clicked, the attacker's own server captures a working token for an account they don't own. Root cause and fix generalize to any future feature that emails a link: never derive it from request data when `trust proxy` is on. (2) **Medium — timing-based account enumeration**: `sendEmail(...)` was `await`ed before responding, so existing-account requests took a real Resend network round-trip while nonexistent-account requests returned near-instantly — a reliable side channel defeating the endpoint's own generic-response anti-enumeration design. | Portal §1/Governance — auth security (no requirement-doc section; core security property of the just-built Forgot Password feature) | Two live-exploitable vulnerabilities in code built earlier the same day | Both fixed; reset links always use a fixed, operator-configured origin; response timing no longer depends on account existence | `server/src/config/index.js` (new `appBaseUrl`, `process.env.APP_BASE_URL` with a `localhost` dev default); `authController.js` (`forgotPassword` now takes injected `appBaseUrl` instead of reading `req.protocol`/`req.get('host')`); `container.js` (wires `config.appBaseUrl` in); `authService.js` (`forgotPassword`'s `sendEmail(...)` call changed from `await`ed to fire-and-forget with its own `.catch()`-based logging; `audit.record` for `password_reset_requested` moved to fire on token issuance itself, not gated on the email send succeeding); `render.yaml` (added `APP_BASE_URL` — set to the real prod origin `https://pricing-portal.onrender.com` — plus `RESEND_API_KEY`/`EMAIL_FROM` as `sync: false` placeholders, so a future deploy can't silently miss them) | **Every finding proven, not just read.** Host-header attack: called the real (pre-fix) `authService.forgotPassword` directly with a spoofed `baseUrl`, captured the exact HTML that would have been emailed via a stubbed `sendEmail`, confirmed the attacker domain + a real valid token were both present. Timing: measured real response times before/after for an existing vs. nonexistent account. Post-fix: re-ran the same spoofed-`Host` test through the real `authController.forgotPassword` (mocked `authService`/`req`) and confirmed `baseUrl` is now always `http://localhost:3001` (the config value) regardless of the header; re-measured timing and confirmed both paths now respond in ~0ms, indistinguishable. Rate limiting on `/auth/forgot-password` was also live-confirmed working (already exhausted from this session's own testing, returning 429). All throwaway-account testing used `zzz-test-forgot-password@ceastest.local`, deactivated afterward. |
| 2026-09-15 | **Full-application security review, user-requested ("review the security of everything in the website, fix every vulnerability") — not scoped to the current diff.** Split across 4 parallel streams (self + 3 forked sub-agents, following this session's own security-review skill's methodology adapted for whole-repo scope rather than a single diff): (1) coordinator reviewed the auth core directly — JWT sign/verify (including a live-tested PoC confirming `jsonwebtoken` v9 correctly rejects a forged `alg:none` token, no algorithm-confusion bypass), bcrypt config (cost 12, standard), refresh-cookie flags, `authenticate` middleware (re-reads role/active-status from DB every request, never trusts the JWT payload for authorization), `requireRole`/`permissions.js` (role-escalation/admin-shielding logic), `accountAdminService`, `errorHandler` (confirmed no stack-trace/internal-path leakage to clients) — all clean, no findings; (2) fork: SQL injection sweep (all repositories, every module) — clean, all dynamic SQL uses parameterized `?` values even where column sets are built dynamically; (3) fork: authorization/IDOR audit (every route in auth/employees/commercial-leads/ceo-dashboard) — clean, every mutating endpoint verified against the *specific* resource, not just "is authenticated" (leave-request decisions checked against the real manager FK, KPI access checked against actual reporting line, notifications scoped by `user_id` in the SQL itself); (4) fork: file uploads/ClickUp webhook signature/CORS/hardcoded secrets/command-injection/cookie config — clean across all six (photo upload already had magic-byte validation + UUID filenames + path-traversal guard from before this session; webhook signature already used `crypto.timingSafeEqual`; zero `cors()` usage anywhere, confirming CLAUDE.md's "never reintroduce permissive CORS" rule holds; zero hardcoded secrets; zero `eval`/`exec`/`child_process`). **One real, High-confidence finding, from the SQL/XSS fork — fixed same session**: stored XSS in the signup wizard's team-head dropdown (`modules/auth/views/js/main.js`'s `refreshManagerOptions`), which interpolated real employee `firstName`/`lastName` into `innerHTML` completely unescaped — this view module's own `dom.js` was the one file in the entire app with no `escapeHtml` helper at all (every other module's `dom.js` has one). Exploitable pre-authentication: self-registration only `.trim()`s first/last name (no HTML stripping), and if that person is later made a department's team head (an ordinary roster action, no re-sanitization), the payload executes in the browser of every unauthenticated visitor who opens the signup wizard and picks that department — no login required to trigger it. | N/A — general application security, not tied to a single requirement-doc section | One live stored-XSS vulnerability (pre-auth-reachable), rest of the app's security posture already solid | Fixed; `modules/auth/views/js/dom.js` now has the same `escapeHtml` every other module already uses, applied to the team-head dropdown | `server/src/modules/auth/views/js/dom.js` (new `escapeHtml`, copied verbatim from the established pattern e.g. `modules/employees/views/js/dom.js`); `views/js/main.js` (`refreshManagerOptions`'s `<option>` building now escapes `h.firstName`/`h.lastName`/`h.id`) | Verified the exact payload the fork identified (`<img src=x onerror=alert(document.cookie)>`) is correctly neutralized by the new `escapeHtml`; syntax-checked both changed files. Did not independently re-verify every one of the ~20 files the initial broad `innerHTML` grep surfaced (the SQL/XSS fork prioritized by realistic user-input risk given its time budget and flagged the KPI/CEO-dashboard chart renderers — mostly numeric/computed data — as lower priority, not individually confirmed clean); worth a follow-up pass if a future session has spare budget, though risk there is assessed as low. |
| 2026-09-15 | **Diagnosed and fixed "Who's Online" (Portal §6, Team Availability) not showing colleagues to each other, then rebuilt it as real-time Socket.IO presence instead of the original 5-minute polling heuristic.** User reported testing on 2 laptops/2 accounts and never seeing each other online. Root cause was two compounding design gaps, both confirmed by reading the actual code (no live reproduction needed — the gaps were structural): (1) **no heartbeat** — `apiClient.js`'s token refresh is purely reactive (only fires on a real 401), so a browser tab sitting open with no further clicks stops generating any request at all, and `last_seen_at` (hence "online") goes stale exactly 5 minutes after whatever the last real click was; (2) **no live refresh** — `main.js` only calls `renderOverview()` when the user switches *to* the Overview tab, so even a genuine simultaneous "online" moment would never surface unless both people happened to (re)land on Overview at that exact moment. Combined, two people could have the portal open the whole time and never once see each other. User chose the bigger, cleaner fix over a heartbeat+poll patch: true push-based presence, since Socket.IO already runs in this app (`common/realtime`, previously only used for ClickUp sync events) — this removes the staleness window entirely rather than shrinking it. | Portal §6 (Team Availability) | "Online" = active within the last 5 minutes, computed in SQL from `last_seen_at`; required a manual tab-switch/reload to see any change | "Online" = has a live Socket.IO connection right now; pushed to every other connected browser the instant it changes, no polling, no refresh needed | `server/src/common/realtime/index.js` (new `onlineUserSockets` map keyed by user id -> Set of socket ids, so one user with multiple tabs/devices only flips offline once the *last* one closes; `broadcastPresence`/`isUserOnline`/`getOnlineUserIds`; socket identity verified once at connect time via an injected `verifyAccessToken`, sent through Socket.IO's handshake `auth` option rather than a query string so it never lands in access logs); `modules/auth/container.js` (exposes `verifyAccessToken` publicly — common/ still never imports modules/auth directly, per the module-boundary rule; index.js, the composition root, is what wires the two together, consistent with ADR-0006's manual-DI approach); `server/src/index.js` (passes it into `initRealtime`); `employee.model.js` (`toDirectoryEntry` now carries `userId`, defaults `online: false`); `employeeRepository.js` (removed the now-dead `user_online`/5-minute SQL computation — `last_seen_at` itself is untouched, still written by `touchLastSeen`, just no longer read here); `rosterService.js` (`listDirectory` overrides `online` per entry from `realtime.getOnlineUserIds()`, injected via `container.js` same as every other dependency); `views/js/overview.js` (`whosOnlineSection` card gets a stable `id`, new exported `applyPresenceChange(userId, online)` patches a cached directory array and re-renders just that card, no-ops quietly if Overview isn't the currently-visible tab); new `views/js/realtime.js` (mirrors commercial-leads' existing `realtime.js` pattern — global `io()` from a script tag, not an import; connects once after `bootstrapAuth()` with the access token in the handshake `auth` option; on an actual *reconnect* — not the initial connect — re-runs `renderOverview()` if Overview is currently showing, to resync anything missed while the socket was down); `views/index.html` (added the `/socket.io/socket.io.js` script tag, same path commercial-leads already serves it from — no new server route needed). | **Fully live-verified in a real browser, both directions.** Registered a genuine throwaway employee account (`zzz-presence-test@ceastest.local`, real employee profile via the actual `/api/auth/register` endpoint, not just a bare user row) to get a real, valid access token without needing a second physical machine. Logged into the real account (`mokhaled2012@gmail.com`) in an actual browser tab, confirmed it immediately self-listed as online via the new system. Then, in that same tab's own JS console, opened an *independent* second Socket.IO connection authenticated as the throwaway account (`io({auth:{token:...}})`) — confirmed the first account's already-open Overview card updated to show the second person **instantly, with zero reload, zero tab-switch, zero manual refresh** ("1 online" → "2 online", the new name appearing in the tile grid). Disconnected that second socket and confirmed the card reverted to "1 online" just as instantly. Both the connect-broadcast and disconnect-broadcast paths are proven working end-to-end, not just read. Throwaway account deactivated afterward (not deleted, per this repo's revoke-never-delete convention). Not yet tested: the actual two-physical-device scenario the user originally reported (this verification used one browser with two independent socket identities rather than two literal laptops, since a single Chrome profile's shared cookie jar can't hold two simultaneous logged-in sessions the way two separate machines can) — the underlying mechanism proven here is identical either way, but the user may want to confirm on their own two laptops directly. |
| 2026-09-16 | **Moved the company directory ("Teams") from a modal reachable only via the shared account-menu dropdown into its own "Teams" nav tab in Employees Portal, placed beside "My Team"; removed the "My Team" tab's role-based visibility gate.** User asked where best to surface reporting-line info; discussion surfaced that manager/reports display already existed in ~6 places, so the actual ask became relocating the existing "Teams" entry point. User explicitly chose the true-move option (delete from the shared account-menu everywhere, not just Employees Portal) after being shown the tradeoff that CEO Dashboard/Commercial Leads/the legacy Margin Planner page would lose their only way to reach the directory — accepted, no replacement built for those three. Also chose to convert the directory from a modal into a real inline tab (not just relocate the modal-trigger button), styled as a normal nav-tab pill. Separately, user reported manual roster-add is no longer how employees get provisioned (signup always creates the employee profile now) and asked to remove "My Team"'s `TEAM_TAB_ROLES`/`canSeeTeamTab` gate entirely. | Portal §2 (Reporting & Organization) / §19 (My Team) — UI reorganization only, not a new backend capability; the manager-reporting-line data itself (already Implemented, §2 row above) is unchanged | "Teams" reachable only via account-menu modal (shared across Employees Portal/CEO Dashboard/Commercial Leads); "My Team" tab hidden for any account with no employee profile that also wasn't `manager`/`people_culture` | "Teams" is a standalone always-visible nav tab in Employees Portal only (new `views/js/teamsDirectory.js`, same department-grouped/expandable-card content, no modal); account-menu "Teams" item removed everywhere; "My Team" tab always visible | New `server/src/modules/employees/views/js/teamsDirectory.js`; `server/src/modules/employees/views/index.html` (`#maintab-teams`/`#tab-teams`, gate removed from `#maintab-team`); `server/src/modules/employees/views/js/main.js` (tab dispatch + gate removed); `server/public/shared/accountMenu.js` (Teams item + listener removed); `server/public/shared/accountMenu.css` (orphaned `.team-directory-modal` rule removed, comment updated); deleted `server/public/shared/teamsDirectory.js` and its `<script>` tag from `employees`/`ceo-dashboard`/`commercial-leads` `views/index.html`; stale in-code comments referencing the old modal updated in `team.js`/`roster.js` — working-tree change on `feature/email-and-forgot-password`, not yet committed | Gate removal verified safe by reading the actual code before removing it, not assumed: both endpoints "My Team" unconditionally calls (`GET /employees/team/mine`, `GET /employees/leave-requests/team`) already null-guard a missing `req.employee` server-side (return empty, not an error) and neither route has role-based restriction; confirmed `create-user.js` can mint *any* role with zero employee record at any time (not just a one-time bootstrap case), so "account with no employee profile" is a real, reachable scenario. **Fully browser-verified live**: as a real existing account, both new/changed tabs render correctly (card expand shows Department/Status/Email/Manager; My Team + Organization Chart cards unaffected) and the account-menu "Teams" item is gone; created and logged in as a throwaway admin account with zero employee profile (`smoketest.*@example.com`, local dev DB only, via `create-user.js`) to confirm the no-profile edge case degrades gracefully — My Team tab shows only the Organization Chart card (no crash, no broken empty state), Teams tab renders normally; confirmed via `/ceo` that the account-menu "Teams" item is gone there too. No console errors on any load. While verifying, also found and corrected a **pre-existing tracker error**: §2's "Organizational structure (org chart)" row was marked "Not Started" with evidence "grep confirms absence" — but a real, working client-side org-chart tree (`orgChartSectionHtml` in `team.js`) already existed before this session; the original grep for the literal string "org chart" missed it because the feature is labeled "Organization Chart" (capital, no hyphen) in the UI and named `orgChart*` in code. Corrected across §1 Executive Summary, §2's score row (50%→75%) and detail table, the module-weighted view, and §9 Gaps and Contradictions — see those sections. **Left as-is / not done**: the throwaway `smoketest.*@example.com` test admin account was not deactivated afterward (flagged here for the user rather than self-managed, since account lifecycle management wasn't part of this task); a per-employee multi-level "reporting chain" breadcrumb (e.g. in Account Settings or the new Teams tab) was discussed as a follow-up idea but not built. |
| 2026-09-16 (same day) | **Moved the Organization Chart from "My Team" to the new "Teams" tab, and restructured it: root is now always the single 'manager'-role account (the CEO), with team heads as an explicit flat tier directly beneath it, rather than a plain `managerEmployeeId` tree where "no manager" was the only thing that produced a root.** User's own words: CEO on top, team heads below it, "below every team head is the accounts they manage and so on"; also asked for more indent between nesting levels "for better visibility." Required one small backend change to make the CEO identifiable client-side at all: `toDirectoryEntry` (`employee.model.js`) previously exposed no auth-role information whatsoever (deliberately, per its own comment) — added a single narrow boolean, `isCompanyManager` (`row.user_role === 'manager'`), rather than the full role string, to answer exactly "is this the CEO" without widening exposure further. Verified against the real local dev DB before implementing (not assumed): queried `employees`/`users` directly and found the data is messier than the old algorithm assumed — the 'manager'-role account's own `manager_employee_id` pointed at an unrelated employee (bad/stale data), and the one real team head had no manager assigned at all — both of which would have made the *old* algorithm nest the CEO under a subordinate and/or produce multiple competing top-level roots. Designed the new algorithm to be robust to exactly this: the root is always the 'manager'-role account regardless of its own `managerEmployeeId`; team heads are always tier 2 regardless of theirs; any other employee whose manager doesn't resolve (bad data, or a genuine orphan) *and* any real non-team-head direct report of the CEO also lands at tier 2 rather than being dropped or silently merged away — nobody who was visible in the old tree disappears in the new one. | Portal §2 (Reporting & Organization) — restructures the existing org-chart *visualization* only; the underlying `managerEmployeeId`/`isTeamHead` data model is unchanged | Org chart lived on "My Team", rooted at whichever employees happened to have no manager (data-dependent, could produce multiple or "wrong" roots); 20px child indent | Org chart lives on "Teams" (`teamsDirectory.js`, above the department-grouped grid built 2026-09-15/16); explicit CEO root + team-head tier regardless of `managerEmployeeId` quirks; 32px child indent | `server/src/modules/employees/models/employee.model.js` (`toDirectoryEntry`'s new `isCompanyManager` field + updated comment); `server/src/modules/employees/views/js/teamsDirectory.js` (new `orgChartSectionHtml`/`orgChartNodeHtml`/`orgChartCardHtml`/`orgChartToggleCard`, shared `cardBodyHtml`/`cardDetailHtml` helpers factored out for both the org-chart and department-grid cards); `server/src/modules/employees/views/js/team.js` (all org-chart code removed, `renderTeam`'s `sections` array updated); `server/src/modules/employees/views/css/employees.css` (`.org-chart-children` `padding-left` 20px→32px, comment updated to point at the new location) — working-tree change on `feature/email-and-forgot-password`, not yet committed | **Fully browser-verified live** against the real local dev DB (not test-only data): logged in, opened the Teams tab, and confirmed the rendered tree matches the DB query exactly — Janet Doe (role `manager`) at the true root despite her own stale `manager_employee_id`; Mohamed Khalid (the one real active team head) at tier 2 with a "Team Head" badge despite having no manager assigned; Amy Manager and QA Manager1 (real direct reports of the CEO who aren't team heads) also correctly promoted to tier 2 instead of vanishing; multi-level chains (Sam Lee → test test → Mohamed Khalid) still nest correctly beneath the team head; card expand-on-click still shows Department/Status/Email/Manager for every node. Confirmed "My Team" no longer shows any org-chart content (only its now-narrower own cards). No console errors. Confirmed the department-grouped grid still renders correctly beneath the org chart on the same tab, unaffected. Also corrected a **second tracker-accuracy issue found while fixing the first one from earlier today**: the previous entry's explanation for §2's stale "Not Started" score ("the original audit's grep missed it") was itself inaccurate — the org chart didn't exist yet at the 2026-09-10 audit baseline, so that row was correct when written; the real gap was that the 2026-09-14 Change History entry documenting the build was never propagated back into the §1/§2/module-weighted/§9 rows afterward. Corrected the causal explanation in all four places this time. |
| 2026-09-16 (same day) | **Reordered the Teams tab: department-grouped directory now renders first, Organization Chart below it** (was the reverse, from earlier the same day). User asked for the org chart to be "below all the teams." | Portal §2 (Reporting & Organization) — display order only | Org chart above the department grid | Department grid above the org chart | `server/src/modules/employees/views/js/teamsDirectory.js` (`renderTeamsDirectory`'s concatenation order swapped) | Browser-verified live (same throwaway admin account, `smoketest.*@example.com`) — department grid renders first, org chart at the bottom, tree content/behavior unchanged. No console errors. |
| 2026-09-16 (same day) | **Built "My Reporting Line" on the "My Team" tab, closing Portal §2's "Reporting line (full chain)" bullet** — user asked whether the Organization Chart already satisfied this requirement; it didn't (they're two separate bullets: the org chart is company-wide, this is per-employee), and user specifically wanted the fix on "My Team", not Account Settings (the alternative suggested earlier the same day). New card at the top of "My Team": a breadcrumb ("You → your manager → their manager → ... → CEO"), walking `managerEmployeeId` upward through the same directory the org chart already loads — no new endpoint. Deliberately reflects the *real* FK chain rather than the org chart's idealized CEO/team-head tiering: stops wherever the actual chain ends, even if that's short of the CEO. | Portal §2 (Reporting & Organization) — closes the "Reporting line (full chain)" bullet, previously Partially Implemented | No per-employee reporting-line view anywhere; only the company-wide org chart and each person's one-level-up manager field | "My Team" tab shows the full real chain for the logged-in employee, above the existing "My Team" card | `server/src/modules/employees/views/js/team.js` (new `myReportingLineSectionHtml`, prepended to `renderTeam`'s `sections` array); `server/src/modules/employees/views/css/employees.css` (new `.reporting-line`/`.reporting-line-link`/`.reporting-line-arrow`) — working-tree change on `feature/email-and-forgot-password`, not yet committed | **Fully browser-verified live with a real, freshly-registered account**, not a mock: registered `zzz-reportingline-test@ceastest.local` through the actual `/api/auth/register` endpoint (real signup validation, real employee row) with the one real active team head (Mohamed Khalid) as manager, logged into a real browser session, and confirmed "My Team" renders "You → Mohamed Khalid [Team Head]" — correctly stopping there rather than inventing a further link to the CEO, since Mohamed Khalid's own `manager_employee_id` is genuinely null. No console errors. Deactivated the throwaway account afterward via the real `/employees/:id/deactivate` endpoint (not deleted). Not separately verified: the CEO-viewing-their-own-chain case and the no-manager-assigned empty state (code-reviewed only — no safe way to test either without resetting a real account's credentials). Updated §2's score (75%→88%), detail table, executive summary, module-weighted view, and the §9 gaps entry (now struck through as closed) to match. |
| 2026-09-16 (same day) | **Converted "My Reporting Line" from plain-text breadcrumb links to full cards** — user asked for cards "including the info such as other cards," i.e. the same avatar/name/title/expand-on-click-detail cards used everywhere else (My Team, Org Chart), not text with arrows. Extracted a new `reportingLineCardHtml(e, badges)` in `team.js`, deliberately its own function/id-namespace (`reporting-line-card-`/`reporting-line-detail-`) rather than reusing `myTeamCardHtml`'s ids directly — the same employee (e.g. your manager) can legitimately appear in both this row and the My Team grid below it on the same page load, and reusing ids would collide. Badges are now a list rather than one string, so a card can carry two at once ("You" + "Team Head" if you manage a team yourself). | Portal §2 (Reporting & Organization) — presentation only, same underlying chain-walking logic | Plain-text names joined by → | Full `.team-directory-card`s (avatar, name, job title, expand-on-click Department/Email/Manager) joined by → | `server/src/modules/employees/views/js/team.js` (new `reportingLineCardHtml`/`reportingLineToggleCard`, `myReportingLineSectionHtml` updated to use them); `server/src/modules/employees/views/css/employees.css` (`.reporting-line`/`.reporting-line-link` replaced by `.reporting-line-row` + a fixed-width override on `.team-directory-card` inside it, since the grid's own auto-fill sizing doesn't apply outside a grid container) — working-tree change on `feature/email-and-forgot-password`, not yet committed | **Browser-verified live**, and incidentally exercised a case the previous row flagged as untested: reactivated the same throwaway test account for a second pass, and in the time between passes a background ClickUp reconciliation job had genuinely reassigned some `manager_employee_id`s — the chain now rendered 4 real levels deep and correctly stopped at the CEO despite her own stale manager FK. Card expand-on-click confirmed working (Department/Email/Manager shown correctly for a mid-chain node). No console errors. Deactivated the test account again afterward. |
| 2026-09-16 (same day) | **Surfaced Sick/Unpaid leave usage — uncapped totals on Overview, plus per-request context on P&C's confirmation queue.** User asked for two additions: (1) Overview's "Leave Balance" card should track sick and unpaid leave too, with no cap/fraction like the other four buckets — just "X taken this year"; (2) whenever a request reaches P&C after the direct manager accepts it, P&C should see the requesting employee's running sick/unpaid totals on that same card, regardless of what type the pending request itself is. `leaveBalanceRules.computeBalances` gained two more accumulators (`sickUsed`/`unpaidUsed`), scoped to the same calendar year as every other bucket and using the same working-day counting (partial-day = 0.5) — deliberately *not* gated by whether the sick leave also drew from the combined pool (that's an orthogonal concern: total-sick-taken and combined-pool-consumption are tracked independently). New buckets omit `total`/`remaining` entirely (present vs. absent is the signal the frontend uses to pick capped-fraction vs. uncapped-count rendering) rather than setting them null. `timeOffService.listPcPending` now calls the existing `getMyBalances(employeeId)` once per request and attaches just the two numbers, not the full balances object, to keep the payload scoped to what's actually needed. | Portal §3 (Leave Balance) — sick/unpaid usage was previously trackable in principle but never aggregated or surfaced anywhere | Overview showed only the four capped buckets; P&C's queue cards showed nothing about an employee's sick/unpaid history | Overview: two new rows, "X Sick Leave taken this year" / "X Unpaid Leave taken this year", no fraction. P&C queue: every card additionally shows "Sick leave taken this year: X day(s) · Unpaid leave taken this year: Y day(s)", independent of the pending request's own leave type | `server/src/modules/employees/services/leaveBalanceRules.js` (`sickUsed`/`unpaidUsed`, new `sick`/`unpaid` buckets, `BUCKET_LABELS`); `server/src/modules/employees/services/timeOffService.js` (`listPcPending` now computes `balances` per row via the existing `getMyBalances`, attaches `sickDaysUsedThisYear`/`unpaidDaysUsedThisYear`); `server/src/modules/employees/views/js/overview.js` (new `uncappedBalanceRow`, `leaveBalanceCardBody` updated); `server/src/modules/employees/views/js/team.js` (new `sickUnpaidUsageHtml`, wired into the P&C card's `extraWarningHtml` alongside the existing late-WFH warning) — working-tree change on `feature/email-and-forgot-password`, not yet committed | **Fully browser- and API-verified end-to-end with real accounts, not mocks or direct DB writes.** Built a real 3-employee chain (a throwaway team head as manager, a throwaway P&C account with a real employee profile via the admin roster API, and a throwaway employee reporting to the manager) and drove the *actual* workflow through every real endpoint: submitted 3 requests (sick/unpaid/sick) as the employee, approved all 3 as the manager, then P&C-confirmed 2 of them (leaving the 3rd at `manager_approved`, i.e. genuinely "hit her page after being accepted by the direct manager"). Confirmed via curl that `/leave-requests/balances/mine` returns `sick.used: 1, unpaid.used: 1` (the unpaid request spanned 2026-09-24→25, but 09-25 is a Friday — this org's weekend — so 1 *working* day is correct, not 2 calendar days; proves the reused working-day-counting logic handles the new buckets correctly, same as every existing one). Then browser-verified both surfaces live: Overview showed "1 Sick Leave taken this year" / "1 Unpaid Leave taken this year" for the employee; P&C's queue card for the still-pending 3rd request showed "Sick leave taken this year: 1 day · Unpaid leave taken this year: 1 day" directly under the reason text. No console errors on either page. All 3 throwaway employee records deactivated afterward (not deleted). |
| 2026-09-16 (same day) | **Corrected P&C's confirmation-queue counter to show the specific leave type each request is for, not a fixed sick+unpaid pair on every card** — user's own words: "show the counter of the requested leave type not sick and unpaid leaves as a rule." The previous same-day change (row above) always attached both `sickDaysUsedThisYear`/`unpaidDaysUsedThisYear` regardless of what the pending request actually was; a Planned Leave request, for instance, showed sick/unpaid numbers that had nothing to do with it. Generalized via `leaveBalanceRules.bucketForType` (previously unused outside a comment, and itself stale — it returned `null` for sick/unpaid even after they got real tracked buckets the same day) extended to cover all 6 types, plus a new `usageForLeaveType(balances, leaveType)` that picks the one relevant bucket. `listPcPending` now attaches a single `leaveTypeUsage` field (the capped `{used,total,remaining,...}` shape for planned/combined/wfh/excuse, the uncapped `{used,...}` shape for sick/unpaid, or `null` for public_holiday) instead of the two hardcoded numbers. | Portal §3 (Leave Balance) — refines the same-day P&C-queue feature above, not a new bullet | Every P&C card showed sick+unpaid regardless of the request's own type | Every P&C card shows only its own request's type — e.g. "Planned Leave: 0/14 used this year" for a planned request, "Sick Leave taken this year: 3 days" for a sick one | `server/src/modules/employees/services/leaveBalanceRules.js` (`bucketForType` extended to sick/unpaid, new `usageForLeaveType`); `server/src/modules/employees/services/timeOffService.js` (`listPcPending`'s `sickDaysUsedThisYear`/`unpaidDaysUsedThisYear` replaced by one `leaveTypeUsage` field); `server/src/modules/employees/views/js/team.js` (`sickUnpaidUsageHtml` replaced by `leaveTypeUsageHtml`, handles both the capped and uncapped shapes plus `null`) — working-tree change on `feature/email-and-forgot-password`, not yet committed | **API- and browser-verified end-to-end.** Reactivated the same throwaway manager/employee, submitted one new request of each of 3 types (sick, planned, unpaid) and manager-approved all 3, leaving them at `manager_approved` alongside the pre-existing sick request from the prior verification pass. Queried the real P&C queue via curl and confirmed each request's `leaveTypeUsage` matched only its own type (sick→`{used:3}` on both sick requests, planned→`{used:0,total:14,remaining:14}`, unpaid→`{used:1}`) — no cross-contamination between types on the same employee, and no sick/unpaid numbers leaking onto the planned request's card. Then confirmed the same live in a real browser: the planned request's card read "Planned Leave: 0/14 used this year" with no mention of sick or unpaid at all. No console errors. Deactivated the throwaway accounts again afterward. |
| 2026-09-16 (same day) | **Added "number of requested days" to every request card the direct manager or P&C sees deciding on it** — closes Portal §4's "Number of requested days" bullet, previously "Unable to Verify" (the day-count logic existed in `timeOffRules.js` but was never confirmed attached to what a request actually returns). User asked to be shown the dates and the day-count, computed from the start/end dates the requester entered — not trusted from the client, and asked to confirm the counting method before building it (working days, matching balance deduction, vs. raw calendar span): user chose working days. New `leaveBalanceRules.requestedDaysFor` mirrors the existing `dayCountForRequest`'s exact rule (partial_day halved) but takes the already-camelCase request shape `listTeam`/`listPcPending` work with, rather than a raw DB row — kept as a separate function alongside the existing one rather than refactoring its signature, to avoid touching the four already-working call sites inside `computeBalances`. A new `withRequestedDays` wrapper (mirroring the existing `withConflictWarnings` pattern) applies it in `listTeam` (both the company-wide `manager` branch and the normal direct-reports branch) and `listPcPending`. | Portal §4 (Time Off Requests) — "Number of requested days" bullet, was Unable to Verify | Request cards showed only the date range, no day-count anywhere the FE could read | Every card the direct manager or P&C sees now shows "· N days" (or "0.5 days" for a partial-day request) appended to the existing date range, computed fresh server-side every time, never trusted from the client | `server/src/modules/employees/services/leaveBalanceRules.js` (new `requestedDaysFor`); `server/src/modules/employees/services/timeOffService.js` (new `withRequestedDays`, applied in `listTeam` and `listPcPending`); `server/src/modules/employees/views/js/team.js` (`requestCard`'s meta line, new `fmtDays` helper) — working-tree change on `feature/email-and-forgot-password`, not yet committed | **API- and browser-verified on both audiences.** Submitted a fresh request spanning Thu→Sun (4 calendar days, crossing the Fri+Sat weekend) and confirmed via curl that both `listTeam` (direct manager) and `listPcPending` (P&C, after manager-approval) returned `requestedDays: 2`, not 4 — proving the weekend exclusion applies here too. Then confirmed the same live in a real browser on both accounts: the manager's "All My Direct Reports' Requests" card read "Planned Leave (PTO) · Oct 15, 2026 → Oct 18, 2026 (Full Day) · 2 days", and the same request's card on P&C's queue matched. Also spot-checked an existing 1-day request ("· 1 day", correct singular) and a 2-day one on a different card. No console errors on either account. Deactivated the throwaway accounts again afterward. Updated §4's score (93%→100%) and detail table to match. |
| 2026-09-16 (same day) | **Added the day(s)-of-week to every request card, alongside the date range and day count** — user's own words: "the day is not shown in the request... format should be for example sept 21 - sep 23, Mon - Wed, 3 days." New `fmtDayOfWeek` in `team.js` (abbreviated weekday via `toLocaleDateString`'s `weekday: 'short'`), shown as a single day for a one-day request or a `→` range for a multi-day one — inserted between the date range and the "(availability)"/day-count parts of `requestCard`'s existing meta line, reusing the same `→`/`·` separators already used elsewhere on that line rather than the literal hyphens in the user's example, for visual consistency with the rest of the app. | Portal §4 (Time Off Requests) — refines the same-day "Number of requested days" bullet above, not a new one | Request cards showed the date range and (as of the row above) a day count, but never which weekdays a request actually fell on | Every card reads e.g. "Oct 5, 2026 → Oct 6, 2026, Mon → Tue (Full Day) · 2 days" (multi-day) or "Sep 30, 2026, Wed (Full Day) · 1 day" (single-day) | `server/src/modules/employees/views/js/team.js` (new `fmtDayOfWeek`, `requestCard`'s meta line restructured to build the date-range and day-of-week segments once and interpolate both) — working-tree change on `feature/email-and-forgot-password`, not yet committed | **Browser-verified directly against real, live production-ish data**, not just test fixtures — logged into a real People & Culture team head's account (a real pending request from a real employee, "test test," was sitting in their queue) and confirmed the single-day format read correctly ("Sep 30, 2026, Wed (Full Day) · 1 day") without touching that real pending request (viewed only, no approve/reject clicked). Then confirmed the multi-day format on the throwaway test data already in the queue: "Oct 5, 2026 → Oct 6, 2026, Mon → Tue (Full Day) · 2 days" and "Oct 15, 2026 → Oct 18, 2026, Thu → Sun (Full Day) · 2 days" (the Thu→Sun one correctly shows the weekend days Fri/Sat are skipped in the day-count while still naming Thu and Sun as the actual span). No console errors. |
| 2026-09-16 (same day) | **Built the Requests Center — closes Portal §21 entirely, gathering what was scattered across three places.** User's own framing: "a lot of these requirements are done already but in different places, we need to gather them in a Requests section." Asked 4 clarifying questions before starting (audience, nav placement, what "activity/history log" means, which filter dimensions) since the scope forked significantly on each answer; user chose the broadest option every time: (1) a new "Requests" tab for everyone, showing their own request history *and*, for anyone with decision authority, the approval queues that used to live on "My Team" — moved there, not duplicated; (2) a new top-level tab, with "Time Off → My History" retired rather than kept as a duplicate; (3) a per-request timeline (submitted → manager decision → P&C decision/reviewed), not one combined feed; (4) category + status + date-range filters, on top of free-text search. Only two genuine "request" tables exist in the schema (`leave_requests`, `employee_profile_change_requests`) — confirmed by grepping every migration's `CREATE TABLE` before scoping anything; `conflict_pairs` is a detection/config table, not something a user submits, so it's out of scope (confirmed with the user via the audience question). New `GET /employees/profile-change-requests/mine` (`rosterService.getMyChangeRequestHistory`, backed by a new `employeeProfileChangeRequestRepository.findByEmployeeId` — the repository previously only had a *pending-only* lookup, since nothing before this needed an employee's full change-request history) is the one new endpoint; everything else is either an existing endpoint reused (`leave-requests/mine`, now also carrying `requestedDays` via the existing `withRequestedDays`) or code relocated verbatim from `team.js` into a new `requestsCenter.js` (all the request-card rendering, actions, and the approval-queue-building logic that used to live in `renderTeam`). `team.js` itself shrank to just "My Reporting Line" + "My Team" (member listing) — no request cards left in it at all. "My Requests" (the one genuinely new list) merges both request types client-side, sorted by `createdAt` descending, with search/filter and a `myRequestToggleTimeline` expand per row; leave-request cancellation (previously only reachable from the now-retired "My History") moved here too, using the same two-click inline-confirm pattern as before, not a native `confirm()`. | Portal §21 (Requests Center) — every bullet in the section | "Recent requests"/"Search filter"/"Activity log" were Not Started; "Request date/category/status/details" were marked Not Applicable specifically because nothing unified existed yet to test them against | All 7 bullets Implemented: unified "My Requests" (leave + profile changes), search + category + status + date-range filters, and a per-request timeline, all on one new tab; approval queues relocated there too | New `server/src/modules/employees/views/js/requestsCenter.js`; `server/src/modules/employees/repositories/employeeProfileChangeRequestRepository.js` (`findByEmployeeId`); `server/src/modules/employees/services/rosterService.js` (`getMyChangeRequestHistory`); `server/src/modules/employees/controllers/rosterController.js` (`myChangeRequestHistory`); `server/src/modules/employees/routes/index.js` (new route); `server/src/modules/employees/services/timeOffService.js` (`listMine` now also carries `requestedDays`); `server/src/modules/employees/views/js/team.js` (trimmed to ~180 lines, all request-card code removed); `server/src/modules/employees/views/js/timeOff.js` (History sub-tab, `renderHistory`, cancel handlers, `rejectionDetail`/`noManagerDetail` all removed); `server/src/modules/employees/views/index.html` (`#maintab-requests`/`#tab-requests` added; `#subtab-history`/`#subtab-panel-history` removed; My Team's subtitle updated); `server/src/modules/employees/views/js/main.js` (dispatch wired); `server/src/modules/employees/views/css/employees.css` (new `.requests-filter-bar`) — working-tree change on `feature/email-and-forgot-password`, not yet committed | **Fully browser- and API-verified end-to-end**, across three real account types (a real People & Culture team head's live account, plus the same throwaway manager/employee/P&C fixtures used earlier the same day). Confirmed: (1) the new tab renders "My Requests" (search text-filtering narrowed results correctly live; a real submitted profile-change request and 8 real leave requests all appeared, merged and sorted correctly) with a working timeline expand and a working two-click cancel (tested both the "Yes" and "No" paths, the latter reverting without a full page reload); (2) "My Team" no longer shows any request cards — only the reporting-line breadcrumb and team-member grid; (3) "Time Off" no longer has a "My History" sub-tab; (4) the relocated approval queues (Pending My Decision, All Requests, Pending P&C Confirmation, Pending Profile Changes) all still work exactly as before on the new tab, including against the *real* pending request already sitting in a real P&C account's queue, which was left untouched (viewed only, never approved/rejected) throughout testing. No console errors on any account. All throwaway employee records deactivated afterward (not deleted). Updated §21's score (15%→100%), its full detail table, the module-weighted view, and two "Recommended Implementation Order"/"Gaps and Contradictions" entries (struck through as closed) to match. |

| 2026-09-20 | **Added a CSV export button to the Users admin table — closes Governance §9's mandated quarterly access-list export gap.** User asked specifically for name/email/role columns only (no status/UUID/timestamps). Followed the existing CSV-export precedent exactly: `accountAdminService.exportUsersCsv` (same `csvEscape` quote/comma/newline handling as `dealsService.exportDealsCsv`, since names are free text) reuses the same `canManageUsers` permission check as `listUsers` rather than introducing a new gate; role is exported as the raw DB slug (e.g. `people_culture`), not the frontend's human-readable label, since no server-side label map exists and one wasn't worth adding for a single CSV column. Button placed in the Users tab's static page-header (not the dynamically-re-rendered table below it) via a new `.page-header-row` CSS modifier — added as a modifier rather than changing `.page-header` itself, since every other tab's page-header has title/subtitle as direct flex-unsafe children. | Governance §9 (mandated quarterly access-list export) | `listUsers` returned data on-screen only, with no export capability (`accountAdminController.js`) | "Export CSV" button on the Users tab, admin/manager/operations-gated same as the table itself, downloads `users-YYYY-MM-DD.csv` (Name, Email, Role) | `server/src/modules/auth/services/accountAdminService.js` (`csvEscape`, `exportUsersCsv`); `server/src/modules/auth/controllers/accountAdminController.js` (`exportUsers`); `server/src/modules/auth/routes/index.js` (`GET /users/export`); `server/src/modules/employees/views/js/usersAdmin.js` (`downloadUsersCsv`, `bindUsersAdminUi`, same raw-fetch+Blob pattern as `kpiHistory.js`/`deals.js`); `server/src/modules/employees/views/js/main.js` (wires `bindUsersAdminUi` into `bindUi()`); `server/src/modules/employees/views/index.html` (button markup); `server/src/modules/employees/views/css/employees.css` (`.page-header-row`) — working-tree change, not yet committed | Route-level verified only: started the real local dev server, confirmed `GET /api/users/export` and `GET /api/users` both return 401 identically with no auth token (proves the new route is wired under the same `authenticate` middleware, not accidentally left open), then stopped the server. Not yet browser-verified with a real logged-in session (no click-test of the button, no confirmation of actual downloaded file content) — should be done before considering this fully verified end-to-end. |

| 2026-09-20 (same day) | **Built "Upcoming Team Leave" on both My Team and Overview — closes Portal §7 entirely.** User's spec: any employee should see, on both tabs, which of their teammates have *approved* upcoming leave, showing only name and date range (deliberately no leave type, reason, or status). Re-verified the prior audit's §7 score (50%, "Partially Implemented — Backend only") against the actual code before building anything: it turned out inaccurate, not just incomplete — `listTeam` (the endpoint that row credited) is scoped to actual direct reports only for a non-manager caller (returns `[]` for anyone who doesn't manage anyone, i.e. most employees), not to "my team" in the department-inclusive sense the My Team tab itself already uses elsewhere, returns full request objects (reason/status/leaveType) rather than a privacy-minimized shape, and — confirmed by `grep -rn upcoming`, zero hits — was never actually consumed by any frontend code for this purpose on either tab. Built new instead of repurposing it: `leaveRequestRepository.findApprovedUpcoming({employeeIds, fromDate})` (approved-only, `end_date >= fromDate`, scoped to a caller-supplied id set — never company-wide); `timeOffService.listUpcomingTeamLeave({actorEmployee})` resolves "my team" via the existing `teamMembership.resolveTeamMembership` (the same direct-reports-union-department rule `rosterService.getMyTeam` already uses for the My Team member grid, injected into `timeOffService` for this — previously only `rosterService`/`kpiScoringService` depended on it), then maps to `{employeeId, name, startDate, endDate}` only. No employee profile → `[]`, same leniency as `getMyTeam`/`listTeam`. | Portal §7 (Upcoming Team Leave) — every bullet, previously 50% ("Upcoming team leave": Partially Implemented; "Team leave calendar (UI)": Unable to Verify) | No frontend surface on either tab; the one backend endpoint the prior audit credited was scoped to direct-reports-only and returned full request detail, not name+dates | Both My Team and Overview show an "Upcoming Team Leave" card (tile grid, same visual pattern as "Who's Off Today") listing every teammate (direct reports ∪ same department, minus the viewer) with an approved leave request whose end date is today or later — name and date range only | `server/src/modules/employees/repositories/leaveRequestRepository.js` (`findApprovedUpcoming`); `server/src/modules/employees/services/timeOffService.js` (`listUpcomingTeamLeave`, new `teamMembership` dependency); `server/src/modules/employees/container.js` (wires `teamMembership` into `createTimeOffService`); `server/src/modules/employees/controllers/timeOffController.js` (`upcomingTeamLeave`); `server/src/modules/employees/routes/index.js` (`GET /employees/leave-requests/team/upcoming`); `server/src/modules/employees/views/js/overview.js` (`upcomingLeaveTile`, `upcomingTeamLeaveSection`); `server/src/modules/employees/views/js/team.js` (`upcomingLeaveTile`, `upcomingTeamLeaveSectionHtml`) — working-tree change, not yet committed | **Fully API- and browser-verified end-to-end against the real local dev DB**, not mocks. Built a real 3-account scenario via the actual endpoints: a throwaway `content`-department teammate submitted a real future planned-leave request (Oct 5–6, 2026), which auto-routed to P&C (no manager assigned) since the throwaway account had none; a second throwaway account was registered and role-elevated to `people_culture` via the real admin role-change endpoint to confirm it to `approved`. Confirmed via curl: (1) baseline empty `[]` before any test data existed; (2) the approved Oct 5–6 request appeared correctly, name+dates only, once truly `approved`; (3) a `manager_approved`-only (not yet P&C-confirmed) request for the same teammate correctly did NOT appear — status gating verified, not assumed; (4) a same-department co-worker saw the entry, but the requester's own view of their own team correctly excluded their own request (self-exclusion, matching `findByDepartment`'s existing `e.id != excludeId` rule); (5) a throwaway account in a *different* department (`ai_innovation`) saw `[]` — cross-department isolation confirmed, not just same-department inclusion. Then browser-verified live, logged into the real admin/CEO-adjacent account (`test test`): the "Upcoming Team Leave" card rendered correctly on both Overview and My Team, reading "Upcoming LeaveTest — Oct 5, 2026 → Oct 6, 2026"; also incidentally picked up a real pre-existing approved future leave request for that account when viewed from the teammate's side, confirming the query works against real, not just synthetic, data. No console errors on fresh page load on either tab. All three throwaway accounts deactivated afterward at both layers (login via `/api/users/:id/deactivate` and employee record via `/api/employees/:id/deactivate`, not deleted) and the empty state ("No upcoming approved leave for your team") re-confirmed live on both tabs afterward. |

| 2026-09-20 (same day) | **Full re-test of Portal §8–§10 (KPI Overview, KPI Details, Performance History) — role-based authorization, API, database, and browser, per explicit user request ("test them... every possible test").** No code changed in this pass; this was pure verification against the existing implementation (already built in `14a1baf`, 2026-09-07). Built 5 throwaway accounts wired into real relationships (a direct manager, that manager's report with `kpiProfile: 'production'`, an unrelated employee, and two elevated-role accounts — `people_culture` and `operations`) to test every combination of viewer against three distinct backend gates: `canViewBreakdown` (view), `enterManualScore` (write a score), `setEmployeeTarget` (write a per-employee target). All 3 gates were tested against all 6 actor types via real HTTP calls (not code reading alone), then cross-checked against raw DB rows (`kpi_scores`, `kpi_employee_targets`, `audit_log`, `employee_kpi_notifications`), then re-confirmed live in a real logged-in browser session for the actually-reachable paths (self-view, direct-manager view/entry, trend chart, CSV export). Found and corrected one real audit error (§10's "Performance trend" was wrongly scored — the chart already existed pre-baseline) and documented two previously-unrecorded authorization nuances (admin cannot enter manual scores unless literally the target's manager; admin's KPI-tab picker never exposes an unrelated employee even though the API would allow viewing one) — both intentional per the code's own comments, not bugs, but worth having on record. | Portal §8 (100%, unchanged), §9 (100%, unchanged), §10 (100%, corrected from 88%) | §10 scored 88% on a stale "no trend chart" claim; §8/§9's authorization boundaries were previously only "Manually Verified" (code-read), not live-tested | All three sections confirmed 100% working end-to-end; §10 corrected to 100%; full authorization matrix now has live evidence instead of code-reading alone | No code changes — `docs/governance/implementation-tracker.md` only | **Every layer tested, nothing assumed:** (1) *API*: full 6-actor × 3-gate authorization matrix via real HTTP calls with real tokens — 18 combinations, every result matched the code exactly (admin/P&C bypass view, self/direct-manager view, unrelated/operations denied view; P&C/direct-manager-by-FK allowed to enter scores, admin/self/unrelated/operations denied; admin/manager-role allowed to set targets, P&C/direct-manager-by-FK/unrelated/operations denied) — plus edge cases (missing `quarter` param → 400, unknown `metricId` → 404, non-`ratio_employee_target` metric rejected, negative target rejected). (2) *Database*: after each successful write, queried the raw SQLite DB directly (`kpi_scores`, `kpi_employee_targets`, `audit_log`, `employee_kpi_notifications`) and confirmed every field — actor id, computed score, comment text, notification recipient — matched what was sent, not just what the API echoed back. (3) *Computation*: hand-calculated the expected weighted breakdown for 3 real entered metrics (a binary "met"→100, a binary "no"→0, a `ratio_employee_target` 20/25→80) and confirmed the live API's `pillarB.total`, `final.total`, `final.achievementPct`, and `statusBand` all matched exactly (9/40, 9, 9.0%, "At Risk"). (4) *Browser*: logged into three different real sessions (the direct manager, the employee themself, and — separately — confirmed admin's own picker via the real admin account) — visually confirmed the quarter score, status badge, Pillar A/B breakdown, per-metric target/actual/weight/comment fields, the "View Performance Details" → History flow, a real 2-bar trend chart, and a real click-triggered CSV download whose content was verified byte-for-byte against the API and DB. Zero console errors across all sessions. All 5 throwaway accounts deactivated afterward at both the login and employee-record layers (not deleted); their `kpi_scores`/`kpi_employee_targets`/`audit_log` rows were left in place as historical records, per this repo's revoke-never-delete convention. |

| 2026-09-20 (same day) | **Verified the KPI framework's fidelity against its actual source spreadsheet (`KPI_Framework.xlsx`, all 8 sheets) and corrected an overstated claim about ClickUp automation coverage — per explicit user request** ("is this fetching statuses from ClickUp lists?... check all pages in the file"). No code changed. Read every sheet (Overview, Content Creators, Art Director, AI Designer, Production Team, Account Manager, People & Culture, Scoring Calculator) via a temporary Python/openpyxl script and hand-compared every metric's name, weight, target, and scoring formula against `kpiFrameworkSeed.data.js`. Separately traced the full ClickUp-automation code path (`kpi_auto_metric_mappings` → `computeAutoScores` → `kpiClickupMetricsService.js`) and queried the live DB to determine how much of it is actually wired up versus just labeled as if it were. | Portal §9 (KPI Details) — adds two new findings, doesn't change existing scores; corrects a factual claim in the Executive Summary (§1) that was never verified against the DB when originally written | Executive Summary claimed ClickUp was "the real data source... for most Pillar B metrics"; framework transcription accuracy and `heads`/`design` profile coverage had never been checked against the source spreadsheet at all | Framework transcription confirmed accurate for all 6 real profiles (zero discrepancies beyond one correctly-resolved spreadsheet ambiguity); ClickUp automation confirmed real but minimal (2 of ~60 metrics configured, manual-trigger only, near-zero accumulated event data); `heads`/`design` confirmed as a dormant schema/UI gap | No code — `docs/governance/implementation-tracker.md` only (§1 Executive Summary paragraph corrected; §9 detail table gained two new rows; §9 Gaps and Contradictions gained two new bullets) | **Every claim traced to source, nothing taken on faith.** Read the actual `.xlsx` file cell-by-cell (not a cached summary or prior transcription) via `openpyxl` in a disposable venv (cleaned up afterward). Cross-checked all ~62 Pillar B metric rows across 6 role sheets against `kpiFrameworkSeed.data.js` line-by-line — every name/weight/target/formula matched, including three-decimal band boundaries (e.g. `94.999`) that only make sense if someone actually read the formula text carefully the first time this was built. Confirmed the P&C sheet's internal Growth-vs-People-and-Retention weight contradiction (50% literal vs. 40% intended) was deliberately and correctly resolved in code, with the reasoning already documented in that file's own comment — not something this pass had to fix, just verify. Queried `kpi_auto_metric_mappings` (2 rows), `kpi_clickup_status_events` (2 rows), and `employees.kpi_profile` (zero `heads`/`design` rows) directly against the real local dev DB, not inferred from code alone. Confirmed zero Odoo-integration code exists anywhere in `server/src` via a repo-wide grep, settling definitively that `'odoo'`-labeled metrics are UI-badge-only. |

| 2026-09-21 | **Added a date-range filter to the Leave Report tab (manager/P&C/admin per-employee leave-request breakdown).** User asked for a single "Date Range" button opening a calendar popover: click a start date, then click an end date, which applies the filter immediately — no separate Apply button or Enter keypress. Built a new vanilla-JS `dateRangePicker.js` (single-month calendar, prev/next nav, no library — this app has no calendar dependency and none was warranted for one filter) rather than two native `<input type="date">` fields, since the user specifically described a two-click range-picking interaction, not two separate date pickers. `timeOffService.getLeaveBreakdown` gained optional `startDate`/`endDate` (both-or-neither, validated `YYYY-MM-DD`, end not before start) and now filters by **overlap** (`row.start_date <= endDate && row.end_date >= startDate`), not "starts inside the range" — a multi-day request spanning into the window still counts, matching how the rest of this app treats leave-date-range queries (e.g. `findApprovedOverlapping`). | *(new finding, not a doc bullet)* — UX addition to the existing Leave Report admin tool (Portal §3 area, not a named requirement bullet in either source doc) | Leave Report showed only an employee picker — no way to scope the breakdown to a period; "export leave history" (§22 gap, still open) is a separate, larger aggregate-export feature this doesn't address | Leave Report has a "Date Range" button next to the employee picker; picking a start+end date narrows every count in the breakdown table to requests overlapping that window, with a "×" to clear back to all-time | New `server/src/modules/employees/views/js/dateRangePicker.js` (`mountDateRangeFilter`); `server/src/modules/employees/services/timeOffService.js` (`getLeaveBreakdown` range params + overlap filter); `server/src/modules/employees/controllers/timeOffController.js` (`getLeaveBreakdown` reads `req.query.startDate`/`endDate`); `server/src/modules/employees/views/js/leaveBreakdown.js` (`currentRange` state, wires the picker in, appends the query string) — `feature/leave-report-date-range-filter` | **Browser- and network-verified live** against the real local dev DB: logged in as the seed owner (admin role), opened Leave Report, confirmed the calendar popover renders correctly in dark theme, that a first click sets the start (highlighted, hint changes to "Click an end date") and a second click immediately applies the filter and closes the popover with no third click — captured the resulting request via `read_network_requests`: `GET /api/employees/leave-requests/9/breakdown?startDate=2026-09-15&endDate=2026-09-26` → 200. Also verified: the "×" clear button removes the params and refetches all-time; reopening the picker after a completed range starts a fresh selection rather than extending the old one; prev/next month navigation preserves an in-progress "click an end date" state across the boundary; a cross-month range (Aug 28 → Sep 5) applies correctly with the right query params. No console errors. Backend-only: no unit/integration test added for the new overlap-filter branch (this repo has no automated test suite — see Testing Status note in §8/§16's other rows). |

## 17. Appendix — Role Mapping (Governance doc ↔ actual code)

| Governance doc role | Code equivalent | Notes |
|---|---|---|
| Employee | `employee` | Direct match |
| HR | `people_culture` | Functional match; naming differs |
| Operations | `operations` | Direct match |
| Admin | `admin` | Direct match; admin-shielding logic in `common/permissions.js` |
| CEO | `manager` | **The code's `manager` role literally is the CEO's account** — confirmed by comments in `rosterService.js:10`, `timeOffService.js:133`, `ceo-dashboard/routes/index.js:9-11`, `employees/views/js/main.js:13,30` |
| Manager (line manager) | *No dedicated role* — enforced via `employees.manager_employee_id` FK | A real employee can approve leave for their direct reports regardless of their own auth role, because `managerDecision`'s authorization checks the FK relationship, not a role string (`timeOffService.js:231-238`) |
| Commercial | *No dedicated role* | Commercial Leads module is gated to `USER_MANAGER_ROLES` (`admin`/`manager`/`operations`) — the same broad set used elsewhere, not a role scoped to Commercial staff |
| *(no doc equivalent)* | `finance` | Exists in the role enum, assignable via the Users admin UI, **zero permission gates found anywhere** — a dead role as of this audit |
| *(no doc equivalent)* | `is_team_head` (employee flag, not an auth role) | Closest code concept to the doc's "Department Head" — a per-employee boolean, independent of `role` |

---

## 18. Functionality Beyond Either Requirement Document

Real, substantial, live features that exist in the codebase with no mention in either requirement document — tracked here so a future audit doesn't mistake them for scope creep or miss them entirely:

- **`/planner` — Margin Planner**: the original "Pricing Portal" project-costing/quoting tool this whole repo is named for. Gated to `USER_MANAGER_ROLES`.
- **`/commercial-lead` — Commercial Leads dashboard**: a live, ClickUp-webhook-driven sales/deals pipeline (deal stages, stage-duration analytics, quarterly KPIs for the Commercial team). Built essentially in one day (`9249f0d`/`74ab150`, 2026-08-13).
- **`/ceo` — CEO Dashboard**: an executive summary dashboard, originally a mocked-data prototype (`bb29011`, 2026-08-23), later partially wired to real Margin Planner cost data.
- **Conflict-pair management**: a leave-overlap warning system between paired employees (e.g. two people who must not be out simultaneously) — not described in either doc, warn-only, never blocks submission.
- **Transactional email (Resend) + Forgot Password**: the app's first outbound-email capability, added 2026-09-15 — see §16 Change History. Neither requirement doc mentions self-service password reset at all (the Governance doc's account lifecycle is entirely HR-request → Ops-approval → Admin-provision, with no reset path named).

These should be tracked as **Not Applicable / Out of documented scope** in any percentage calculation, not silently ignored.

---

## 19. Future Update Workflow

When a future session is asked to **"update the CEAS Portal implementation tracker,"** follow this sequence. The goal is evolution, not regeneration — this file's history (§12 Regression Tracking, §13 Decision Log, §15 Progress History, §16 Change History) is itself the record of how the project moved; overwriting it destroys that record.

1. **Read this file in full first.** Do not start from the requirement documents alone — you need to know what was already found, what was already flagged as "Open," and what the last "Last Verified" date was for the area you're about to touch.
2. **Re-read the relevant requirement document section(s)** — `CEAS_Portal_Governance_Workflow.pdf` and/or `Employee_Portal_Sections_Requirements.pages` (the latter is Apple's binary iWork format; export it via `osascript` driving Pages.app to PDF, then `pdftotext -layout`, the same approach used to produce this version — do not attempt to parse the `.pages` bundle's `.iwa` files directly).
3. **Inspect the current codebase** for the area in question — routes, controllers, services, repositories, models, and migrations, not just the frontend. A UI element, route, or DB column existing is not evidence a feature works end-to-end; verify the logic behind it.
4. **Inspect the current website/running app** where practical — the dev server can be started with `npm start`/`npm run dev` in `server/`; check `lsof -i :3001` first in case it's already running (it was, throughout this audit). Use authenticated, read-only `curl` calls (seed credentials in `server/.env`, `SEED_OWNER_USERNAME`/`SEED_OWNER_PASSWORD`, login via `POST /api/auth/login` with `{"email":..., "password":...}`) against a **real dev copy**, never production, and avoid mutating shared dev data unless the user explicitly asks for a live write-path test.
5. **Inspect recent Git history** — `git log --oneline -30`, and targeted `git log --grep` searches for the feature area — to see what actually shipped since the last "Last Verified" date on the rows you're revisiting.
6. **Run relevant tests where practical.** As of this version, **zero automated tests exist in this repository** (`npm test` → "Missing script"); if that has changed, note it explicitly as a major status change (see step 13) — don't silently assume it's still true.
7. **Compare the current implementation against this tracker's existing rows** for the area in question, not just against the requirement doc from scratch — you're looking for *drift* (regressions, newly-fixed gaps, newly-changed scope), not re-deriving everything.
8. **Identify newly implemented features** — update their row's Progress/Depth/Evidence/Testing columns in §3, and add a line to §4 Implemented if it's now fully done.
9. **Identify newly partially-implemented features** — same, moving items between §5/§6/§7 as appropriate.
10. **Identify newly discovered gaps** — add rows to §3/§6/§9 (Gaps and Contradictions) as needed; do not delete a gap that turns out to still be open, just confirm it's still accurate.
11. **Identify regressions** — anything previously marked Implemented that is now broken, removed, or degraded gets a **new row in §12 Regression Tracking** (never edit a past regression row's "Previous state"/"Current state" — those are historical facts about that point in time). Cross-reference the specific commit that caused it if findable via `git log`/`git blame`.
12. **Update Testing Status (§8)** for anything you actually exercised this pass — be honest about what's still only "Manually Verified" via code-reading vs. what you clicked through in a real browser or ran through an automated test.
13. **Update the Overall Progress percentages (§2)** — recompute using the same bullet-level methodology already documented there (or explicitly document a methodology change if you're deliberately changing it — never silently change how the number is computed without saying so).
14. **Append a new row to §15 Progress History** — never overwrite the existing rows, even if the methodology changed.
15. **Append rows to §16 Change History** for every meaningful change** (new features, completed requirements, regressions, changed requirements, new/resolved gaps, new/failed tests, fixed bugs, architecture/database/permission changes, integrations added/changed, important decisions). Skip purely cosmetic file changes — this log is for things a stakeholder would care about.
16. **Add rows to §13 Decision Log** for any new architectural/product decision discovered or made during the update — tag each as **Existing decision** (found in code/docs, not made this session), **Recommended decision** (your suggestion, not yet accepted), or **Open decision** (genuinely unresolved) — never record a decision that wasn't actually made or proposed.
17. **Update §14 Questions / Decisions Needed** — remove a question once it's been genuinely answered (and note where/how, e.g. "resolved 2026-10-01: user confirmed the ✅ mark should be removed"), and add any new ones surfaced this pass.
18. **Preserve everything else.** Do not delete historical rows in §12/§13/§15/§16 even if they look resolved or superseded — mark their `Status`/`Resolution` column instead. Do not regenerate §17 (Role Mapping) or §18 (Functionality Beyond Either Requirement Document) from scratch — update them in place if the underlying facts changed.

**A note on parallel research agents:** this initial audit used five parallel sub-agents to cover the codebase's functional areas concurrently, which is why §0's Audit History looks the way it does. That's a valid approach for future large updates too (e.g., "re-audit everything" after months of development), but for a small, targeted update ("did the leave balance feature ship yet?") a single focused read is more appropriate — match the effort to the size of the actual change being verified, and always have one coordinating pass review and reconcile whatever the sub-agents produce before treating it as final, the way this version's §0/§2 corrections were made.
